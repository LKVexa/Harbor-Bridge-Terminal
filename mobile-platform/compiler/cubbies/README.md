# Cubby runtime helpers

- `registry.js` — allocate/list mobile cubbies (`MC-NNN`), persist `fleet/CUBBIES.json`, session files.
- `sessions/` — live projection session JSON (gitignored).

Gateway: `bridge-terminal/gateway/cubby-projection.js`, `cubby-access-gate.js`, `mobile-auth-hook.js`.
