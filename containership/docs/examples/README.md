# Contract examples

These are schema fixtures, not executable workloads or genuine artifact identities. The repeated-character digests in graph.json are synthetic placeholders for validating shape and dependencies only; they do not authenticate files. request.json does not run a command, and its generation must not be assumed current.
