{
  "schema": "PK_ECP_PERF_THRESHOLDS/1",
  "owner": "INV-66 service owner (see OWNERSHIP.md)",
  "notes": "Contract SLO: admission p99 < 50 ms (1% budget). Measured with the durable journal (fsync ON) on the reference host class in perf/REFERENCE_HOST.md. Regression gate fails if any metric is worse than threshold, or worse than the retained baseline by more than regression_tolerance.",
  "admission_latency_ms": {"p50": 10, "p95": 30, "p99": 50, "max": 250},
  "throughput_min_per_s": 100,
  "startup_replay_ms_per_10k_records_max": 5000,
  "rss_growth_mb_per_10k_admissions_max": 16,
  "journal_bytes_per_admission_max": 6000,
  "regression_tolerance": 0.25
}
