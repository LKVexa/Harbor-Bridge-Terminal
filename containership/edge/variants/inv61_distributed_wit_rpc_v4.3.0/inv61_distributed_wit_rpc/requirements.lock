# INV-61 4.3.0 runtime lock: the runtime has ZERO third-party dependencies.
# Build-time only (not shipped): setuptools>=68, wheel>=0.41 (from the build host).
# gate extra: pk_core==4.0.* - hash cannot be recorded until the artifact is supplied (W-002).
