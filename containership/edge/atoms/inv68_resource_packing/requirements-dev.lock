# INV-68 development/evidence tooling lock (MC-09, MC-12). The runtime has NO third-party dependencies.
# Exact versions observed in the 4.3.0 evidence environment (CPython 3.11, Linux x86_64).
# Hashes could not be collected in this build environment (package index downloads unavailable);
# add --hash=sha256:... entries from a trusted mirror before using this lock with pip --require-hashes.
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
referencing==0.37.0
rpds-py==0.30.0
attrs==26.1.0
typing_extensions==4.15.0
setuptools==79.0.1  # bundled by ensurepip in the build venv
# wheel: not required (setuptools>=70 builds wheels itself)
