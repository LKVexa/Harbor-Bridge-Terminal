# EOL policy (M34)

Supported: current minor + previous minor. Each minor gets security fixes for 12 months after the next minor ships. Python: versions in `compatibility/matrix.json` marked `supported`; dropping one needs a minor bump and a deprecation note one release earlier. Schema v1 lives until v2 has been served for one full minor.
