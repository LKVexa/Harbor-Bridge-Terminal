# Traceability (M31)

`traceability/INV65_RTM.json` is generated by `tools/build_rtm.py` and checked by `tools/check_rtm.py` (run in CI and by `tests/test_release_tooling.py`). Status is never upgraded by prose: `verified-local` requires a cited, existing test; `partial`/`blocked` require a named blocker; owner/reviewer stay `UNASSIGNED` until real people are recorded.
