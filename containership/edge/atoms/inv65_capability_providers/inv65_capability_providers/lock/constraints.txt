# M36 reproducible lock. Runtime: stdlib only (nothing to pin).
# Optional extra [crypto] -- pinned to the version verified in this build environment.
# Hashes: add with `pip hash` from the owner's approved wheel mirror; not fetched from here.
cryptography==46.0.7
# pk_core: UNRESOLVED (see lock/pk_core.lock.json)
