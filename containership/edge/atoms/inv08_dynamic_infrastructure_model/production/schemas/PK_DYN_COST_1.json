{
  "$id": "PK_DYN_COST/1",
  "title": "Node-hour cost record per pool and interval",
  "type": "object",
  "required": ["schema", "schema_minor", "pool_id", "interval_start", "interval_end", "node_hours"],
  "additionalProperties": false,
  "properties": {
    "schema": {"const": "PK_DYN_COST/1"},
    "schema_minor": {"type": "integer", "minimum": 0, "maximum": 1000},
    "pool_id": {"type": "string", "minLength": 1, "maxLength": 64},
    "tenant": {"type": "string", "minLength": 1, "maxLength": 64},
    "interval_start": {"type": "number", "minimum": 0},
    "interval_end": {"type": "number", "minimum": 0},
    "node_hours": {"type": "number", "minimum": 0},
    "unit": {"enum": ["node_hour"]},
    "ext": {"type": "object"}
  }
}
