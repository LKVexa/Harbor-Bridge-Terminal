[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "inv15-new-asynchronous-abi"
version = "4.3.0"
description = "INV-15 PK_ASYNC 1.1 reference host (stdlib-only reference model; not a production host)"
requires-python = ">=3.10"
dependencies = []
license = {text = "UNDECLARED - owner to choose"}

[tool.setuptools]
packages = ["inv15_new_asynchronous_abi", "inv15_new_asynchronous_abi.wire"]
package-dir = {"inv15_new_asynchronous_abi" = "."}
