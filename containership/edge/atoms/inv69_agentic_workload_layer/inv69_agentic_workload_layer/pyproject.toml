[build-system]
requires = ["setuptools==75.1.0"]
build-backend = "setuptools.build_meta"

[project]
name = "inv69-agentic-workload-layer"
version = "4.3.0"
description = "INV-69 Agentic workload layer: governed agent tool execution (stdlib-only runtime)"
requires-python = ">=3.10,<3.14"
dependencies = []
license = { text = "UNDECIDED - see THIRD-PARTY-NOTICES.md; owner must choose (W-001 follow-up)" }

[project.optional-dependencies]
test = ["jsonschema==4.26.0"]

[tool.setuptools]
packages = []
