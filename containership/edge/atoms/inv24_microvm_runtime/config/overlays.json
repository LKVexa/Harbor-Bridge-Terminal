{
  "environments": {
    "cloud":      {"environment": "cloud"},
    "datacenter": {"environment": "datacenter"},
    "near-edge":  {"environment": "near-edge", "max_instances": 128, "max_instances_per_tenant": 16, "admission_queue_depth": 64},
    "far-edge":   {"environment": "far-edge", "max_instances": 16, "max_instances_per_tenant": 4, "admission_queue_depth": 8,
                   "per_tenant_queue_depth": 4, "permitted_devices": ["virtio-block", "virtio-vsock", "serial", "rtc"]}
  }
}
