{
  "$id": "inv36.health/1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "live", "state", "ready", "reasons", "dependencies", "seconds_since_progress", "seconds_since_peer_exchange", "queue_saturation"],
  "properties": {
    "schema": {"const": "inv36.health/1"},
    "live": {"type": "boolean"},
    "state": {"enum": ["ready", "degraded", "not_ready"]},
    "ready": {"type": "boolean"},
    "reasons": {"type": "array", "items": {"type": "string", "maxLength": 128}},
    "dependencies": {"type": "object"},
    "seconds_since_progress": {"type": "number", "minimum": 0},
    "seconds_since_peer_exchange": {"type": ["number", "null"]},
    "queue_saturation": {"type": "number", "minimum": 0}
  }
}
