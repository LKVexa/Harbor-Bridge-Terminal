{
  "schema": "INV29_WAIVER_REGISTER/1",
  "rules": [
    "P0 components can never be waived",
    "every waiver needs owner + security-owner approval, a mandatory expiry, and a remediation issue",
    "a waiver applies only to the exact version and environment named",
    "expired, unapproved or mismatched waivers are ignored by evidence.derive_gate (the item stays a blocker)"
  ],
  "fields": ["id", "component", "requirements", "rationale", "risk", "compensating_controls", "owner", "approvers", "created", "expires", "remediation", "status", "applies_to_version", "applies_to_environment"],
  "waivers": []
}
