# Pinned, reproducible dependency constraints (A2). Hashes: add with `pip hash` when publishing from a
# controlled index; this archive was built offline, so hashes are not recorded (see release/evidence.json).
cryptography>=41,<47          # Ed25519 artifact statements (runtime)
jsonschema>=4.18,<5           # contract-schemas lane (test only)
# pk_core==UNRESOLVED         # external checklist framework - no published version/digest (W-07)
