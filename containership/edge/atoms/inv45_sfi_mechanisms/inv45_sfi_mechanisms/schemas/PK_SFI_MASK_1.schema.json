{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:pk:schema:PK_SFI_MASK:1",
  "title": "PK_SFI_MASK/1 access result",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "requested", "effective", "inside_region"],
  "properties": {
    "schema": {"const": "PK_SFI_MASK/1"},
    "requested": {"type": "integer"},
    "effective": {"type": "integer", "minimum": 0},
    "inside_region": {"type": "boolean"}
  }
}
