# Governance review records

One file per review (access, policy, dependency, configuration, architecture) named `YYYY-MM-DD-<type>.md`, signed by the reviewer named in `docs/OWNERSHIP.md`. No reviews have been held yet (MC-087 open_human).
