// golden fixture for the INV-11 -> INV-16 declaration lowering
package pk:demo@1.0.0;

interface api {
    async func fetch;
    func now;
    async func stream-body;
}

interface admin {
    async func drain;
    func now;          // same name as api#now: identity must not collide
}
