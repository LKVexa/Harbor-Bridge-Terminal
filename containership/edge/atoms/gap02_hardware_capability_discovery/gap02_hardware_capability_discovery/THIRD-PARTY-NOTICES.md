# Third-party notices

GAP-02 4.3.0 bundles **no third-party code**. All modules are original and use only the Python standard library.
Optional at runtime (not bundled): `pk_core` (suite-internal), `opentelemetry-api`.

Vendor tools invoked at runtime when present on the host (not redistributed): `nvidia-smi`, `rocm-smi`, `clinfo`, `system_profiler`, `sysctl`, `smartctl`, `ethtool`, Windows PowerShell CIM/`Get-Tpm`.
