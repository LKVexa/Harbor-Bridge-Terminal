# RB-01-deploy-and-verify — PROPOSED, drills NOT executed

Install: `pip install .` (pyproject). Verify: `python3 -B ci/ci.sh` locally; `health()['ready']` true; config_rev expected.

Owner: UNASSIGNED. Last drill: never.
