# RB-02-config-rollout — PROPOSED, drills NOT executed

Sign a revision with the parent `rev_id`; stage with `canary.Rollout`; INSUFFICIENT or FAIL rolls back automatically; manual rollback: `ConfigStore.rollback(rev)`.

Owner: UNASSIGNED. Last drill: never.
