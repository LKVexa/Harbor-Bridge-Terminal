[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sch01-workload-classification-and-runtime-placement"
version = "4.3.0"
description = "SCH-01 workload classification and runtime placement engine"
requires-python = ">=3.10"
dependencies = []
license = {text = "UNDETERMINED - see LICENSE-STATUS.md"}

[project.optional-dependencies]
pkcore = []   # pk_core is external (EXT-01) and not published

[tool.setuptools]
package-dir = {"sch01_workload_classification_and_runtime_placem" = "."}
packages = ["sch01_workload_classification_and_runtime_placem"]
