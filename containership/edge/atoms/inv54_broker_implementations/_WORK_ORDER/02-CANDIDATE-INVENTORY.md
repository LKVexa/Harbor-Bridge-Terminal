# 02 — Candidate inventory

The yard was unreachable, so no donor search could run. Every item below is **build-new**, and the searches are still owed to a later session. Keywords recorded for that session: `kafka adapter confluent`, `rabbitmq pika confirm`, `sqs boto3 visibility`, `hash chain audit hmac`, `append-only log crc recovery`, `lease epoch fencing`, `token bucket admission`, `w3c traceparent`, `cyclonedx sbom`.

Pattern-only precedents from earlier shop passes (not copied, and not re-read in this session): the fail-closed gate plus "a skipped check is never a PASS" rule (`_YARDOFFICE/spb` qualification; `Project_Architecture_Studio` gates), the chained evidence head, and AES-256-GCM replacing CBC (`Project_Embedded_Quality_Gate` fix).

| Need (components) | Donor | Result |
|---|---|---|
| all 100 | — | build-new (yard unreachable) |
