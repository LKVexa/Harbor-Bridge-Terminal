# INV-54 v4.3.0 dependency lock (component 35).  Runtime core: NO third-party dependencies.
# Optional extras, exact pins.  Hashes must be added with `pip-compile --generate-hashes`
# in the certification environment (network access required) -- status: UNVERIFIED.
cryptography==46.0.7      # extra: encryption  (verified present in build container)
confluent-kafka==2.6.1    # extra: kafka      (NOT installed/verified in build container)
pika==1.3.2               # extra: rabbitmq   (NOT installed/verified in build container)
boto3==1.35.99            # extra: sqs        (NOT installed/verified in build container)
