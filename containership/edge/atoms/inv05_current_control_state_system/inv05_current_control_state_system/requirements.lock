# INV-05 runtime lock (MC-049-01). Stdlib-only core; cryptography is required when
# storage.encrypt_at_rest=true (default).  Versions are exact.
#
# HASHES PENDING (EX-009): the build sandbox had no package-index access, so the
# --hash lines could not be generated here.  In the release environment run:
#   pip-compile --generate-hashes --output-file requirements.lock requirements.in
# CI installs with --require-hashes and therefore FAILS CLOSED until this is done.
cryptography==46.0.7
cffi==2.0.0
pycparser==3.0
