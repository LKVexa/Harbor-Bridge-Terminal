package p:t@1.0.0;
interface t {
  record point { x: s32, y: s32, }
  variant shape { circle(u32), rect(point), empty }
  enum color { red, green, blue }
  flags perms { read, write, exec }
  draw: func(s: shape, c: color, p: perms) -> point;
}
