# Runtime dependency lock for pln02-application-plane 4.3.0.
# The runtime is stdlib-only, so this lock is intentionally empty. Optional extras are pinned in
# pyproject.toml; add `--hash=sha256:` lines here if an extra becomes a production dependency.
