# PLN-05 dependency lock (PLN05_LOCK/1) — must match pyproject.toml [project].dependencies
# runtime third-party dependencies: none (stdlib only)
# optional framework tier (UNRESOLVED; not installable from any index reachable here):
#   pk_core>=4.0,<5.0
# build: stdlib only (tools/build_release.py writes wheel and sdist with zipfile/tarfile)
# interpreter used for the 4.2.0 evidence run: CPython 3.11.15
