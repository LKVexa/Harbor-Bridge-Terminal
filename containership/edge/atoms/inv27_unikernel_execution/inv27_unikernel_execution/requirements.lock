# INV-27 4.3.0 — runtime dependency lock
# The runtime is stdlib-only (verified by tools/deps_check.py). Nothing to pin for the base install.
# Interpreter: CPython >=3.10; evidence produced on CPython 3.11.15, Linux x86_64.
# Vendored: pk_core (see _vendor/PK_CORE_PROVENANCE.json for per-file sha256).
# Optional extra [crypto]:
#   cryptography==46.0.7   # version present in the build container; hashes not pinned (no index access recorded)
