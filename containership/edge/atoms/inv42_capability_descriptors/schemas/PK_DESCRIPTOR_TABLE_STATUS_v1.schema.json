{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:post-kubernetes:PK_DESCRIPTOR_TABLE_STATUS:1",
  "title": "PK_DESCRIPTOR_TABLE_STATUS/1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema", "version", "live", "issued", "closed", "destroyed",
    "table_limit", "session_allocation_limit", "foreign_resolutions",
    "closed_reuse_attempts", "type_mismatches", "invalid_descriptors",
    "capacity_rejections", "fork_rejections"
  ],
  "properties": {
    "schema": {"const": "PK_DESCRIPTOR_TABLE_STATUS/1"},
    "version": {"const": "PK_DESCRIPTOR/2"},
    "live": {"type": "integer", "minimum": 0},
    "issued": {"type": "integer", "minimum": 0},
    "closed": {"type": "integer", "minimum": 0},
    "destroyed": {"type": "boolean"},
    "table_limit": {"type": "integer", "minimum": 1},
    "session_allocation_limit": {"type": "integer", "minimum": 1},
    "foreign_resolutions": {"type": "integer", "minimum": 0},
    "closed_reuse_attempts": {"type": "integer", "minimum": 0},
    "type_mismatches": {"type": "integer", "minimum": 0},
    "invalid_descriptors": {"type": "integer", "minimum": 0},
    "capacity_rejections": {"type": "integer", "minimum": 0},
    "fork_rejections": {"type": "integer", "minimum": 0}
  }
}
