# Release tooling pins. Owner must regenerate hashes with:
#   pip-compile --generate-hashes --allow-unsafe  (for cryptography>=42 and coverage>=7)
# CI runs pip with --require-hashes, so an unhashed or unpinned entry fails the gate by design.
