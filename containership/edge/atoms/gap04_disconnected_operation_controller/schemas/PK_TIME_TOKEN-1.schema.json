{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "PK_TIME_TOKEN/1",
  "title": "Signed time token v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "version",
    "time",
    "nonce",
    "key_id",
    "issuer",
    "alg",
    "sig"
  ],
  "properties": {
    "version": {
      "const": "PK_TIME_TOKEN/1"
    },
    "time": {
      "type": "integer",
      "minimum": 0
    },
    "nonce": {
      "type": "string",
      "pattern": "^[0-9a-f]{32}$"
    },
    "key_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"
    },
    "issuer": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"
    },
    "alg": {
      "const": "Ed25519"
    },
    "sig": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]+$"
    }
  }
}
