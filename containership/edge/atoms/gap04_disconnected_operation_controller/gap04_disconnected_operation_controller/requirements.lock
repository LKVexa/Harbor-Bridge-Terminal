# GAP-04 v4.3.0 runtime dependency lock (GAP04-C45)
# Core state machine (controller.py): stdlib only, CPython >= 3.10.
# Production runtime (runtime/): the pinned set below. Exact versions were resolved and
# tested on CPython 3.11.15 / linux x86_64 on 2026-09-22.
#
# HASHES_PENDING: this build environment had no PyPI access, so per-wheel sha256 hashes
# could not be fetched. Before release, regenerate with hashes on a networked builder:
#   pip-compile --generate-hashes --output-file requirements.lock requirements.in
# and install with:  pip install --require-hashes --no-deps -r requirements.lock
# Tracked as waiver W-004 in docs/WAIVERS.md (blocks C45-005 / C46 Production-GO).
cryptography==46.0.7 ; python_version >= "3.10"
cffi==2.0.0 ; platform_python_implementation == "CPython"
pycparser==3.00
