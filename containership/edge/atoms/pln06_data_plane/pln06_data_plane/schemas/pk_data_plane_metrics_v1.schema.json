{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "PK_DATA_PLANE_METRICS/1",
  "title": "PLN-06 runtime metrics snapshot",
  "type": "object",
  "required": ["schema", "inflight", "inflight_limit", "admitted_by_tier", "bytes_by_tier", "residency_refusals", "backpressure_events", "completed", "cancelled"],
  "properties": {
    "schema": {"const": "PK_DATA_PLANE_METRICS/1"},
    "inflight": {"type": "integer", "minimum": 0},
    "inflight_limit": {"type": "integer", "minimum": 1},
    "per_tenant_inflight_limit": {"type": ["integer", "null"], "minimum": 1},
    "admitted_by_tier": {"type": "object"},
    "bytes_by_tier": {"type": "object"},
    "residency_refusals": {"type": "object"},
    "backpressure_events": {"type": "integer", "minimum": 0},
    "completed": {"type": "integer", "minimum": 0},
    "cancelled": {"type": "integer", "minimum": 0}
  },
  "additionalProperties": false
}
