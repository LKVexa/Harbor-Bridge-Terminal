{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "PK_TRANSPORT_TIER/1",
  "title": "PLN-06 transport tier",
  "type": "object",
  "required": ["name", "max_bytes"],
  "properties": {
    "name": {"enum": ["inline", "local", "bulk"]},
    "max_bytes": {"type": "integer", "minimum": 0},
    "locality_floor": {"enum": ["auto", "in_process", "same_node", "same_host_vm", "remote"]}
  },
  "additionalProperties": false
}
