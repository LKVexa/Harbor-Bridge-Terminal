{
  "schema": "PK_POLL_PERF_THRESHOLDS/1",
  "status": "PROPOSED",
  "note": "Proposed by the v4.3.0 build from its own first measurement with ~10x headroom. Not approved; an owner must set real SLO-derived values per deployment cell.",
  "thresholds": [
    {"metric": "ready_poll_us.p99", "max": 500},
    {"metric": "wake_latency_us.p99", "max": 20000},
    {"metric": "timeout_overshoot_ms.p99", "max": 20},
    {"metric": "fanout_us.4096.p99", "max": 100000},
    {"metric": "import_ms.max", "max": 2000}
  ]
}
