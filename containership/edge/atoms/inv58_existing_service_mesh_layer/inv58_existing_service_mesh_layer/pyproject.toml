[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "inv58-existing-service-mesh-layer"
version = "4.3.0"
description = "INV-58 Existing service-mesh layer: retry ownership, identity hand-off, bypass detection, boundary security"
requires-python = ">=3.10,<3.14"
dependencies = []
# Licence intentionally unset: owner decision pending (NOTICE.md).

[project.optional-dependencies]
test = ["jsonschema>=4.18,<5"]
framework = []  # pk_core: external, UNPINNED (governance/bom.json) - supply version + sha256 to pin

[tool.inv58]
parent_inheritance = "Standalone. Historically shipped inside the Post-Kubernetes Master Prompt & Workflow Series v4.0.0 source inventory (group 01_Source_Inventory); nothing is inherited implicitly - every dependency, CI step and release rule is declared in this directory."
release_gate = "python -B tools/release_gate.py"
