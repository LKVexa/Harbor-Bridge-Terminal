# Evidence ledger

Generated by `verify.py`: `gate-<version>.json` (sealed when `INV35_EVIDENCE_KEY` is set).

Rollout drills (G13): `rollout-<version>.json` with fields `{candidate_tree_digest, stages[], rollback_exercised, operator, date}`, sealed with `runtime.release.seal`.
