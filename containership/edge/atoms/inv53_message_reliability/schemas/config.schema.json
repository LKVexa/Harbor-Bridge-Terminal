{
 "keys": {
  "audit_required": {
   "default": true,
   "description": "Fail closed when the audit sink cannot record.",
   "max": null,
   "min": null,
   "type": "bool"
  },
  "breaker_failure_threshold": {
   "default": 5,
   "description": "Consecutive storage failures that open the breaker.",
   "max": 1000,
   "min": 1,
   "type": "int"
  },
  "breaker_reset_seconds": {
   "default": 30.0,
   "description": "Open-breaker cool-down before a half-open probe.",
   "max": 3600,
   "min": 0.001,
   "type": "float"
  },
  "fsync": {
   "default": true,
   "description": "fsync each journal record before acknowledging the caller.",
   "max": null,
   "min": null,
   "type": "bool"
  },
  "max_attempts": {
   "default": 5,
   "description": "Delivery attempts before dead-lettering.",
   "max": 100,
   "min": 1,
   "type": "int"
  },
  "max_dead_letters": {
   "default": 100000,
   "description": "Hard cap on dead letters per queue.",
   "max": 10000000,
   "min": 1,
   "type": "int"
  },
  "max_in_flight": {
   "default": 10000,
   "description": "Hard cap on leased messages per queue.",
   "max": 1000000,
   "min": 1,
   "type": "int"
  },
  "max_lease_extension_seconds": {
   "default": 300.0,
   "description": "Largest single visibility extension.",
   "max": 43200,
   "min": 0.001,
   "type": "float"
  },
  "max_message_bytes": {
   "default": 262144,
   "description": "Largest accepted serialized message.",
   "max": 16777216,
   "min": 64,
   "type": "int"
  },
  "max_ready": {
   "default": 100000,
   "description": "Hard cap on ready messages per queue.",
   "max": 10000000,
   "min": 1,
   "type": "int"
  },
  "require_authentication": {
   "default": true,
   "description": "Refuse unauthenticated calls (secure default).",
   "max": null,
   "min": null,
   "type": "bool"
  },
  "shed_ready_ratio": {
   "default": 0.9,
   "description": "Shed new puts when ready depth exceeds this share of max_ready.",
   "max": 1.0,
   "min": 0.01,
   "type": "float"
  },
  "stall_seconds": {
   "default": 120.0,
   "description": "No progress while work is ready for this long => stalled.",
   "max": 86400,
   "min": 0.001,
   "type": "float"
  },
  "tenant_burst": {
   "default": 1000,
   "description": "Token-bucket depth per tenant.",
   "max": 1000000,
   "min": 1,
   "type": "int"
  },
  "tenant_rate_per_second": {
   "default": 500.0,
   "description": "Token-bucket refill per tenant.",
   "max": 1000000,
   "min": 0.001,
   "type": "float"
  },
  "visibility_seconds": {
   "default": 30.0,
   "description": "Lease visibility timeout.",
   "max": 43200,
   "min": 0.001,
   "type": "float"
  }
 },
 "schema": "inv53.config/1"
}
