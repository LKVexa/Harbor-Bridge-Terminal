{
  "$comment": "C031 dependency lock. The primitive, runtime, adapters and gate need only the CPython standard library. pk_core is required only by the audit integration (component.py, contract.py, tests/test_component.py); it is NOT in the supplied archive, so it is unpinned and the gate reports PK_CORE_MISSING as BLOCKED. Update policy: pins change only via a reviewed PR that updates sha256 and CHANGELOG.",
  "schema": "INV18_LOCK/1",
  "python": ">=3.10,<3.14",
  "dependencies": [
    {"name": "python-stdlib", "kind": "runtime", "version": ">=3.10", "required": true, "sha256": null, "license": "PSF-2.0", "source": "CPython distribution"},
    {"name": "pk_core", "kind": "first-party", "version": null, "required": true, "scope": "audit integration only", "sha256": null, "source": "Post-Kubernetes Master Prompt & Workflow Series v4.0.0 estate (owner's projects); to be vendored at vendor/pk_core", "license": "unknown (first-party, owner to confirm)", "status": "BLOCKED"}
  ],
  "vulnerability_scanning": "no third-party packages; pk_core to be scanned once vendored (REVIEWS.json dependencies_sbom, 30 d)",
  "update_policy": "monthly review; security fixes within the patch SLAs of docs/OPERATIONS.md"
}
