# Support — INV-25

SLOs (from `contract.py`): 100 % review coverage, zero legacy-emulation devices, a surface diff for every
version change — no error budget on any. Operational support commitments follow
`docs/incident-response.md`; support window follows `docs/vulnerability-eol.md`. Channel: PENDING owner.
