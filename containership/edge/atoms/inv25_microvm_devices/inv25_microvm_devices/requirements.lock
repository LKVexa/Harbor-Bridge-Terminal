# Runtime: standard library only - no third-party runtime dependencies to lock.
# Build: setuptools==75.8.0 (pinned in pyproject.toml build-system).
# pk_core: PENDING source declaration; once declared add e.g.
#   pk_core==<x.y.z> --hash=sha256:<digest>
