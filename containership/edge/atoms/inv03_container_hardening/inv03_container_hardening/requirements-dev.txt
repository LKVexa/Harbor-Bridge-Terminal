# Development / CI tools only. The runtime (policy.py, hardening/) has zero third-party dependencies.
# Pinned by version; hash locking (pip-compile --generate-hashes) needs network access to the index
# and is left for the first CI run (checklist item 65 stays PARTIAL until then).
jsonschema==4.26.0
ruff==0.15.11
mypy==1.20.2
