{
 "$id": "urn:pk:inv28:PK_TOOLCHAIN_SELECTION_REQUEST-1",
 "$schema": "https://json-schema.org/draft/2020-12/schema",
 "additionalProperties": false,
 "properties": {
  "abi": {
   "maxLength": 64,
   "pattern": "^([a-z0-9][a-z0-9._+\\-/]*)?$",
   "type": "string"
  },
  "architecture": {
   "maxLength": 64,
   "pattern": "^[a-z0-9][a-z0-9._+\\-/]*$",
   "type": "string"
  },
  "devices": {
   "items": {
    "maxLength": 64,
    "pattern": "^[a-z0-9][a-z0-9._+\\-/]*$",
    "type": "string"
   },
   "maxItems": 64,
   "type": "array",
   "uniqueItems": true
  },
  "environment": {
   "maxLength": 64,
   "pattern": "^[a-z0-9][a-z0-9._+\\-/]*$",
   "type": "string"
  },
  "features": {
   "items": {
    "maxLength": 64,
    "pattern": "^[a-z0-9][a-z0-9._+\\-/]*$",
    "type": "string"
   },
   "maxItems": 64,
   "type": "array",
   "uniqueItems": true
  },
  "hypervisor": {
   "maxLength": 64,
   "pattern": "^([a-z0-9][a-z0-9._+\\-/]*)?$",
   "type": "string"
  },
  "language": {
   "maxLength": 64,
   "pattern": "^[a-z0-9][a-z0-9._+\\-/]*$",
   "type": "string"
  },
  "provider": {
   "maxLength": 64,
   "pattern": "^([a-z0-9][a-z0-9._+\\-/]*)?$",
   "type": "string"
  },
  "runtime": {
   "maxLength": 64,
   "pattern": "^([a-z0-9][a-z0-9._+\\-/]*)?$",
   "type": "string"
  },
  "schema": {
   "const": "PK_TOOLCHAIN_SELECTION_REQUEST/1"
  },
  "site": {
   "oneOf": [
    {
     "type": "null"
    },
    {
     "additionalProperties": false,
     "properties": {
      "architectures": {
       "items": {
        "maxLength": 64,
        "pattern": "^[a-z0-9][a-z0-9._+\\-/]*$",
        "type": "string"
       },
       "maxItems": 64,
       "minItems": 1,
       "type": "array",
       "uniqueItems": true
      },
      "hypervisors": {
       "items": {
        "maxLength": 64,
        "pattern": "^[a-z0-9][a-z0-9._+\\-/]*$",
        "type": "string"
       },
       "maxItems": 64,
       "type": "array",
       "uniqueItems": true
      },
      "providers": {
       "items": {
        "maxLength": 64,
        "pattern": "^[a-z0-9][a-z0-9._+\\-/]*$",
        "type": "string"
       },
       "maxItems": 64,
       "type": "array",
       "uniqueItems": true
      },
      "site_id": {
       "maxLength": 64,
       "pattern": "^[a-z0-9][a-z0-9._+\\-/]*$",
       "type": "string"
      }
     },
     "required": [
      "architectures",
      "hypervisors",
      "providers",
      "site_id"
     ],
     "type": "object"
    }
   ]
  },
  "tenant": {
   "maxLength": 512,
   "minLength": 1,
   "type": "string"
  },
  "workload_id": {
   "maxLength": 512,
   "minLength": 1,
   "type": "string"
  }
 },
 "required": [
  "architecture",
  "environment",
  "language",
  "tenant",
  "workload_id"
 ],
 "title": "INV-28 selection request",
 "type": "object"
}
