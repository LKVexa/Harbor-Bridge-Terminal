{
 "$id": "urn:pk:inv28:PK_TOOLCHAIN_SELECTION-1",
 "$schema": "https://json-schema.org/draft/2020-12/schema",
 "additionalProperties": false,
 "properties": {
  "architecture": {
   "maxLength": 64,
   "pattern": "^[a-z0-9][a-z0-9._+\\-/]*$",
   "type": "string"
  },
  "eliminated": {
   "items": {
    "maxLength": 512,
    "minLength": 1,
    "type": "string"
   },
   "type": "array"
  },
  "environment": {
   "maxLength": 64,
   "pattern": "^[a-z0-9][a-z0-9._+\\-/]*$",
   "type": "string"
  },
  "language": {
   "maxLength": 64,
   "pattern": "^[a-z0-9][a-z0-9._+\\-/]*$",
   "type": "string"
  },
  "maturity": {
   "enum": [
    "experimental",
    "beta",
    "mature"
   ]
  },
  "reason": {
   "maxLength": 512,
   "minLength": 1,
   "type": "string"
  },
  "schema": {
   "const": "PK_TOOLCHAIN_SELECTION/1"
  },
  "toolchain": {
   "maxLength": 512,
   "minLength": 1,
   "type": "string"
  }
 },
 "required": [
  "architecture",
  "eliminated",
  "environment",
  "language",
  "maturity",
  "reason",
  "schema",
  "toolchain"
 ],
 "title": "INV-28 selection result v1 (deprecated; emitted only by component.ToolchainRegister)",
 "type": "object"
}
