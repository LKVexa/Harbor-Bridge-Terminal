# GAP-09 operator runbooks (component 56)

Generated by `tools/runbooks.py` from `checklist/bindings.py`. Escalation owner for every
entry: **UNASSIGNED** (component 56 stays BLOCKED until an owner and on-call rota are named).

## Shared procedure

1. **Identify** — read the stable error `code` (never parse messages). Codes: `malformed`,
   `unauthenticated`, `unauthorized`, `unverifiable`, `revoked`, `expired`,
   `dependency_unavailable`, `throttled`, `quota_exceeded`, `quarantined`, `corrupted`,
   `config_rejected`, `time_untrusted`, `replay_detected`, `reporter_untrusted`, `internal_error`.
2. **Contain** — `QuarantineRegistry.freeze(scope, name, reason=…, actor=…)` isolates a
   reporter, site or tenant without stopping unrelated ingestion.
3. **Preserve** — copy the audit ledger and record `AuditLedger.head()`; verify it against the
   last external anchor before any restart.
4. **Recover** — config `rollback()`; state `durable.restore()` (verifies digests first);
   replay a site with `ingest.reconcile()`.
5. **Close** — record the decision in the audit ledger and the waiver registry if a debt remains.

Security-sensitive validation never degrades to caller trust: if a trust dependency is down,
ingestion stops (`dependency_unavailable` / `time_untrusted`). That is the intended state.

## Per-component entries

| # | Component | Symptom | Action |
|---|---|---|---|
| 01 | Real GAP-06 attestation adapter | attestation reject spike (codes unverifiable/expired/revoked) | check root set + policy version; revoke_device(); freeze reporter via QuarantineRegistry |
| 02 | Real GAP-07 signature/provenance adapter | reporter_untrusted with reason unverifiable | compare transcript key_id/fingerprint; revoke key; rotate |
| 03 | Durable replay protection | replay_detected on legitimate retries | confirm submission id reuse; ids are single-use once signature verifies (ADR-001 #5) |
| 04 | Key/certificate lifecycle | key compromise | KeyRegistry.revoke(); rotate() new key; audit ledger shows lifecycle |
| 05 | mTLS/authenticated transport | TLS handshake failures | check client CA bundle; principal_from_peercert CN; expired certs |
| 06 | Secret/KMS and at-rest encryption integration | dependency_unavailable purpose=wal | a KMS provider is not configured; persistence of sensitive state is refused by design |
| 07 | Central authorization-policy integration | all queries unauthorized/expired | policy bundle expired or not loaded -> load a newer signed bundle |
| 08 | Authenticated query-principal binding | 401 unauthenticated on /v1/query | token kid unknown, audience or window wrong; check gateway issuer keys |
| 09 | Time authority / clock-skew policy | time_untrusted everywhere | trusted source unsynchronised > max_sync_age; ingestion halts by design until sync returns |
| 10 | Canonical cross-language signing profile | signature mismatch from a non-Python reporter | diff reporter canonical text against canonical.canonicalize() for the same envelope |
| 11 | Durable local write-ahead buffer | WAL at disk bound (quota_exceeded) | reconnect and reconcile(); raise max_bytes only with capacity approval |
| 12 | Admission control and backpressure | 429 throttled storms | inspect DecisionLog rules ADM-*; tune buckets via config (audited) |
| 13 | Per-tenant cardinality quotas | quota_exceeded for one tenant | confirm with quota.usage(); raise override only via config change |
| 14 | Tamper-evident security audit ledger | audit verify fails (corrupted) | stop writers, preserve file, compare head against external anchor, open incident |
| 15 | Production configuration subsystem | config_rejected on apply | active version unchanged; fix document; rollback() available |
| 18 | Evidence-gate hardening | a record claims PASS | engine.validate refuses PASS without owner+reviewer; re-run engine |
| 19 | Network service / RPC handlers | 5xx from /v1/* | codes are stable; internal_error means a bug -- capture request id and reproduce |
