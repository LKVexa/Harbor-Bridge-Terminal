# Third-party notices

No third-party source code was copied into this package in 4.3.0. The yard map, ledger and donor search were unavailable (the yard office holds only `work-orders/`). Sibling work orders were read for **patterns only**: fail-closed release gate with a falsifier, declared skip lanes, and never minting approvals. No files were copied from them.

Runtime dependencies: none (CPython standard library only). Optional external: `pk_core` (not supplied, unpinned) and the tier runtimes listed in `ops/EXTERNAL_DEPENDENCIES.json`. Each carries its own licence when an operator installs it.
