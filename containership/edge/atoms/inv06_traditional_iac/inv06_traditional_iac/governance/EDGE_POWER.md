# MC-055 — Edge power/thermal benchmark

Status: **EXTERNAL — requires constrained edge hardware with power instrumentation.** Harness hook: run `release.run_benchmarks()` on the device while sampling power (e.g. RAPL / INA219) and record energy-per-operation = joules / ops. No measurement has been made; no claim is made.
