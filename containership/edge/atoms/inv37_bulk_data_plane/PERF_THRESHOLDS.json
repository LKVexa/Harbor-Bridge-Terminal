{
  "schema": "INV37_PERF_THRESHOLDS/1",
  "status": "PROPOSED - pending performance/architecture owner approval (see APPROVALS.json)",
  "absolute": {
    "shm_zero_copy_receiver.data_plane_copies": {"max": 0},
    "shm_zero_copy_receiver.peak_alloc_over_size": {"max": 0.01},
    "legacy_copy_receiver.peak_alloc_over_size": {"max": 2.1},
    "load_profiles.recovery.recovered": {"eq": true},
    "service.startup_s": {"max": 0.5}
  },
  "relative_to_baseline": {
    "manifest_mib_s": {"min_ratio": 0.7},
    "shm_zero_copy_receiver.mib_s": {"min_ratio": 0.7},
    "legacy_copy_receiver.mib_s": {"min_ratio": 0.7},
    "shm_zero_copy_receiver.chunk_latency_s.p99": {"max_ratio": 1.5},
    "legacy_copy_receiver.chunk_latency_s.p99": {"max_ratio": 1.5},
    "durable_checkpoint_fsync.chunk_latency_s.p99": {"max_ratio": 2.0},
    "service.startup_s": {"max_ratio": 2.0}
  },
  "note": "Relative thresholds are only meaningful against a baseline captured on the same hardware class. The committed baseline is from the audit sandbox and is NOT a production baseline."
}
