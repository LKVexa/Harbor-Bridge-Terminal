# G12-B023 — DNS resolver resilience (P0)

_Generated from `evidence/registry.py`; lifecycle **validation**._

## Normative requirement
- **Success:** an answer with provenance within the overall deadline, or a distinct DNS_* reason.
- **Failure:** any other outcome, reported with a registered reason code (`wan/reasons.py`); failures never silently fall back to a less-trusted path.
- **Degraded:** a dependency is down/stale (see `wan.state.degraded_policy`); existing trusted sessions continue until trust expiry, new ones are refused.
- **Unsupported:** DNSSEC validation, DoT/DoH, TCP fallback on truncation — reported as `UNSUPPORTED` / NOT-EVIDENCED, never emulated.
- **Policy rejection:** `POLICY_*` reason codes (egress, mechanism disabled, quarantine, untrusted peer, config rejected).

## Interfaces
- `wan.dns.Resolver`
- `wan.dns.DnsServer`
- `wan.dns.decode_response`

## Dependencies and trust boundaries
- configured resolvers

## Configurable parameters
- `query_timeout` (validated at construction; see module docstring for units/ranges)
- `overall_timeout` (validated at construction; see module docstring for units/ranges)
- `race_delay` (validated at construction; see module docstring for units/ranges)
- `max_concurrent` (validated at construction; see module docstring for units/ranges)
- `max_stale` (validated at construction; see module docstring for units/ranges)
- `negative_cap` (validated at construction; see module docstring for units/ranges)

## Protocol map
- See the module docstring of the interfaces above for the RFC/specification sections implemented and the explicit deviations.

## Operator diagnostics
- Answer.source fresh|cache|stale|negative-cache; DNS_MISMATCH = spoof attempt
- Escalate (do not auto-retry) on AUTH_* reasons, DNS_MISMATCH, audit-chain breaks and quota exhaustion.
- Disable: kill-switch control naming the mechanism (`wan.rollout.KillSwitch`), or `mechanisms.<name>=false` in config.
