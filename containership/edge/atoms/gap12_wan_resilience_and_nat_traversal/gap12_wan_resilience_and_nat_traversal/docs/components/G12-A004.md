# G12-A004 — UDP hole-punch implementation (P0)

_Generated from `evidence/registry.py`; lifecycle **validation**._

## Normative requirement
- **Success:** both peers receive an HMAC-valid punch packet from the ticket's endpoint before the window closes.
- **Failure:** any other outcome, reported with a registered reason code (`wan/reasons.py`); failures never silently fall back to a less-trusted path.
- **Degraded:** a dependency is down/stale (see `wan.state.degraded_policy`); existing trusted sessions continue until trust expiry, new ones are refused.
- **Unsupported:** port prediction for symmetric NATs (falls to relay by design) — reported as `UNSUPPORTED` / NOT-EVIDENCED, never emulated.
- **Policy rejection:** `POLICY_*` reason codes (egress, mechanism disabled, quarantine, untrusted peer, config rejected).

## Interfaces
- `wan.holepunch.punch`
- `wan.holepunch.Rendezvous`
- `wan.holepunch.Ticket`
- `wan.holepunch.verify_ticket`

## Dependencies and trust boundaries
- rendezvous service (trusted, keyed per peer)
- peer NAT behaviour

## Configurable parameters
- `MAX_PROBES` (validated at construction; see module docstring for units/ranges)
- `INTERVAL_MIN` (validated at construction; see module docstring for units/ranges)
- `INTERVAL_MAX` (validated at construction; see module docstring for units/ranges)
- `prime_ttl` (validated at construction; see module docstring for units/ranges)
- `window` (validated at construction; see module docstring for units/ranges)

## Protocol map
- See the module docstring of the interfaces above for the RFC/specification sections implemented and the explicit deviations.

## Operator diagnostics
- PunchResult.cause: coordination | nat_filtering | mapping_change | local_firewall | unreachable
- Escalate (do not auto-retry) on AUTH_* reasons, DNS_MISMATCH, audit-chain breaks and quota exhaustion.
- Disable: kill-switch control naming the mechanism (`wan.rollout.KillSwitch`), or `mechanisms.<name>=false` in config.
