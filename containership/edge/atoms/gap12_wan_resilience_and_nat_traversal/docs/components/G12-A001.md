# G12-A001 — STUN client (P0)

_Generated from `evidence/registry.py`; lifecycle **validation**._

## Normative requirement
- **Success:** a Binding success whose transaction id, source and FINGERPRINT/INTEGRITY (when configured) verify, yielding a server-reflexive address; discovery is OK only when >= 1 server answered and all answering servers agree.
- **Failure:** any other outcome, reported with a registered reason code (`wan/reasons.py`); failures never silently fall back to a less-trusted path.
- **Degraded:** a dependency is down/stale (see `wan.state.degraded_policy`); existing trusted sessions continue until trust expiry, new ones are refused.
- **Unsupported:** STUN over TCP/TLS/DTLS; ALTERNATE-SERVER redirect is reported, not followed; PASSWORD-ALGORITHMS — reported as `UNSUPPORTED` / NOT-EVIDENCED, never emulated.
- **Policy rejection:** `POLICY_*` reason codes (egress, mechanism disabled, quarantine, untrusted peer, config rejected).

## Interfaces
- `wan.stun.binding`
- `wan.stun.discover`
- `wan.stun.transact`
- `wan.stun.decode`
- `wan.stun.StunServer`

## Dependencies and trust boundaries
- STUN servers (UDP 3478): untrusted network input
- DNS resolver: dependency

## Configurable parameters
- `rto` (validated at construction; see module docstring for units/ranges)
- `rc` (validated at construction; see module docstring for units/ranges)
- `rm` (validated at construction; see module docstring for units/ranges)
- `deadline_s` (validated at construction; see module docstring for units/ranges)

## Protocol map
- See the module docstring of the interfaces above for the RFC/specification sections implemented and the explicit deviations.

## Operator diagnostics
- reason NET_UDP_BLOCKED = every server silent; DEP_INCONSISTENT = servers disagree (symmetric NAT); DEP_MALFORMED = parser rejected answers
- Escalate (do not auto-retry) on AUTH_* reasons, DNS_MISMATCH, audit-chain breaks and quota exhaustion.
- Disable: kill-switch control naming the mechanism (`wan.rollout.KillSwitch`), or `mechanisms.<name>=false` in config.
