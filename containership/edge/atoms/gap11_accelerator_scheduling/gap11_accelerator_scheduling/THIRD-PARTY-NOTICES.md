# Third-party notices — GAP-11 v4.3.0

No third-party code, data or text is included. The control plane is written against the Python standard library only (release/SBOM.cdx.json lists zero third-party components).

Chop-shop note: this pass was run from the two supplied archives in the cloud workspace. The linked computer's `GitHub Junkyard` folder was reachable for listing but has no `_YARDOFFICE` (no ledger, no map), so no donor car was searched or pulled. Patterns reused (not code) are the shop's own conventions: fail-closed gates, evidence-first records, and the NO_GO falsifier.

The v4.2.0 package was supplied without a licence file; `pyproject.toml` records the licence as UNSPECIFIED rather than inventing one.
