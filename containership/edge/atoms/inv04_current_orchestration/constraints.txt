# INV-04 dependency lock strategy (component 64)
# Runtime: stdlib only - there is nothing to pin for production installs.
# Build backend (pyproject [build-system]) and developer tooling are pinned exactly:
setuptools==75.8.0
ruff==0.15.11
mypy==1.20.2
# Integration (optional extra "pk-core"): pk_core==4.0.* from the internal estate source, never a public index.
