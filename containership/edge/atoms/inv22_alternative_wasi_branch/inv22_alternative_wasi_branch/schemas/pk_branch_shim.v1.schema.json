{
  "$id": "urn:pk:PK_BRANCH_SHIM:1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Max request 64 KiB, depth 16, 2048 items. Translation is pure and idempotent: same request => same response bytes except correlation_id. No partial outputs; errors never carry a payload.",
  "properties": {
    "contract": {
      "const": "PK_BRANCH_SHIM/1"
    },
    "correlation_id": {
      "maxLength": 128,
      "minLength": 1,
      "type": "string"
    },
    "deadline_ms": {
      "maximum": 60000,
      "minimum": 1,
      "type": "integer"
    },
    "encoding": {
      "const": "json-canonical"
    },
    "interface": {
      "maxLength": 128,
      "minLength": 1,
      "type": "string"
    },
    "operation": {
      "maxLength": 128,
      "minLength": 1,
      "type": "string"
    },
    "payload": {},
    "source_branch": {
      "enum": [
        "standards",
        "fork"
      ]
    },
    "target_branch": {
      "enum": [
        "standards",
        "fork"
      ]
    }
  },
  "required": [
    "contract",
    "interface",
    "operation",
    "source_branch",
    "target_branch",
    "encoding",
    "payload",
    "correlation_id"
  ],
  "title": "PK_BRANCH_SHIM/1 request",
  "type": "object"
}
