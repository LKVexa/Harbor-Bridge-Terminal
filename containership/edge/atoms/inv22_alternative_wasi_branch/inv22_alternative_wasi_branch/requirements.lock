# INV-22 runtime lock (MC-04). Exact versions of the runtime closure verified in the build environment.
# Hashes: this build environment has no package-index access, so artifact hashes are NOT yet recorded.
# CI must regenerate with:  pip-compile --generate-hashes --strip-extras pyproject.toml -o requirements.lock
# and install with:        pip install --require-hashes -r requirements.lock
cryptography==46.0.7
cffi==2.0.0
pycparser==3.0
