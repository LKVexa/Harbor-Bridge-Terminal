# INV-63 Threat Model (STRIDE)

| Field | Value |
|---|---|
| Document ID | INV63-ARCH-THREAT-MODEL |
| INV-63 C-IDs covered | C041, C042, C043, C044, C045, C046, C047, C048, C049, C050 (also C023, C024, C039, C085, C087) |
| Status | DRAFT — pending approval |
| Owner | Security contact (role) — UNASSIGNED |
| Reviewers | Security reviewer (role), Service owner (role), Architecture reviewer (role) — UNASSIGNED |
| Revision | 4.3.0 |
| Approval date | pending |
| Supersedes | none |
| Change-review triggers | Revisit when interfaces, state ownership, topology or dependencies change, and on any change to `security.py`, `schema.py`, `store.py` or trust boundaries in `BOUNDARIES.md`. |

Assets: desired-state journal, lattice placement authority, token/data/signing keys, tenant isolation, audit trail. Trust boundaries TB-1..TB-6 are defined in `BOUNDARIES.md`. Tests are in `tests/`; names are `file::Class::method`.

## STRIDE table

| # | STRIDE | Threat / actor | Control (file::symbol) | Test | Residual risk |
|---|---|---|---|---|---|
| T-01 | S | Forged/expired/foreign token (hostile caller) | `security.py::TokenAuthority.verify` (HMAC-SHA256, `hmac.compare_digest`, iss/aud, nbf/exp ± `max_skew_s`, kid) | `test_security.py::AuthNTest::test_spoofed_expired_and_foreign_tokens_rejected` | Symmetric HMAC: any holder of a token key can mint tokens for any subject/tenant/role. Key compromise = full compromise |
| T-02 | S/R | Token replay | `TokenAuthority.verify` jti nonce cache (`replay_window` 100000) → `INV63-E-REPLAY` | `test_security.py::AuthNTest::test_token_replay_detected` | Nonce cache is in memory, per process: replay across restart or across instances within token TTL (default 300 s) is not detected (waiver DEBT-002) |
| T-03 | S | Request replay via idempotency key reuse with different body | `service.py::DeploymentService.handle` idempotency digest → `INV63-E-CONFLICT` | `test_service.py::IdempotencyTest::test_idempotency_key_replay_and_conflict` | LRU of 10000 keys in memory; replayed from journal on restart (outcome only) |
| T-04 | E | Privilege escalation (tenant role calls operator op) | `security.py::ROLES`, `Authorizer.check`, `OP_CAPABILITY` | `test_security.py::AuthZTest::test_privilege_escalation_blocked`, `::test_roles_are_minimal` | Roles are carried in the token claims; trust rests on key secrecy (T-01) |
| T-05 | I/T/E | Malicious tenant reads/modifies another tenant | `security.py::namespace`, `Authorizer.check` (`INV63-E-TENANT-ISOLATION`), per-tenant `tenant_quotas`, `Admission` buckets | `test_security.py::AuthZTest::test_cross_tenant_access_blocked`, `::test_tenants_with_same_component_name_do_not_collide`, `test_service.py::SemanticsTest::test_tenant_quota_enforced` | Hosts are shared across tenants (no per-tenant host pools); isolation of running workloads is INV-60's job |
| T-06 | E | Insider / platform operator mutates tenant workloads | `Authorizer.check`: `tenant="*"` denied `desired:write`, `rollout:run`; every control op journaled with `by` | `test_security.py::AuthZTest::test_privilege_escalation_blocked` | `emergency_disable`/`quarantine_host` require a platform `Principal` (tenant `*`) holding `control:freeze`/`control:quarantine` (`service._operator`), but the Principal is constructed in-process, so anyone with process/host access can call them or edit `state_dir`. Two-person rule not implemented |
| T-07 | T | Supply chain: unsigned/tampered/untrusted/unapproved/revoked artifact | `security.py::ArtifactVerifier.verify` (digest, trusted `key_id`, Ed25519, `approved_versions`, `revoked_digests`); `op_set_desired` rejects `PK_DEPLOY_DESIRED/1` when `require_signed_artifacts`; `op_rollout` verifies new version | `test_security.py::ArtifactTest::test_unsigned_tampered_untrusted_unapproved_revoked`, `test_service.py::RolloutTest::test_rollout_requires_signed_new_version` | Content is re-hashed only when an `artifact_fetcher(digest) -> bytes` is injected into `DeploymentService` (`_verify_artifact`); without it only signed metadata is checked. Dependency pins (`pins.json`) unapproved; Wadm UNPINNED; no SBOM/provenance attestation checked |
| T-08 | D | Verifier/crypto unavailable → accept unsigned | `security.py::_require_crypto`; `op_set_desired`/`op_rollout` raise `INV63-E-DEPENDENCY-UNAVAILABLE` when `verifier is None`; preflight `A-07` | `test_security.py::ArtifactTest::test_verifier_unavailable_fails_closed` | Fail-closed = availability loss |
| T-09 | T | Hostile input (oversize, deep nesting, dup keys, NaN, injection strings) | `schema.py::parse_bytes` (64 KiB, depth 8, duplicate-key and constant rejection), strict `schema.validate` (fails closed on unknown keywords), `manager._validate_identifier` | `test_security.py::InjectionTest::test_injection_and_resource_exhaustion_inputs`, `test_contracts.py::FuzzTest::test_random_bytes_never_crash_and_always_structured`, `::test_structured_fuzz_of_desired_schema` | Fuzzing is randomized in-process, not coverage-guided; no long-running fuzz campaign |
| T-10 | D | Resource exhaustion (request floods) | `resilience.py::Admission` (`max_inflight`, batch shedding at 80%, per-tenant 50/s burst 100), `Deadline`, `observability.MAX_LABEL_SETS`, bounded `Tracer`/`Logger`/`DecisionLog` buffers, `Journal.max_bytes` | `test_platform.py::ResilienceTest::test_admission_shedding_and_quota`, `test_platform.py::PerformanceTest::test_memory_bounded_under_many_decisions` | Every successful non-explain request journals an `idempotency` record; at 256 MiB writes fail `INV63-E-QUOTA` until an operator runs `DeploymentService.compact()` (not automatic). `Admission.queue` serves async integrations only, not `handle` |
| T-11 | T/R | Journal tampering / history rewrite | `store.py::Journal._load` hash chain (`seq`, `prev`, SHA-256 `digest`) → `INV63-E-STATE-CORRUPT`; torn tail truncation only | `test_service.py::DurabilityTest::test_mid_journal_corruption_refuses_start`, `test_manager.py::ManagerTest::test_audit_chain_detects_record_tampering` | Chain is unkeyed: an attacker with write access can rewrite the whole file with a valid chain. External head anchoring not implemented (evidence key `external-anchor`). `compact()` replaces history with one `snapshot` record carrying `previous_head`/`previous_records`; earlier history survives only in backups |
| T-12 | I | State disclosure at rest | `security.py::Sealer` (AES-256-GCM, key ids, `rotate`, `reseal`) used by `Journal` when a `sealer` is injected | `test_security.py::SecretsAndCryptoTest::test_encryption_at_rest_and_rotation`, `::test_encrypted_journal_roundtrip` | Record envelope (`seq`, `epoch`, `ts`, `kind`) stays plaintext; `BACKUP.json` plaintext. No KMS (evidence `kms`). `DeploymentService.__init__` refuses to start (`INV63-E-CONFIG-INVALID`) when `require_encryption_at_rest` and the journal has no Sealer |
| T-13 | I | Secrets leak via config/logs/traces/errors | `config.py::_scan_secrets` (`INV63-E-SECRET-IN-CONFIG`), `security.py::redact` in `Logger.log` and `Tracer.span`, `resolve_secret_ref` | `test_platform.py::ConfigTest::test_secrets_rejected_in_config`, `test_security.py::SecretsAndCryptoTest::test_redaction`, `::test_secret_refs`, `test_platform.py::ObservabilityTest::test_structured_logs_have_stable_ids_and_no_secrets` | Pattern-based redaction misses unknown secret formats; only `env:`/`file:` refs are supported (a `vault:` backend is not implemented and is rejected at config validation) |
| T-14 | S/T | Compromised control plane (Wadm/lattice lies or is hijacked) | Credentials via `secret_refs.lattice_creds`; `_observe` filters instances to known hosts; rollout checks `adapter.healthy`; breaker/retry bounds | `test_platform.py::ResilienceTest::test_fault_injection_breaker_trips_and_recovers`, `test_service.py::RolloutTest::test_failed_rollout_rolls_back_automatically` | Lattice is trusted for truth of `list_instances`/`healthy` (A-17). A malicious lattice can cause flapping or fake convergence. No mutual attestation |
| T-15 | T | Stale/split-brain controller writes | `Journal.append` epoch fencing (`INV63-E-STALE-EPOCH`), concurrent-writer size check (`INV63-E-CONFLICT`); `status().ready` requires leadership | `test_service.py::DurabilityTest::test_stale_controller_is_fenced`, `test_platform.py::ConcurrencyTest::test_two_handles_cannot_interleave_journal_writes` | Fencing is file-based on one filesystem; a stale controller can still act on the lattice before its next journal append fails. Fencing is single-host; no consensus lease (waiver DEBT-001, evidence `consensus-lease`) |
| T-16 | E | Compromised workload escapes to control plane | INV-63 never executes workload code; workloads have no inbound path to the service other than B-01 with a token | none specific (out of scope: sandboxing is INV-60) | Relies on INV-60 / runtime isolation |
| T-17 | E | Ambient authority (C043) | `service.py::DeploymentService.__init__` holds only injected `adapter`, `journal`, `tokens`, `verifier`, `clock`/`mono`; no direct socket/FS use outside the injected `Journal` directory (and `preflight` probing `journal.dir`) | reviewed by inspection only | `adapter.py` reads `pins.json` at import; `security.resolve_secret_ref` can open any `file:` path given in config. No OS-level sandbox (seccomp/landlock) |
| T-18 | I | Side channels (timing, cache, co-tenancy) | Constant-time HMAC compare only | none | Not analysed: timing of authz/quota paths, shared-host microarchitectural channels, metric/decision-count leakage across tenants. Accepted as open residual |
| T-19 | R | Operator denies an action | `_control` journals `by: subject`; `_decide` records reason/inputs/policies/`trace_id`/release/topology digest | `test_platform.py::ObservabilityTest::test_every_decision_has_reason_and_explain_view` | `DecisionLog` is in memory only (lost on restart); journal is the durable record |
| T-20 | D | Rollout takes all replicas down / spread collapses | `op_rollout` batches by `max_unavailable` / `canary`; spread placement in `manager.diff` | `test_service.py::RolloutTest::test_canary_then_bounded_batches`, `test_manager.py::ManagerTest::test_deterministic_spread_and_idempotent_reconcile` | `op_rollout` stops before start inside a batch (not make-before-break; waiver DEBT-004). Starts go through the circuit breaker |

## Open items
- Security review sign-off (evidence `security-review`), node attestation (`node-attestation`), KMS (`kms`), external audit anchor (`external-anchor`): none done.
- Shared replay cache (DEBT-002); automatic compaction trigger; token-verified operator path; trace propagation to adapter (DEBT-003).
