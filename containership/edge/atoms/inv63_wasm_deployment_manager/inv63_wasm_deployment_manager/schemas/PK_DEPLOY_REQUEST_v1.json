{
  "$defs": {
    "digest": {
      "pattern": "sha256:[0-9a-f]{64}",
      "type": "string"
    },
    "id": {
      "maxLength": 256,
      "minLength": 1,
      "pattern": "[A-Za-z0-9][A-Za-z0-9._:@/+-]{0,255}",
      "type": "string"
    },
    "instance": {
      "items": {
        "$ref": "#/$defs/id"
      },
      "maxItems": 3,
      "minItems": 3,
      "type": "array"
    },
    "tenant": {
      "pattern": "[a-z0-9][a-z0-9-]{0,62}",
      "type": "string"
    }
  },
  "$id": "urn:inv63:PK_DEPLOY_REQUEST/1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "accept_versions": {
      "items": {
        "maximum": 99,
        "minimum": 1,
        "type": "integer"
      },
      "maxItems": 8,
      "type": "array"
    },
    "body": {
      "type": "object"
    },
    "deadline_ms": {
      "maximum": 300000,
      "minimum": 1,
      "type": "integer"
    },
    "idempotency_key": {
      "pattern": "[A-Za-z0-9-]{8,64}",
      "type": "string"
    },
    "op": {
      "enum": [
        "set_desired",
        "reconcile",
        "rollout",
        "freeze",
        "unfreeze",
        "quarantine",
        "release_quarantine",
        "explain",
        "rollback"
      ]
    },
    "schema": {
      "const": "PK_DEPLOY_REQUEST/1"
    },
    "token": {
      "maxLength": 2048,
      "type": "string"
    },
    "traceparent": {
      "pattern": "00-[0-9a-f]{32}-[0-9a-f]{16}-[0-9a-f]{2}",
      "type": "string"
    }
  },
  "required": [
    "schema",
    "op",
    "token",
    "idempotency_key",
    "body"
  ],
  "title": "PK_DEPLOY_REQUEST/1",
  "type": "object",
  "x-inv63": {
    "owner": "INV-63",
    "stability": "stable"
  }
}
