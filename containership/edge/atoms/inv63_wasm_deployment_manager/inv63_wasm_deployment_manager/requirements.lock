# INV-63 runtime dependency lock (INV-63-C031). Install: pip install --require-hashes -r requirements.lock
# Hash must be filled for the target platform wheel at approval time (pip hash / pip-compile --generate-hashes).
cryptography==46.0.7
# Wadm: external service, pinned in pins.json (UNPINNED until approved)
# pk_core: external framework, pinned in pins.json (UNPINNED until supplied)
