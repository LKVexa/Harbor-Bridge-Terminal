{
 "schema": "PK_TELEMETRY_POLICY/1",
 "retention": {
  "metrics_days": 30,
  "logs_days": 14,
  "audit_days": 400,
  "decision_ring": "1000 in memory"
 },
 "sampling": {
  "debug_info_logs": "profile telemetry.sample_rate (default 1.0)",
  "warn_error_logs": "never sampled",
  "audit": "never sampled",
  "traces": "parent sampled flag honoured"
 },
 "privacy": {
  "tenant_ids": "pseudonymised (HMAC, per-process key) in metrics and logs; explain view (accel.read) shows raw ids",
  "secrets": "redaction.redact on every log/audit/explain payload",
  "cardinality": "Metrics.max_series bound; overflow counted in dropped_series"
 },
 "export": {
  "metrics": "Prometheus text via Metrics.render",
  "logs": "JSON lines to a host sink",
  "audit": "AuditLog.export to durable storage + external head anchor",
  "failure": "sink failure counted, decision unaffected"
 }
}
