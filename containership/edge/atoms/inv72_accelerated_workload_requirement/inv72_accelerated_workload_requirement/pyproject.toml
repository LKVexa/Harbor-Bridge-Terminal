[project]
name = "inv72_accelerated_workload_requirement"
version = "4.3.0"
description = "INV-72 Accelerated workload requirement: strict, fail-closed accelerator matching and reservation"
requires-python = ">=3.10,<3.14"
dependencies = []
license = { text = "UNSPECIFIED - owner's PK series material; no licence file supplied (see THIRD-PARTY-NOTICES.md)" }

[tool.inv72]
runtime = "stdlib-only"
