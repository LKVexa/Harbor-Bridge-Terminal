# INV-10 API reference — 4.3.0

Generated by `tools/gen_docs.py`; do not edit by hand.

## Error codes

| code | class |
|---|---|
| `AMBIGUOUS_EXPORT` | `AmbiguousExport` |
| `BOUNDARY_VIOLATION` | `BoundaryViolation` |
| `COMPOSITION_CYCLE` | `CompositionCycle` |
| `COMPOSITION_ERROR` | `CompositionError` |
| `CONFLICT` | `Conflict` |
| `DEPENDENCY_INCOMPATIBLE` | `DependencyIncompatible` |
| `DEPENDENCY_UNAVAILABLE` | `DependencyUnavailable` |
| `EXTERNAL_UNAVAILABLE` | `ExternalUnavailable` |
| `FROZEN` | `Frozen` |
| `INTEGRITY_ERROR` | `IntegrityError` |
| `INTERFACE_INCOMPATIBLE` | `InterfaceIncompatible` |
| `INVALID_COMPOSITION` | `InvalidComposition` |
| `NOT_FOUND` | `NotFound` |
| `OVERLOADED` | `Overloaded` |
| `POLICY_REJECTED` | `PolicyRejected` |
| `PROVENANCE_REJECTED` | `ProvenanceRejected` |
| `QUARANTINED` | `Quarantined` |
| `RESOURCE_LIMIT_EXCEEDED` | `ResourceLimitExceeded` |
| `SCHEMA_VIOLATION` | `SchemaViolation` |
| `UNAUTHENTICATED` | `Unauthenticated` |
| `UNSATISFIED_IMPORT` | `UnsatisfiedImport` |
| `VALIDATION_REQUIRED` | `ValidationRequired` |
| `WIT_PARSE_ERROR` | `WitParseError` |

## Default limits

```json
{
 "max_components": 4096,
 "max_interfaces_per_component": 4096,
 "max_external_imports": 16384,
 "max_total_interface_references": 262144,
 "max_identifier_length": 512
}
```

## `inv10_component_composition_system.composition`

Deterministic, fail-closed component composition primitives for INV-10.

This module deliberately has no dependency on :mod:`pk_core` so that the
linker itself can be unit-tested and embedded independently of the checklist
and evidence framework.

### class `AmbiguousExport(message: 'str', **details: 'Any') -> 'None'`

Raised when exports or component identities are ambiguous.


### class `CompositionCycle(message: 'str', **details: 'Any') -> 'None'`

Raised when components depend on each other in a cycle.


### class `CompositionError(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.

- `as_dict(self) -> 'dict[str, Any]'` — 

### class `CompositionLimits(max_components: 'int' = 4096, max_interfaces_per_component: 'int' = 4096, max_external_imports: 'int' = 16384, max_total_interface_references: 'int' = 262144, max_identifier_length: 'int' = 512) -> None`

Fail-closed resource ceilings for one composition operation.


### class `InvalidComposition(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


### class `ResourceLimitExceeded(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


### class `Unit(name: 'str', imports: 'frozenset[str]', exports: 'frozenset[str]') -> None`

A unit of composition: its stable name, imports, and exports.


### class `UnsatisfiedImport(message: 'str', **details: 'Any') -> 'None'`

Raised when a composition leaves an internal import with no export.


### `compose(components: 'Iterable[Unit]', *, external: 'Iterable[str]' = frozenset(), limits: 'CompositionLimits' = CompositionLimits(max_components=4096, max_interfaces_per_component=4096, max_external_imports=16384, max_total_interface_references=262144, max_identifier_length=512)) -> 'dict[str, Any]'`

Link components into a deterministic, internally closed composition.

Imports must bind either to exactly one export in this composition or to an
explicitly declared external interface. Component dependency cycles,
including self-cycles, are rejected. The returned composition identifier is
the SHA-256 digest of a canonical manifest containing every unit's imports,
exports, and resolved bindings.

## `inv10_component_composition_system.schemas`

MC-03 / MC-04: machine-readable PK_COMPONENT/1 and PK_COMPOSITION/1 contracts.

The JSON Schema documents in ``schemas/`` are the portable artifacts for other
implementations. This module ships an equivalent dependency-free validator so
the package can enforce them without a third-party JSON Schema library.
Unknown-field policy: PK_COMPONENT/1 rejects unknown fields (closed input);
PK_COMPOSITION/1 permits additive fields (consumers must ignore unknowns).

### `component_document(unit: 'Unit', metadata: 'dict[str, Any] | None' = None) -> 'dict[str, Any]'`



### `load_schema(name: 'str') -> 'dict[str, Any]'`



### `validate_component(doc: 'Any') -> 'Unit'`

Validate a PK_COMPONENT/1 document and return the Unit it declares.

### `validate_composition(doc: 'Any') -> 'dict[str, Any]'`

Validate a PK_COMPOSITION/1 result, tolerating additive fields.

## `inv10_component_composition_system.manifest`

MC-15: declarative PK_COMPOSITION_MANIFEST/1 loader/validator.

Bounded (size, depth), strict (unknown fields rejected), duplicate-key-safe
JSON ingestion with deterministic, path-addressed errors.

### class `CompositionManifest(units: 'list[Unit]', external: 'frozenset[str]', context: 'CompositionContext | None' = None, aliases: 'dict[str, dict[str, str]]' = <factory>, provenance: 'dict[str, Any]' = <factory>, metadata: 'dict[str, dict[str, Any]]' = <factory>) -> None`

CompositionManifest(units: 'list[Unit]', external: 'frozenset[str]', context: 'CompositionContext | None' = None, aliases: 'dict[str, dict[str, str]]' = <factory>, provenance: 'dict[str, Any]' = <factory>, metadata: 'dict[str, dict[str, Any]]' = <factory>)


### `from_document(doc: 'Any') -> 'CompositionManifest'`



### `load(path: 'str | pathlib.Path') -> 'CompositionManifest'`



### `loads(text: 'str | bytes') -> 'CompositionManifest'`



## `inv10_component_composition_system.wit`

MC-06: WebAssembly Component Model / WIT world integration.

A bounded, dependency-free parser for the WIT subset INV-10 needs to link:
``package ns:pkg@ver;``, ``interface name { ... }`` (functions / resources /
types are recorded, bodies are brace-balanced), and ``world name { import x;
export y; }``. Each world becomes a :class:`Unit` whose imports/exports are
fully qualified ``ns:pkg/iface@ver`` identifiers. It does not validate core
Wasm binaries or canonical-ABI lowering — that stays with INV-09 — and a
world referencing an interface that is neither local nor fully qualified is
refused rather than guessed.

### class `WitInterface(name: 'str', functions: 'list[str]' = <factory>, resources: 'list[str]' = <factory>, types: 'list[str]' = <factory>) -> None`

WitInterface(name: 'str', functions: 'list[str]' = <factory>, resources: 'list[str]' = <factory>, types: 'list[str]' = <factory>)


### class `WitPackage(namespace: 'str', package: 'str', version: 'str | None', interfaces: 'dict[str, WitInterface]' = <factory>, worlds: 'dict[str, WitWorld]' = <factory>) -> None`

WitPackage(namespace: 'str', package: 'str', version: 'str | None', interfaces: 'dict[str, WitInterface]' = <factory>, worlds: 'dict[str, WitWorld]' = <factory>)

- `qualify(self, ref: 'str') -> 'str'` — 
- `to_unit(self, world: 'str', *, component_name: 'str | None' = None) -> 'Unit'` — 

### class `WitWorld(name: 'str', imports: 'list[str]' = <factory>, exports: 'list[str]' = <factory>) -> None`

WitWorld(name: 'str', imports: 'list[str]' = <factory>, exports: 'list[str]' = <factory>)


### `parse(text: 'str') -> 'WitPackage'`



## `inv10_component_composition_system.adapters`

MC-07..MC-10: adjacent-layer integration ports.

INV-10 does not own module validation (INV-09), interface typing (INV-11),
language interop (INV-12) or realization (PLN-02). These are the *ports* it
consumes/produces, with fail-closed reference implementations that the real
adjacent services plug into. Each port is a small Protocol so the production
service can be substituted without touching the linker.

### class `InterfaceResolver(*args, **kwargs)`

Base class for protocol classes.

Protocol classes are defined as::

    class Proto(Protocol):
        def meth(self) -> int:
            ...

Such classes are primarily used with static type checkers that recognize
structural subtyping (static duck-typing).

For example::

    class C:
        def meth(self) -> int:
            return 0

    def func(x: Proto) -> int:
        return x.meth()

    func(C())  # Passes static type check

See PEP 544 for details. Protocol classes decorated with
@typing.runtime_checkable act as simple-minded runtime protocols that check
only the presence of given attributes, ignoring their type signatures.
Protocol classes can be generic, they are defined as::

    class GenProto(Protocol[T]):
        def meth(self) -> T:
            ...

- `compatible(self, required: 'str', provided: 'str') -> 'bool'` — 

### class `LanguageBinding(unit: 'str', language: 'str', abi: 'str' = 'component-model/canonical-abi', string_encoding: 'str' = 'utf8') -> None`

LanguageBinding(unit: 'str', language: 'str', abi: 'str' = 'component-model/canonical-abi', string_encoding: 'str' = 'utf8')


### class `ModuleValidator(*args, **kwargs)`

Base class for protocol classes.

Protocol classes are defined as::

    class Proto(Protocol):
        def meth(self) -> int:
            ...

Such classes are primarily used with static type checkers that recognize
structural subtyping (static duck-typing).

For example::

    class C:
        def meth(self) -> int:
            return 0

    def func(x: Proto) -> int:
        return x.meth()

    func(C())  # Passes static type check

See PEP 544 for details. Protocol classes decorated with
@typing.runtime_checkable act as simple-minded runtime protocols that check
only the presence of given attributes, ignoring their type signatures.
Protocol classes can be generic, they are defined as::

    class GenProto(Protocol[T]):
        def meth(self) -> T:
            ...

- `validate(self, unit: 'str', module: 'bytes') -> 'ValidatedModule'` — 

### class `RealizationPlan(composition: 'str', steps: 'list[dict[str, Any]]' = <factory>, externals: 'list[str]' = <factory>) -> None`

RealizationPlan(composition: 'str', steps: 'list[dict[str, Any]]' = <factory>, externals: 'list[str]' = <factory>)


### class `ReferenceModuleValidator(profile: 'str' = 'wasm-component-0.2') -> 'None'`

Stand-in INV-09 validator: checks the Wasm magic/version preamble only.

- `validate(self, unit: 'str', module: 'bytes') -> 'ValidatedModule'` — 

### class `SemverInterfaceResolver()`

Reference INV-11 resolver: same base, same major, provided >= required.

Pre-release versions only match exactly. Unversioned references only match
unversioned references with the same base (the 4.2.0 exact-string rule).
For 0.x, the minor is treated as the compatibility boundary.

- `compatible(self, required: 'str', provided: 'str') -> 'bool'` — 

### class `ValidatedModule(unit: 'str', module_digest: 'str', validator: 'str', profile: 'str') -> None`

Attestation that INV-09 validated the module backing a unit.


### `check_language_interop(bindings: 'list[dict[str, Any]]', languages: 'Mapping[str, LanguageBinding]', matrix: 'Mapping[str, set[str]]' = {'component-model/canonical-abi': {'python', 'go', 'cpp', 'zig', 'c', 'js', 'csharp', 'rust'}}) -> 'list[dict[str, str]]'`

Refuse a binding whose consumer/provider cannot share ABI semantics.

### `parse_interface(ref: 'str') -> 'tuple[str, tuple[int, int, int] | None, str | None]'`



### `realization_plan(result: 'Mapping[str, Any]') -> 'RealizationPlan'`

PLN-02 port: an instantiation plan that honours order, bindings, externals, id.

### `require_validated(units: 'list[Unit]', attestations: 'Mapping[str, ValidatedModule]') -> 'None'`

Refuse composition unless every unit carries an INV-09 attestation.

### `resolve_interfaces(units: 'list[Unit]', resolver: 'InterfaceResolver') -> 'tuple[list[Unit], dict[str, dict[str, str]]]'`

Rewrite each import to the single compatible export that satisfies it.

Returns rewritten units and an alias map ``{consumer: {required: provided}}``
so the rewrite is explicit in the audit trail. Ambiguity is refused.

### `verify_realization(result: 'Mapping[str, Any]', instantiated: 'list[str]', wired: 'Mapping[str, Mapping[str, str]]') -> 'None'`

Check that a runtime actually honoured the composition (conformance oracle).

## `inv10_component_composition_system.governance`

MC-11..MC-14: context boundaries, link authorization, provenance, externals.

* :class:`CompositionContext` carries tenant/workload/environment/site identity.
* :class:`LinkPolicy` decides whether consumer -> provider bindings are allowed.
* :class:`ProvenanceVerifier` checks artifact digest, signature and approved version.
* :class:`EnvironmentResolver` proves each declared external exists in the
  target environment with a compatible version and trust level.
All are fail-closed: absence of a rule is denial.

### class `ArtifactRecord(unit: 'str', version: 'str', digest: 'str', signature: 'str', key_id: 'str') -> None`

ArtifactRecord(unit: 'str', version: 'str', digest: 'str', signature: 'str', key_id: 'str')


### class `CompositionContext(tenant: 'str', workload: 'str', environment: 'str', site: 'str' = 'any') -> None`

CompositionContext(tenant: 'str', workload: 'str', environment: 'str', site: 'str' = 'any')

- `as_dict(self) -> 'dict[str, str]'` — 

### class `EnvironmentOffer(interface: 'str', trust: 'str' = 'platform', capabilities: 'tuple[str, ...]' = ()) -> None`

EnvironmentOffer(interface: 'str', trust: 'str' = 'platform', capabilities: 'tuple[str, ...]' = ())


### class `EnvironmentResolver(offers: 'Mapping[str, Iterable[EnvironmentOffer]]', interface_resolver: 'Any' = None) -> 'None'`

Proves declared externals are actually supplied by an environment.

- `resolve(self, ctx: 'CompositionContext', externals: 'Iterable[str]', *, min_trust: 'str' = 'vendor') -> 'dict[str, str]'` — 

### class `LinkPolicy(rules: 'list[LinkRule]' = <factory>, version: 'str' = '0') -> None`

Ordered capability policy; deny rules win; default deny.

- `decide(self, ctx: 'CompositionContext', binding: 'Mapping[str, Any]') -> 'dict[str, Any]'` — 
- `enforce(self, ctx: 'CompositionContext', bindings: 'Iterable[Mapping[str, Any]]') -> 'list[dict[str, Any]]'` — 

### class `LinkRule(consumer: 'str', interface: 'str', provider: 'str' = '*', tenants: 'tuple[str, ...]' = ('*',), effect: 'str' = 'allow') -> None`

LinkRule(consumer: 'str', interface: 'str', provider: 'str' = '*', tenants: 'tuple[str, ...]' = ('*',), effect: 'str' = 'allow')


### class `ProvenanceVerifier(keys: 'Any', approved: 'Mapping[str, set[str]]') -> 'None'`

Verifies digest, signature (via a key provider) and approved versions.

Signatures use HMAC-SHA256 because the package is stdlib-only; production
deployments should plug an asymmetric verifier (e.g. Sigstore/ed25519) with
the same ``verify`` signature.

- `message(unit: 'str', version: 'str', digest: 'str') -> 'bytes'` — 
- `verify(self, record: 'ArtifactRecord', artifact: 'bytes | None' = None) -> 'None'` — 
- `verify_all(self, units: 'Iterable[Unit]', records: 'Mapping[str, ArtifactRecord]') -> 'None'` — 

### `enforce_tenant_boundary(ctx: 'CompositionContext', unit_tenants: 'Mapping[str, str]') -> 'None'`

Compositions do not link across tenants (contract boundary).

## `inv10_component_composition_system.security`

MC-21..MC-23: key management, actor authentication, tamper-evident audit.

* :class:`KeyProvider` — versioned keys with rotation, retirement and a
  fail-closed ``unavailable`` state (degraded mode refuses signing/verifying).
* :class:`Authenticator` — HMAC bearer tokens binding actor, role, expiry.
* :class:`AuditTrail` — append-only JSONL hash chain; ``verify()`` detects any
  edit, deletion or reorder. Secrets are never written to the trail.

### class `AuditTrail(path: 'str | os.PathLike | None' = None, clock: 'Callable[[], float]' = <built-in function time>) -> 'None'`

Append-only, hash-chained JSONL audit log (optionally file-backed).

- `append(self, event: 'str', actor: 'str', **fields: 'Any') -> 'dict[str, Any]'` — 
- `head(self) -> 'str'` — 
- `verify(self) -> 'int'` — 

### class `Authenticator(keys: 'KeyProvider', clock: 'Callable[[], float]' = <built-in function time>) -> 'None'`



- `authenticate(self, token: 'str') -> 'Principal'` — 
- `authorize(principal: 'Principal', action: 'str') -> 'None'` — 
- `issue(self, actor: 'str', role: 'str', ttl: 'float' = 900.0) -> 'str'` — 

### class `KeyProvider(clock: 'Callable[[], float]' = <built-in function time>) -> 'None'`

In-process key ring. Production: back with a KMS/HSM via the same API.

- `add(self, key_id: 'str', material: 'bytes', *, activate: 'bool' = False) -> 'None'` — 
- `generate(self, key_id: 'str | None' = None, *, activate: 'bool' = True) -> 'str'` — 
- `revoke(self, key_id: 'str') -> 'None'` — 
- `rotate(self) -> 'str'` — 
- `sign(self, message: 'bytes') -> 'tuple[str, str]'` — 
- `signing_key(self) -> 'tuple[str, bytes]'` — 
- `status(self) -> 'dict[str, Any]'` — 
- `verification_key(self, key_id: 'str') -> 'bytes'` — 

### class `Principal(actor: 'str', role: 'str') -> None`

Principal(actor: 'str', role: 'str')


### `redact(obj: 'Any') -> 'Any'`



## `inv10_component_composition_system.observability`

MC-24..MC-27: metrics, structured logs + trace context, health, explain.

* :class:`Metrics` implements the contract's named signals (``compositions``
  counter by outcome, ``unsatisfied_imports`` by interface, ``cycles_detected``,
  ``external_imports`` gauge) plus latency histogram, and renders Prometheus
  text exposition.
* :class:`StructuredLogger` emits JSON lines with operation id, W3C
  ``traceparent`` propagation, tenant/workload correlation, sampling and
  redaction.
* :func:`health_report` and :func:`explain` are the operator surfaces.

### class `Metrics() -> 'None'`



- `inc(self, name: 'str', value: 'float' = 1.0, **labels: 'str') -> 'None'` — 
- `observe_ms(self, name: 'str', ms: 'float') -> 'None'` — 
- `record_outcome(self, outcome: 'str', *, error: 'Mapping[str, Any] | None' = None, result: 'Mapping[str, Any] | None' = None, latency_ms: 'float | None' = None) -> 'None'` — 
- `render_prometheus(self) -> 'str'` — 
- `set(self, name: 'str', value: 'float', **labels: 'str') -> 'None'` — 

### class `StructuredLogger(stream: 'TextIO | None' = None, *, sample_rate: 'float' = 1.0, clock: 'Callable[[], float]' = <built-in function time>, rng: 'random.Random | None' = None) -> 'None'`



- `log(self, level: 'str', event: 'str', *, operation_id: 'str', traceparent: 'str | None' = None, **fields: 'Any') -> 'dict[str, Any] | None'` — 

### `child_traceparent(parent: 'str | None') -> 'str'`



### `explain(units: 'list[Any]', *, external: 'frozenset[str]' = frozenset(), result: 'Mapping[str, Any] | None' = None, error: 'Mapping[str, Any] | None' = None, decisions: 'list[Mapping[str, Any]] | None' = None) -> 'dict[str, Any]'`

Operator report: dependency paths, candidates, cycle path, decisions.

### `health_report(*, version: 'str', limits: 'Any', dependencies: 'Mapping[str, Callable[[], bool]]', frozen: 'bool' = False, active: 'str | None' = None) -> 'dict[str, Any]'`



### `render_explain_text(report: 'Mapping[str, Any]') -> 'str'`



## `inv10_component_composition_system.controlplane`

MC-16..MC-18, MC-28..MC-31: the composition control plane.

* :class:`Registry` — namespace-governed component registry (MC-16).
* :class:`CompositionStore` — durable content-addressed store with
  immutability, verified reads, GC (MC-17). Plain files, atomic rename; no
  SQLite (safe on synced/network folders).
* :class:`ActivationController` — atomic promote/rollback with persisted
  previous-good lineage and compare-and-swap generations (MC-18, MC-40).
* :class:`ConfigLedger` — versioned limits/policy config with author, source,
  activation time and rollback lineage (MC-28).
* :class:`AdmissionController` — concurrency, per-tenant fairness, bounded
  queue, deadlines, load shedding (MC-29).
* :class:`CompositionService` — authenticated facade with idempotency keys,
  replay protection, restart recovery (MC-30) and quarantine/freeze (MC-31),
  audit, metrics, logs.

### class `ActivationController(store: 'CompositionStore', workload: 'str', history: 'int' = 16) -> 'None'`

Pointer file ``active.json`` = {generation, active, previous[]}; CAS updates.

- `activate(self, cid: 'str', *, expected_generation: 'int | None' = None, verify: 'Callable[[dict[str, Any]], None] | None' = None) -> 'dict[str, Any]'` — 
- `rollback(self, *, expected_generation: 'int | None' = None) -> 'dict[str, Any]'` — 
- `state(self) -> 'dict[str, Any]'` — 

### class `AdmissionController(*, max_concurrent: 'int' = 8, max_per_tenant: 'int' = 4, max_queue: 'int' = 64, queue_timeout: 'float' = 1.0) -> 'None'`



- `admit(self, tenant: 'str', *, deadline: 'float | None' = None) -> 'Iterator[None]'` — 

### class `CompositionService(deps: 'ServiceDeps') -> 'None'`

Stateful wrapper around the pure linker. Restart-safe: all state that
matters (store, activation pointer, idempotency journal, audit) is on disk.

- `health(self) -> 'dict[str, Any]'` — 
- `publish(self, token: 'str', units: 'list[Unit]', *, context: 'Any', external: 'Iterable[str]' = frozenset(), idempotency_key: 'str | None' = None, nonce: 'str | None' = None, traceparent: 'str | None' = None) -> 'dict[str, Any]'` — 
- `quarantine_component(self, token: 'str', name: 'str', reason: 'str') -> 'None'` — 
- `quarantine_namespace(self, token: 'str', pattern: 'str', reason: 'str') -> 'None'` — 
- `set_frozen(self, token: 'str', frozen: 'bool', reason: 'str') -> 'None'` — 

### class `CompositionStore(root: 'str | os.PathLike') -> 'None'`

Content-addressed, immutable, verified-on-read file store.

- `fsck(self) -> 'dict[str, list[str]]'` — 
- `gc(self, retain: 'Iterable[str]') -> 'list[str]'` — 
- `get(self, cid: 'str') -> 'dict[str, Any]'` — 
- `ids(self) -> 'list[str]'` — 
- `put(self, result: 'Mapping[str, Any]') -> 'str'` — 

### class `ConfigLedger(path: 'str | os.PathLike | None' = None) -> 'None'`

Append-only config versions; activation and rollback are recorded entries.

- `activate(self, config_id: 'str', *, actor: 'str') -> 'dict[str, Any]'` — 
- `active_id(self) -> 'str | None'` — 
- `active_limits(self) -> 'CompositionLimits'` — 
- `propose(self, limits: 'CompositionLimits', *, author: 'str', source: 'str', policy_version: 'str' = '0') -> 'dict[str, Any]'` — 
- `rollback(self, *, actor: 'str') -> 'dict[str, Any]'` — 

### class `QuarantineList() -> 'None'`



- `check(self, units: 'Iterable[Unit]') -> 'None'` — 

### class `Registry() -> 'None'`

Authoritative registry: owners claim namespaces; versions are immutable.

- `claim(self, pattern: 'str', owner: 'str') -> 'None'` — 
- `providers_of(self, interface: 'str') -> 'list[dict[str, Any]]'` — 
- `register(self, unit: 'Unit', version: 'str', owner: 'str', **meta: 'Any') -> 'dict[str, Any]'` — 
- `resolve(self, name: 'str', version: 'str') -> 'Unit'` — 
- `set_state(self, name: 'str', version: 'str', state: 'str') -> 'None'` — 

### class `ServiceDeps(store: 'CompositionStore', auth: 'Any', audit: 'Any', metrics: 'Any', logger: 'Any', policy: 'Any | None' = None, environment: 'Any | None' = None, config: 'ConfigLedger | None' = None, admission: 'AdmissionController | None' = None) -> None`

ServiceDeps(store: 'CompositionStore', auth: 'Any', audit: 'Any', metrics: 'Any', logger: 'Any', policy: 'Any | None' = None, environment: 'Any | None' = None, config: 'ConfigLedger | None' = None, admission: 'AdmissionController | None' = None)


## `inv10_component_composition_system.features`

MC-46..MC-50 (optional contract capabilities) and MC-33 (legacy migration).

* :func:`compose_extended` — aliasing (MC-47) and dead-export elimination
  (MC-48) as explicit, recorded pre-link rewrites on top of :func:`compose`.
* :class:`IncrementalLinker` — cached, change-scoped relinking (MC-46).
* :func:`as_unit` / :func:`compose_nested` — a composition is itself a unit (MC-49).
* :func:`diff` — provider/external/order/identity impact analysis (MC-50).
* :func:`migrate_legacy` — 4.1.0 -> PK_COMPOSITION_ID/2 remap tool (MC-33).

Identity rule: every rewrite is applied to the *units* before linking, so the
``composition`` id always addresses the graph actually linked; the rewrite
itself is reported in additive fields (``aliases``, ``eliminated_exports``,
``nested``) and bound into ``extension_digest``.

### class `IncrementalLinker(*, capacity: 'int' = 256, limits: 'CompositionLimits' = CompositionLimits(max_components=4096, max_interfaces_per_component=4096, max_external_imports=16384, max_total_interface_references=262144, max_identifier_length=512)) -> 'None'`

Relinks only when the unit set changes; reports the affected subgraph.

The full link is still executed on change (correctness first: closure and
cycles are global properties), but unchanged requests are served from an
LRU keyed by the canonical request, and ``affected`` lists every component
whose transitive providers changed — the set a build system must rebuild.

- `apply(self, *, upsert: 'Iterable[Unit]' = (), remove: 'Iterable[str]' = (), external: 'Iterable[str] | None' = None) -> 'dict[str, Any]'` — 
- `link(self, units: 'Iterable[Unit]', external: 'Iterable[str]' = frozenset()) -> 'dict[str, Any]'` — 

### `apply_aliases(units: 'Iterable[Unit]', aliases: 'Mapping[str, Mapping[str, str]]') -> 'list[Unit]'`

Rename consumer imports: ``{consumer: {declared_import: target_interface}}``.

### `as_unit(result: 'Mapping[str, Any]', name: 'str', *, exports: 'Iterable[str] | None' = None) -> 'Unit'`

Present a closed composition as a component: its imports are its externals.

### `compose_extended(units: 'Iterable[Unit]', *, external: 'Iterable[str]' = frozenset(), aliases: 'Mapping[str, Mapping[str, str]] | None' = None, eliminate_dead: 'bool' = False, keep_exports: 'Iterable[str]' = (), limits: 'CompositionLimits' = CompositionLimits(max_components=4096, max_interfaces_per_component=4096, max_external_imports=16384, max_total_interface_references=262144, max_identifier_length=512)) -> 'dict[str, Any]'`



### `compose_nested(units: 'Iterable[Unit]', nested: 'Mapping[str, tuple[Mapping[str, Any], Iterable[str] | None]]', *, external: 'Iterable[str]' = frozenset(), limits: 'CompositionLimits' = CompositionLimits(max_components=4096, max_interfaces_per_component=4096, max_external_imports=16384, max_total_interface_references=262144, max_identifier_length=512)) -> 'dict[str, Any]'`

Compose units plus sub-compositions; bind inner ids into ``tree_digest``.

### `diff(active: 'Mapping[str, Any]', candidate: 'Mapping[str, Any]') -> 'dict[str, Any]'`



### `eliminate_dead_exports(units: 'Iterable[Unit]', keep: 'Iterable[str]' = ()) -> 'tuple[list[Unit], list[dict[str, str]]]'`



### `legacy_id_41(result: 'Mapping[str, Any]') -> 'str'`

Reconstruction of the 4.1.0 identity from its documented inputs.

The 4.1.0 source is not in the archive; AUDIT_REPORT A-02/A-03 describe the
inputs (names, order, external imports, export names; sha256 truncated to 24
hex). Treat matches as corroboration only — never as proof.

### `migrate_legacy(records: 'Iterable[Mapping[str, Any]]') -> 'dict[str, Any]'`

Recompute PK_COMPOSITION_ID/2 for persisted 4.1.0 records.

Each record needs ``legacy_id`` and the ``units`` (``[{name, imports,
exports}]``) and ``external`` it was built from. Records without units are
reported ``unrecoverable`` — a truncated legacy id cannot be inverted.
Several legacy ids mapping to one new id, or vice versa, are reported as
``collisions`` (the A-02 defect made this possible).

## `inv10_component_composition_system.errors`

Stable machine-readable error categories for INV-10 service layers (MC-01..MC-50).

All service-layer errors derive from :class:`CompositionError` so callers
handle one hierarchy. Codes are part of the public contract; never rename.

### class `BoundaryViolation(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


### class `Conflict(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


### class `DependencyIncompatible(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


### class `DependencyUnavailable(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


### class `ExternalUnavailable(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


### class `Frozen(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


### class `IntegrityError(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


### class `InterfaceIncompatible(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


### class `NotFound(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


### class `Overloaded(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


### class `PolicyRejected(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


### class `ProvenanceRejected(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


### class `Quarantined(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


### class `SchemaViolation(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


### class `Unauthenticated(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


### class `ValidationRequired(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


### class `WitParseError(message: 'str', **details: 'Any') -> 'None'`

Base class for machine-readable, fail-closed composition failures.


## `inv10_component_composition_system.pkcore_compat`

MC-01: pinned, fail-closed ``pk_core`` dependency gate.

The checklist adapter needs ``pk_core``. This module declares the supported
range, the exact API surface INV-10 consumes, and refuses to bind to a
partial/unexpected implementation (e.g. a stray module on ``PYTHONPATH``).

### `check(*, require_distribution: 'bool' = True) -> 'dict[str, Any]'`

Verify pk_core is installed, in range, and exposes the required surface.

## `inv10_component_composition_system.cli`

Operator CLI: ``python -m inv10_component_composition_system.cli <cmd>``.

Commands: compose MANIFEST | explain MANIFEST | diff A.json B.json |
migrate RECORDS.json | wit FILE WORLD | schema-check RESULT.json | version
Exit codes: 0 ok, 1 refused (machine-readable error on stdout), 2 usage.

### `main(argv: 'list[str] | None' = None) -> 'int'`



