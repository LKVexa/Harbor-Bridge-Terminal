# Reproducible install lock for PLN-07 4.3.0 (MC-21).
# Runtime core is stdlib-only. Optional extras are pinned exactly.
# Install: pip install --require-hashes -r lock/requirements.lock   (after hashes are filled
# by `pip-compile --generate-hashes` on the release builder; see docs/runbooks/day0-install.md)
cryptography==46.0.7
jsonschema==4.23.0
# pk_core==<UNPINNED - MC-02 external blocker>
