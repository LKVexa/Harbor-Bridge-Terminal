package fixture:egress@0.1.0;
interface handler { handle: func(x: u32) -> u32; }
interface outgoing { send: func(cap: string, x: u32) -> u32; }
world service-with-egress { import outgoing; export handler; }
