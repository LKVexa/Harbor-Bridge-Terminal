# Dapr manifests (candidate)

Not applied to any cluster by this build. Target Dapr minor: see `docs/COMPATIBILITY.md` (1.17.x candidate, approval pending). `test_governance.py` checks that these files contain no inline secrets and that timeouts/retries match the library defaults.
