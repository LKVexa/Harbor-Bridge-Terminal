# Resolved runtime lock for INV-61 4.3.0 (python 3.11, linux x86_64).
# Versions are pinned. Artifact hashes are NOT yet recorded: the package index was
# unreachable from the build container (pip: "from versions: none"), so the
# --require-hashes form is a release-CI step (docs/RELEASE.md step 3). Do not
# invent hashes.
cryptography==46.0.7
cffi  # transitive of cryptography; version as resolved by the release CI job
