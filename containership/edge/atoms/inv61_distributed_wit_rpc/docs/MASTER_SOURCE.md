# Historical master source — `MASTER.md` (M02)

**Status: BLOCKED — not recoverable from the supplied material.**

The v4.2.0 README cited `MASTER.md` as the 100-item master prompt/workflow source for
INV-61. It is not in the archive. `CHECKLIST.json` (4 top-level keys, 100 requirements,
digest in `SHA256SUMS`) is the only surviving source-of-truth artifact and is treated as
authoritative for this release.

Nothing was reconstructed or paraphrased into a substitute `MASTER.md`: a fabricated
source would carry false provenance. To close M02 the owner either supplies the original
(its digest is then recorded here and in `SHA256SUMS`) or signs a supersession record
declaring `CHECKLIST.json` + `docs/REQUIREMENTS.md` the replacement.
