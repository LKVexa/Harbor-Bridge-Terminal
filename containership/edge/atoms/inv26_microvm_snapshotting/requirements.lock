# INV-26 runtime lock. Versions are exact pins of what the 6.0.0 evidence ran against.
# Wheel hashes are NOT recorded: the build environment had no package index (pip download failed),
# so `--require-hashes` cannot be used yet. Regenerate with hashes on a networked builder:
#   pip-compile --generate-hashes  (or)  pip download ... && pip hash <wheel>
# Tracked as a gap under C031/X004 in COMPONENTS_STATUS.json.
cryptography==46.0.7
cffi==2.0.0
