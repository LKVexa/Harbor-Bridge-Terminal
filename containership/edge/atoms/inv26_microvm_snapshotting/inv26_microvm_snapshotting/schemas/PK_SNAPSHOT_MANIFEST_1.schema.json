{
 "$id": "urn:pk:inv26:PK_SNAPSHOT_MANIFEST/1",
 "$schema": "https://json-schema.org/draft/2020-12/schema",
 "additionalProperties": false,
 "properties": {
  "capture_schema": {
   "const": "PK_SNAPSHOT/2"
  },
  "created_at": {
   "minimum": 0,
   "type": "integer"
  },
  "envelope": {
   "additionalProperties": false,
   "properties": {
    "aad_schema": {
     "const": "PK_SNAPSHOT_AAD/1"
    },
    "alg": {
     "const": "AES-256-GCM-CHUNKED/1"
    },
    "chunk_size": {
     "maximum": 67108864,
     "minimum": 4096,
     "type": "integer"
    },
    "chunks": {
     "maximum": 16777216,
     "minimum": 1,
     "type": "integer"
    },
    "ciphertext_sha256": {
     "pattern": "^[0-9a-f]{64}$",
     "type": "string"
    },
    "key_id": {
     "maxLength": 63,
     "pattern": "^[a-z0-9][a-z0-9._-]{0,62}$",
     "type": "string"
    },
    "key_version": {
     "minimum": 1,
     "type": "integer"
    },
    "plaintext_bytes": {
     "minimum": 0,
     "type": "integer"
    },
    "wrapped_dek": {
     "maxLength": 512,
     "type": "string"
    }
   },
   "required": [
    "alg",
    "key_id",
    "key_version",
    "wrapped_dek",
    "chunk_size",
    "chunks",
    "ciphertext_sha256",
    "plaintext_bytes",
    "aad_schema"
   ],
   "type": "object"
  },
  "environment": {
   "maxLength": 63,
   "pattern": "^[a-z0-9][a-z0-9._-]{0,62}$",
   "type": "string"
  },
  "fingerprint": {
   "pattern": "^[0-9a-f]{64}$",
   "type": "string"
  },
  "generation": {
   "minimum": 1,
   "type": "integer"
  },
  "memory_mib": {
   "maximum": 1048576,
   "minimum": 1,
   "type": "integer"
  },
  "runtime": {
   "additionalProperties": false,
   "properties": {
    "adapter": {
     "enum": [
      "firecracker",
      "cloud-hypervisor",
      "reference"
     ]
    },
    "arch": {
     "enum": [
      "x86_64",
      "aarch64"
     ]
    },
    "version": {
     "maxLength": 32,
     "type": "string"
    }
   },
   "required": [
    "adapter",
    "version",
    "arch"
   ],
   "type": "object"
  },
  "schema": {
   "const": "PK_SNAPSHOT_MANIFEST/1"
  },
  "site": {
   "maxLength": 63,
   "pattern": "^[a-z0-9][a-z0-9._-]{0,62}$",
   "type": "string"
  },
  "snapshot_id": {
   "maxLength": 63,
   "pattern": "^[a-z0-9][a-z0-9._-]{0,62}$",
   "type": "string"
  },
  "tenant": {
   "maxLength": 63,
   "pattern": "^[a-z0-9][a-z0-9._-]{0,62}$",
   "type": "string"
  },
  "workload": {
   "maxLength": 63,
   "pattern": "^[a-z0-9][a-z0-9._-]{0,62}$",
   "type": "string"
  }
 },
 "required": [
  "schema",
  "snapshot_id",
  "tenant",
  "workload",
  "environment",
  "site",
  "fingerprint",
  "memory_mib",
  "runtime",
  "capture_schema",
  "generation",
  "envelope",
  "created_at"
 ],
 "title": "PK_SNAPSHOT_MANIFEST/1",
 "type": "object"
}
