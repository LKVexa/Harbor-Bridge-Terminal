{
 "$id": "urn:pk:inv26:PK_SNAPSHOT_RESTORE_REQUEST/2",
 "$schema": "https://json-schema.org/draft/2020-12/schema",
 "additionalProperties": false,
 "properties": {
  "deadline_ms": {
   "maximum": 60000,
   "minimum": 1,
   "type": "integer"
  },
  "devices": {
   "items": {
    "maxLength": 63,
    "pattern": "^[a-z0-9][a-z0-9._:-]{0,62}$",
    "type": "string"
   },
   "maxItems": 64,
   "minItems": 1,
   "type": "array",
   "uniqueItems": true
  },
  "environment": {
   "maxLength": 63,
   "pattern": "^[a-z0-9][a-z0-9._-]{0,62}$",
   "type": "string"
  },
  "grant": {
   "maxLength": 4096,
   "type": "string"
  },
  "idempotency_key": {
   "pattern": "^[A-Za-z0-9_-]{8,64}$",
   "type": "string"
  },
  "schema": {
   "const": "PK_SNAPSHOT_RESTORE_REQUEST/2"
  },
  "site": {
   "maxLength": 63,
   "pattern": "^[a-z0-9][a-z0-9._-]{0,62}$",
   "type": "string"
  },
  "snapshot_id": {
   "maxLength": 63,
   "pattern": "^[a-z0-9][a-z0-9._-]{0,62}$",
   "type": "string"
  },
  "target_vm_id": {
   "maxLength": 63,
   "pattern": "^[a-z0-9][a-z0-9._-]{0,62}$",
   "type": "string"
  },
  "tenant": {
   "maxLength": 63,
   "pattern": "^[a-z0-9][a-z0-9._-]{0,62}$",
   "type": "string"
  },
  "traceparent": {
   "maxLength": 55,
   "type": "string"
  },
  "workload": {
   "maxLength": 63,
   "pattern": "^[a-z0-9][a-z0-9._-]{0,62}$",
   "type": "string"
  }
 },
 "required": [
  "schema",
  "snapshot_id",
  "tenant",
  "workload",
  "environment",
  "devices",
  "target_vm_id",
  "grant",
  "idempotency_key"
 ],
 "title": "PK_SNAPSHOT_RESTORE_REQUEST/2",
 "type": "object"
}
