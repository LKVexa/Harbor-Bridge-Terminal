# EXECUTED — INV-66 v4.2.0 → 4.3.0 missing-components checklist

Generated by `tools/mc_status.py` from the governing checklist (verbatim copy alongside) and `release/ci_report.json`.
Items: 1420 · DONE 852 · NA_PROPOSED 7 · OPEN_EXTERNAL 21 · OPEN_HUMAN 114 · PARTIAL 426
MC dispositions: IMPLEMENTED_PENDING_HUMAN_DECISIONS 4 · NA_PROPOSED 1 · PARTIAL 50 · PARTIAL_EXTERNAL_DEPENDENCY 16 · **CLOSED 0**
A box is ticked only for DONE items whose evidence verified in this run. No item is independently reviewed yet.

---

# INV-66 v4.2.0 — Missing Components Implementation & Verification Checklist

**Repository:** Enterprise Wasm Control Plane (`INV-66`)  
**Baseline audited release:** `4.2.0`  
**Source gap inventory:** `inv66_enterprise_wasm_control_plane_v4.2.0_MISSING_COMPONENTS.md`  
**Checklist scope:** all 71 post-hardening missing production components/artifacts  
**Severity distribution:** 26 Critical, 39 High, 5 Medium, 1 Low/conditional  

## How to use this checklist

- A checkbox is **not complete** because a design note exists. Mark it complete only when the described implementation/control exists and its required evidence is retained.
- Task IDs are stable in the form `MC-###-T##`; Definition-of-Done evidence items use `MC-###-D##`.
- Security-critical controls should fail closed unless an explicitly approved degraded mode is defined and tested.
- Machine-readable acceptance evidence should record artifact/test digests, tool/runtime versions, environment, timestamp, owner, and reviewer.
- Any `N/A` disposition must be explicit, justified, approved, and represented in the RTM/evidence bundle; do not delete the task.
- Any temporary exception requires an entry in the exception/waiver register (MC-067) with owner, compensating controls, and expiry.

## Recommended implementation sequence

1. **Governance and normative baseline:** MC-001 through MC-011 plus MC-071.
2. **Typed trust boundaries and integrations:** MC-012 through MC-021, MC-029 through MC-037.
3. **Durable configuration/state and distributed correctness:** MC-022 through MC-028, MC-038 through MC-043, MC-069 and MC-070.
4. **Performance and operator visibility:** MC-044 through MC-054.
5. **Certification, release, and operations:** MC-055 through MC-068.

> This ordering is dependency-oriented, not a waiver of severity. Critical security/durability gaps should remain release-blocking until closed.

# Architecture and governance

## MC-001 — Bundled master prompt/workflow source (`MASTER.md`)

> **4.3.0 disposition: PARTIAL** · DONE 15 · OPEN_HUMAN 2 · PARTIAL 3  
> Evidence: `MASTER.md` (exists), `tools/gen_master.py` (exists), `RELEASE_MANIFEST.sha256` (exists), `lane:master` (PASS)

**Severity:** Medium  
**Related controls:** C020, C090, C100  
**Gap statement:** README previously claimed it was present, but it is not in the archive. Restore the authoritative 100-item master prompt/workflow source or remove it from the release bill of materials.  

### Implementation checklist

- [ ] `MC-001-T01` Locate the authoritative source for the Post-Kubernetes Master Prompt & Workflow Series and determine whether INV-66 is governed by a frozen release, generated source, or maintained upstream document.  
  **→ PARTIAL** — upstream series archive not supplied; MASTER.md is derived from CHECKLIST.json and marked informative
- [x] `MC-001-T02` Create `MASTER.md` with an immutable document identifier, semantic version, source revision/commit, generation timestamp, and SHA-256 digest of the authoritative source.  
  **→ DONE** — implemented; see evidence
- [x] `MC-001-T03` Ensure all 100 INV-66 checklist requirements are represented without silent omission, reordering, or semantic weakening; add a machine-verifiable count and ID reconciliation check.  
  **→ DONE** — implemented; see evidence
- [x] `MC-001-T04` Add a provenance header identifying the generator/source path and the exact transformation rules used if `MASTER.md` is generated rather than hand-maintained.  
  **→ DONE** — implemented; see evidence
- [x] `MC-001-T05` Add CI validation that fails if README/BOM claims `MASTER.md` exists but the file is missing, or if its digest/count no longer matches the authoritative source.  
  **→ DONE** — implemented; see evidence
- [x] `MC-001-T06` Add release-manifest coverage so `MASTER.md` is included in `RELEASE_MANIFEST.sha256` and therefore protected by release integrity checks.  
  **→ DONE** — implemented; see evidence
- [x] `MC-001-T07` Document whether `MASTER.md` is normative or informative and define the change-control process required to update it.  
  **→ DONE** — implemented; see evidence
- [x] `MC-001-T08` If the project intentionally does not bundle the master source, remove all inclusion claims and replace them with a resolvable, version-pinned source reference plus offline retrieval instructions.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-001-T09` Define the artifact owner, reviewer roles, approval authority, change-control path, and review cadence.  
  **→ OPEN_HUMAN** — roles defined in governance/owners.json; people/teams UNASSIGNED
- [x] `MC-001-T10` Capture assumptions, non-goals, dependencies, trust boundaries, failure domains, and unsupported modes that materially affect this component.  
  **→ DONE** — stated in the artifact
- [x] `MC-001-T11` Use stable document/schema IDs and semantic versions; keep a changelog or revision history suitable for release evidence.  
  **→ DONE** — stable IDs + versions; CHANGELOG
- [x] `MC-001-T12` Link the artifact to relevant `CHECKLIST.json` controls and normative requirement IDs in the RTM.  
  **→ DONE** — release/rtm.json
- [x] `MC-001-T13` Add automated repository checks for required presence, internal links/references, version consistency, and release-manifest inclusion.  
  **→ DONE** — tools/ci.py lanes docs/master/schema-drift/requirements-drift

### Definition of done / acceptance evidence

- [x] `MC-001-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-001-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-001-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-001-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-001-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-001-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-001-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-002 — Approved architecture decision record (ADR)

> **4.3.0 disposition: IMPLEMENTED_PENDING_HUMAN_DECISIONS** · DONE 14 · OPEN_HUMAN 3 · PARTIAL 3  
> Evidence: `docs/adr/ADR-0001-enterprise-wasm-control-plane.md` (exists)

**Severity:** High  
**Related controls:** C010  
**Gap statement:** No ADR records the Cosmonic Control/wasmCloud technology choice, alternatives, constraints, or approval.  

### Implementation checklist

- [x] `MC-002-T01` Create an ADR using a stable ID such as `ADR-0001-enterprise-wasm-control-plane.md` and record status, date, deciders, technical owners, and review expiry.  
  **→ DONE** — implemented; see evidence
- [x] `MC-002-T02` Document the decision to use Cosmonic Control/wasmCloud-aligned control-plane concepts, distinguishing product-specific dependencies from architecture-level contracts.  
  **→ DONE** — implemented; see evidence
- [x] `MC-002-T03` Compare at least the chosen approach with a custom wasmCloud controller, Kubernetes/GitOps controller patterns, direct lattice management, and alternative policy/admission architectures.  
  **→ DONE** — implemented; see evidence
- [x] `MC-002-T04` Record decision drivers: multi-lattice governance, tenant isolation, signer/registry policy, GitOps integration, append-only audit, latency SLO, edge/site autonomy, and operational ownership.  
  **→ DONE** — implemented; see evidence
- [x] `MC-002-T05` Record rejected alternatives and explicit reasons including complexity, failure domains, lock-in, portability, security boundary, and lifecycle cost.  
  **→ DONE** — implemented; see evidence
- [x] `MC-002-T06` Capture hard constraints and assumptions from `contract.py`, including untrusted callers, independently failing peers/paths/stores, and local/remote behavioral equivalence.  
  **→ DONE** — implemented; see evidence
- [x] `MC-002-T07` Document consequences: new durable stores, identity federation, policy-engine coupling, deployment-manager adapter, audit anchoring, HA requirements, and operations burden.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-002-T08` Require architecture/security/SRE approval and link the ADR to the RTM and production-exit gate.  
  **→ OPEN_HUMAN** — architecture/security/SRE approval not recorded; ADR is PROPOSED
- [ ] `MC-002-T09` Define the artifact owner, reviewer roles, approval authority, change-control path, and review cadence.  
  **→ OPEN_HUMAN** — roles defined in governance/owners.json; people/teams UNASSIGNED
- [x] `MC-002-T10` Capture assumptions, non-goals, dependencies, trust boundaries, failure domains, and unsupported modes that materially affect this component.  
  **→ DONE** — stated in the artifact
- [x] `MC-002-T11` Use stable document/schema IDs and semantic versions; keep a changelog or revision history suitable for release evidence.  
  **→ DONE** — stable IDs + versions; CHANGELOG
- [x] `MC-002-T12` Link the artifact to relevant `CHECKLIST.json` controls and normative requirement IDs in the RTM.  
  **→ DONE** — release/rtm.json
- [x] `MC-002-T13` Add automated repository checks for required presence, internal links/references, version consistency, and release-manifest inclusion.  
  **→ DONE** — tools/ci.py lanes docs/master/schema-drift/requirements-drift

### Definition of done / acceptance evidence

- [x] `MC-002-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-002-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-002-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [ ] `MC-002-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ PARTIAL** — verification is document review only, or evidence did not verify
- [ ] `MC-002-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-002-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-002-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-003 — Accountable owner and escalation matrix

> **4.3.0 disposition: PARTIAL** · DONE 11 · OPEN_HUMAN 5 · PARTIAL 4  
> Evidence: `governance/owners.json` (exists), `CODEOWNERS` (exists), `tools/review.py` (exists)

**Severity:** High  
**Related controls:** C009, C097  
**Gap statement:** No named service owner, security owner, on-call owner, or escalation route is packaged.  

### Implementation checklist

- [x] `MC-003-T01` Define a RACI/ownership matrix covering service owner, product owner, architecture owner, security owner, IAM owner, SRE/on-call owner, data/audit owner, and release manager.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-003-T02` Assign each role to a durable team or rotation rather than only an individual; include primary and secondary escalation contacts.  
  **→ OPEN_HUMAN** — no teams/rotations bound
- [ ] `MC-003-T03` Define 24x7 vs business-hours support expectations and severity-based acknowledgement/engagement targets.  
  **→ OPEN_HUMAN** — 24x7 vs business hours undecided
- [x] `MC-003-T04` Map ownership to concrete boundaries: admission service, RBAC/policy data, registry/signer policy, audit store, identity federation, deployment adapter, CI/CD, and infrastructure.  
  **→ DONE** — implemented; see evidence
- [x] `MC-003-T05` Define who can approve emergency policy changes, disable admission, rotate keys, restore state, waive controls, and declare production readiness.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-003-T06` Create an escalation tree for dependency failures involving INV-63, INV-64, GAP-13, GAP-07, identity, registry, KMS, and storage teams.  
  **→ OPEN_HUMAN** — escalation contacts for peer teams UNASSIGNED
- [ ] `MC-003-T07` Include ownership metadata in runbooks, alert routing, CODEOWNERS/review rules, and production-exit evidence.  
  **→ PARTIAL** — CODEOWNERS/alert routing use UNASSIGNED placeholders
- [x] `MC-003-T08` Add a recurring control that detects orphaned ownership when teams, aliases, or rotations change.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-003-T09` Define the artifact owner, reviewer roles, approval authority, change-control path, and review cadence.  
  **→ OPEN_HUMAN** — roles defined in governance/owners.json; people/teams UNASSIGNED
- [x] `MC-003-T10` Capture assumptions, non-goals, dependencies, trust boundaries, failure domains, and unsupported modes that materially affect this component.  
  **→ DONE** — stated in the artifact
- [x] `MC-003-T11` Use stable document/schema IDs and semantic versions; keep a changelog or revision history suitable for release evidence.  
  **→ DONE** — stable IDs + versions; CHANGELOG
- [x] `MC-003-T12` Link the artifact to relevant `CHECKLIST.json` controls and normative requirement IDs in the RTM.  
  **→ DONE** — release/rtm.json
- [x] `MC-003-T13` Add automated repository checks for required presence, internal links/references, version consistency, and release-manifest inclusion.  
  **→ DONE** — tools/ci.py lanes docs/master/schema-drift/requirements-drift

### Definition of done / acceptance evidence

- [x] `MC-003-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-003-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-003-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [ ] `MC-003-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ PARTIAL** — verification is document review only, or evidence did not verify
- [ ] `MC-003-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-003-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-003-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-004 — Deployment/topology architecture specification

> **4.3.0 disposition: PARTIAL** · DONE 14 · OPEN_HUMAN 2 · PARTIAL 4  
> Evidence: `docs/TOPOLOGY.md` (exists)

**Severity:** High  
**Related controls:** C003, C005, C006, C051  
**Gap statement:** No production topology defines control-plane instances, trust zones, stores, sites, dependency endpoints, or failure domains.  

### Implementation checklist

- [x] `MC-004-T01` Produce a production topology diagram covering control-plane instances, load balancers/gateways, identity provider, policy engine, provenance service, registry endpoints, audit store, configuration store, and INV-63 deployment manager.  
  **→ DONE** — implemented; see evidence
- [x] `MC-004-T02` Define trust zones and network boundaries for tenant traffic, administrative traffic, east-west service calls, storage access, and cross-site replication.  
  **→ DONE** — implemented; see evidence
- [x] `MC-004-T03` Document deployment modes for single site, multi-site, regional, disconnected/edge, and disaster-recovery operation, including unsupported topologies.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-004-T04` Define failure domains and placement rules so replicas do not share node/rack/zone/site single points of failure where the availability target requires separation.  
  **→ PARTIAL** — placement rules stated; not validated against real infrastructure
- [x] `MC-004-T05` Identify every persisted dataset, its authoritative owner, consistency model, replication scope, retention class, residency constraints, and recovery objective.  
  **→ DONE** — implemented; see evidence
- [x] `MC-004-T06` Define ingress/egress ports, protocols, TLS identities, DNS/service-discovery assumptions, firewall policy, and required network reachability.  
  **→ DONE** — implemented; see evidence
- [x] `MC-004-T07` Document scaling units and control loops: stateless admission replicas, stateful stores, worker/queue partitions, and cross-lattice inventory shards.  
  **→ DONE** — implemented; see evidence
- [x] `MC-004-T08` Add sequence/data-flow diagrams for successful admission, denial, dependency failure, configuration activation, rollback, and emergency freeze.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-004-T09` Define the artifact owner, reviewer roles, approval authority, change-control path, and review cadence.  
  **→ OPEN_HUMAN** — roles defined in governance/owners.json; people/teams UNASSIGNED
- [x] `MC-004-T10` Capture assumptions, non-goals, dependencies, trust boundaries, failure domains, and unsupported modes that materially affect this component.  
  **→ DONE** — stated in the artifact
- [x] `MC-004-T11` Use stable document/schema IDs and semantic versions; keep a changelog or revision history suitable for release evidence.  
  **→ DONE** — stable IDs + versions; CHANGELOG
- [x] `MC-004-T12` Link the artifact to relevant `CHECKLIST.json` controls and normative requirement IDs in the RTM.  
  **→ DONE** — release/rtm.json
- [x] `MC-004-T13` Add automated repository checks for required presence, internal links/references, version consistency, and release-manifest inclusion.  
  **→ DONE** — tools/ci.py lanes docs/master/schema-drift/requirements-drift

### Definition of done / acceptance evidence

- [x] `MC-004-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-004-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-004-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [ ] `MC-004-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ PARTIAL** — verification is document review only, or evidence did not verify
- [ ] `MC-004-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-004-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-004-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-005 — Production source-of-truth design

> **4.3.0 disposition: IMPLEMENTED_PENDING_HUMAN_DECISIONS** · DONE 16 · OPEN_HUMAN 2 · PARTIAL 2  
> Evidence: `docs/SOURCE_OF_TRUTH.md` (exists), `production/journal.py` (exists), `tests.disaster.test_disaster.DisasterTest.test_backup_restore_reconstructs_identical_state` (1/1 pass), `tests.fault.test_faults.FaultTest.test_crash_between_decision_and_delivery_resumes_after_restart` (1/1 pass)

**Severity:** Critical  
**Related controls:** C004, C032, C057, C095  
**Gap statement:** The contract says the audit log is authoritative, but only process-local memory exists; no durable authoritative store or consistency model is implemented.  
**Key cross-component dependencies:** MC-014, MC-023, MC-035, MC-038, MC-063, MC-070  

### Implementation checklist

- [x] `MC-005-T01` Define the authoritative data model for admissions, refusals, policy/config versions, RBAC changes, registry/signer changes, forwarding outcomes, and cross-lattice inventory.  
  **→ DONE** — implemented; see evidence
- [x] `MC-005-T02` Select a durable source-of-truth architecture: append-only event log with projections, transactional database plus immutable journal, or equivalent design with documented consistency semantics.  
  **→ DONE** — implemented; see evidence
- [x] `MC-005-T03` Define event IDs, monotonic ordering scope, causal/correlation identifiers, idempotency keys, actor identity, policy/config digests, timestamps, and integrity fields.  
  **→ DONE** — implemented; see evidence
- [x] `MC-005-T04` Define exactly which state is reconstructed from the log and which state is independently authoritative; prohibit ambiguous dual-write ownership.  
  **→ DONE** — implemented; see evidence
- [x] `MC-005-T05` Implement atomic persistence semantics so a decision cannot be acknowledged as durable if its authoritative audit/state record was not committed.  
  **→ DONE** — implemented; see evidence
- [x] `MC-005-T06` Define recovery/replay from an empty projection, checkpoint/snapshot strategy, schema evolution, and deterministic replay requirements.  
  **→ DONE** — implemented; see evidence
- [x] `MC-005-T07` Protect the source of truth using access control, encryption, tamper-evident anchoring, backup/restore, retention, and legal-hold controls.  
  **→ DONE** — implemented; see evidence
- [x] `MC-005-T08` Add consistency tests proving post-crash state, replayed state, exported audit state, and live projections converge to the same logical result.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-005-T09` Define the artifact owner, reviewer roles, approval authority, change-control path, and review cadence.  
  **→ OPEN_HUMAN** — roles defined in governance/owners.json; people/teams UNASSIGNED
- [x] `MC-005-T10` Capture assumptions, non-goals, dependencies, trust boundaries, failure domains, and unsupported modes that materially affect this component.  
  **→ DONE** — stated in the artifact
- [x] `MC-005-T11` Use stable document/schema IDs and semantic versions; keep a changelog or revision history suitable for release evidence.  
  **→ DONE** — stable IDs + versions; CHANGELOG
- [x] `MC-005-T12` Link the artifact to relevant `CHECKLIST.json` controls and normative requirement IDs in the RTM.  
  **→ DONE** — release/rtm.json
- [x] `MC-005-T13` Add automated repository checks for required presence, internal links/references, version consistency, and release-manifest inclusion.  
  **→ DONE** — tools/ci.py lanes docs/master/schema-drift/requirements-drift

### Definition of done / acceptance evidence

- [x] `MC-005-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-005-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-005-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-005-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-005-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-005-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-005-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

# Requirements and semantics

## MC-006 — Normative SHALL requirements specification

> **4.3.0 disposition: IMPLEMENTED_PENDING_HUMAN_DECISIONS** · DONE 16 · OPEN_HUMAN 2 · PARTIAL 2  
> Evidence: `requirements/requirements.json` (exists), `tools/gen_requirements.py` (exists), `lane:requirements-drift` (PASS)

**Severity:** High  
**Related controls:** C011-C019  
**Gap statement:** The checklist asks for testable requirements, but the archive has no separate normative requirements document with IDs, acceptance criteria, and owners.  

### Implementation checklist

- [x] `MC-006-T01` Create a normative requirements specification with stable IDs (for example `REQ-INV66-*`) and RFC 2119/8174-style SHALL/SHOULD/MAY language.  
  **→ DONE** — implemented; see evidence
- [x] `MC-006-T02` Cover functional requirements for admission, RBAC, registry/signer policy, audit, cross-lattice inventory, GitOps intake, configuration, and administrative controls.  
  **→ DONE** — implemented; see evidence
- [x] `MC-006-T03` Cover non-functional requirements for availability, durability, consistency, latency, throughput, isolation, residency, recoverability, observability, and operability.  
  **→ DONE** — implemented; see evidence
- [x] `MC-006-T04` For every SHALL, define measurable acceptance criteria, evidence type, owner, and failure disposition.  
  **→ DONE** — implemented; see evidence
- [x] `MC-006-T05` Separate normative requirements from implementation notes, rationale, examples, and future work so certification cannot be satisfied by prose-only intent.  
  **→ DONE** — implemented; see evidence
- [x] `MC-006-T06` Express negative/security requirements explicitly, including zero forwarding of unadmitted manifests, fail-closed conditions, and forbidden cross-tenant disclosure.  
  **→ DONE** — implemented; see evidence
- [x] `MC-006-T07` Assign version and change history; require impact analysis when a normative requirement changes.  
  **→ DONE** — implemented; see evidence
- [x] `MC-006-T08` Validate that the specification maps without gaps to all applicable C001-C100 checklist items.  
  **→ DONE** — implemented; see evidence
- [x] `MC-006-T09` Express externally observable behavior deterministically, including success, denial, degraded, retryable, and terminal outcomes.  
  **→ DONE** — typed decision states + error registry
- [x] `MC-006-T10` Define tenant/site/environment scope and any consistency, ordering, freshness, time, or isolation guarantees needed to remove ambiguity.  
  **→ DONE** — scope paths + site/environment in config
- [x] `MC-006-T11` Assign stable normative IDs and map each behavior to implementation and verification evidence in the RTM.  
  **→ DONE** — requirements/requirements.json REQ-INV66-*
- [x] `MC-006-T12` Create executable/golden fixtures for boundary cases and conflicting inputs rather than relying solely on prose.  
  **→ DONE** — fixtures/compat + policy conflict fixtures in tests
- [ ] `MC-006-T13` Require architecture/security/SRE review for semantics that can affect authorization, durability, or production availability.  
  **→ OPEN_HUMAN** — review not performed

### Definition of done / acceptance evidence

- [x] `MC-006-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-006-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-006-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-006-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-006-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-006-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-006-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-007 — Requirements traceability matrix (RTM)

> **4.3.0 disposition: IMPLEMENTED_PENDING_HUMAN_DECISIONS** · DONE 16 · OPEN_HUMAN 2 · PARTIAL 2  
> Evidence: `release/rtm.json` (exists), `tools/rtm.py` (exists), `docs/COVERAGE.md` (exists), `release/rtm-history` (exists), `lane:rtm` (PASS)

**Severity:** High  
**Related controls:** C020, C090, C100  
**Gap statement:** No machine-readable mapping connects all 100 requirements to code, tests, evidence, exceptions, and release status.  

### Implementation checklist

- [x] `MC-007-T01` Create a machine-readable RTM in JSON/YAML/CSV with one row per normative requirement and one row per C001-C100 control where appropriate.  
  **→ DONE** — implemented; see evidence
- [x] `MC-007-T02` Include fields for requirement ID, source, owner, implementation artifact/path, test IDs, evidence URI/hash, status, waiver ID, release version, and last verification timestamp.  
  **→ DONE** — implemented; see evidence
- [x] `MC-007-T03` Allow many-to-many mappings between requirements, code modules, schemas, tests, dashboards, runbooks, and evidence.  
  **→ DONE** — implemented; see evidence
- [x] `MC-007-T04` Fail CI when a normative SHALL has no implementation reference, no verification reference, an expired waiver, or stale/missing evidence.  
  **→ DONE** — implemented; see evidence
- [x] `MC-007-T05` Validate that all 100 checklist IDs are present exactly once in the control coverage view and that no unknown/deprecated IDs appear silently.  
  **→ DONE** — implemented; see evidence
- [x] `MC-007-T06` Generate human-readable coverage summaries from the RTM rather than maintaining duplicate manually edited status tables.  
  **→ DONE** — implemented; see evidence
- [x] `MC-007-T07` Preserve historical RTM snapshots per release to support auditability and regression analysis.  
  **→ DONE** — implemented; see evidence
- [x] `MC-007-T08` Link each MC-001..MC-071 closure to the RTM evidence that demonstrates the gap is actually resolved.  
  **→ DONE** — implemented; see evidence
- [x] `MC-007-T09` Express externally observable behavior deterministically, including success, denial, degraded, retryable, and terminal outcomes.  
  **→ DONE** — typed decision states + error registry
- [x] `MC-007-T10` Define tenant/site/environment scope and any consistency, ordering, freshness, time, or isolation guarantees needed to remove ambiguity.  
  **→ DONE** — scope paths + site/environment in config
- [x] `MC-007-T11` Assign stable normative IDs and map each behavior to implementation and verification evidence in the RTM.  
  **→ DONE** — requirements/requirements.json REQ-INV66-*
- [x] `MC-007-T12` Create executable/golden fixtures for boundary cases and conflicting inputs rather than relying solely on prose.  
  **→ DONE** — fixtures/compat + policy conflict fixtures in tests
- [ ] `MC-007-T13` Require architecture/security/SRE review for semantics that can affect authorization, durability, or production availability.  
  **→ OPEN_HUMAN** — review not performed

### Definition of done / acceptance evidence

- [x] `MC-007-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-007-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-007-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-007-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-007-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-007-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-007-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-008 — Lifecycle/state-transition model

> **4.3.0 disposition: PARTIAL** · DONE 15 · OPEN_HUMAN 2 · PARTIAL 3  
> Evidence: `production/lifecycle.py` (exists), `docs/LIFECYCLE.md` (exists), `tests.unit.test_units.SmallPartsTest.test_lifecycle` (1/1 pass), `tests.concurrency.test_races.RaceTest.test_idempotent_duplicates_under_race_yield_one_decision` (1/1 pass)

**Severity:** High  
**Related controls:** C014-C015  
**Gap statement:** No explicit state machine defines proposed/admitted/rejected/deployed/rolled-back/quarantined states and legal transitions.  

### Implementation checklist

- [x] `MC-008-T01` Define an explicit workload/change lifecycle such as proposed -> validated -> authorized -> admitted -> persisted -> forwarded -> acknowledged -> deployed, plus rejected/quarantined/rolled-back/failed terminal or recovery states.  
  **→ DONE** — implemented; see evidence
- [x] `MC-008-T02` Define legal transitions, transition initiators, guards, side effects, and required durable writes using a state-transition table or executable state machine.  
  **→ DONE** — implemented; see evidence
- [x] `MC-008-T03` Distinguish admission decision state from downstream deployment/reconciliation state so a successful admission is not misreported as successful deployment.  
  **→ DONE** — implemented; see evidence
- [x] `MC-008-T04` Define retryable vs terminal failures at each transition and whether retries reuse the same idempotency key and decision record.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-008-T05` Define cancellation and supersession semantics for GitOps changes that are replaced while validation or forwarding is in progress.  
  **→ PARTIAL** — supersession defined by per-commit idempotency; in-flight cancellation of superseded GitOps change not implemented
- [x] `MC-008-T06` Define rollback transitions and whether rollback itself requires fresh authorization/admission.  
  **→ DONE** — implemented; see evidence
- [x] `MC-008-T07` Include quarantine/freeze transitions that prevent forwarding while preserving operator visibility and auditability.  
  **→ DONE** — implemented; see evidence
- [x] `MC-008-T08` Create transition tests that prove illegal transitions fail closed and concurrent transitions cannot produce contradictory terminal states.  
  **→ DONE** — implemented; see evidence
- [x] `MC-008-T09` Express externally observable behavior deterministically, including success, denial, degraded, retryable, and terminal outcomes.  
  **→ DONE** — typed decision states + error registry
- [x] `MC-008-T10` Define tenant/site/environment scope and any consistency, ordering, freshness, time, or isolation guarantees needed to remove ambiguity.  
  **→ DONE** — scope paths + site/environment in config
- [x] `MC-008-T11` Assign stable normative IDs and map each behavior to implementation and verification evidence in the RTM.  
  **→ DONE** — requirements/requirements.json REQ-INV66-*
- [x] `MC-008-T12` Create executable/golden fixtures for boundary cases and conflicting inputs rather than relying solely on prose.  
  **→ DONE** — fixtures/compat + policy conflict fixtures in tests
- [ ] `MC-008-T13` Require architecture/security/SRE review for semantics that can affect authorization, durability, or production availability.  
  **→ OPEN_HUMAN** — review not performed

### Definition of done / acceptance evidence

- [x] `MC-008-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-008-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-008-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-008-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-008-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-008-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-008-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-009 — Capacity, quota, and fairness model

> **4.3.0 disposition: PARTIAL** · DONE 13 · OPEN_HUMAN 2 · PARTIAL 5  
> Evidence: `production/quota.py` (exists), `production/resilience.py` (exists), `docs/CAPACITY.md` (exists), `tests.unit.test_units.SmallPartsTest.test_quota` (1/1 pass), `tests.unit.test_units.SmallPartsTest.test_shedder_fairness` (1/1 pass), `tests.fault.test_faults.FaultTest.test_quota_exhaustion_is_retryable_and_journaled` (1/1 pass)

**Severity:** High  
**Related controls:** C017, C028, C067, C069  
**Gap statement:** `max_components` and manifest bytes are local parser limits only; tenant/lattice quotas, concurrency budgets, and fairness are absent.  

### Implementation checklist

- [ ] `MC-009-T01` Define quota dimensions by organisation, tenant, lattice, environment, site, workload, principal, and API operation where applicable.  
  **→ PARTIAL** — quota by tenant + global in-flight; per-principal/per-site/per-operation dimensions not implemented
- [ ] `MC-009-T02` Specify hard limits and soft budgets for request rate, concurrent admissions, manifest bytes/components, pending forwards, inventory objects, audit write rate, and storage consumption.  
  **→ PARTIAL** — limits for request rate, in-flight, manifest bytes/components; pending forwards/inventory/audit-rate/storage not hard-limited
- [x] `MC-009-T03` Define fairness algorithm and isolation strategy (for example per-tenant token buckets/weighted fair queues) so one tenant cannot starve others.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-009-T04` Define burst capacity, refill rates, borrowing rules, and administrative overrides with expiration and audit.  
  **→ PARTIAL** — no borrowing or time-bounded overrides; overrides only via new config generation
- [x] `MC-009-T05` Expose quota headers/status or typed error details so clients can distinguish policy denial from throttling/capacity exhaustion.  
  **→ DONE** — implemented; see evidence
- [x] `MC-009-T06` Define queue depth and wait-time limits; reject or shed load before unbounded memory or latency growth occurs.  
  **→ DONE** — implemented; see evidence
- [x] `MC-009-T07` Create capacity formulas linking replica count and dependency throughput to safe admitted QPS and tenant count.  
  **→ DONE** — implemented; see evidence
- [x] `MC-009-T08` Test noisy-neighbor scenarios and prove per-tenant limits and global protection remain effective under overload.  
  **→ DONE** — implemented; see evidence
- [x] `MC-009-T09` Express externally observable behavior deterministically, including success, denial, degraded, retryable, and terminal outcomes.  
  **→ DONE** — typed decision states + error registry
- [x] `MC-009-T10` Define tenant/site/environment scope and any consistency, ordering, freshness, time, or isolation guarantees needed to remove ambiguity.  
  **→ DONE** — scope paths + site/environment in config
- [x] `MC-009-T11` Assign stable normative IDs and map each behavior to implementation and verification evidence in the RTM.  
  **→ DONE** — requirements/requirements.json REQ-INV66-*
- [x] `MC-009-T12` Create executable/golden fixtures for boundary cases and conflicting inputs rather than relying solely on prose.  
  **→ DONE** — fixtures/compat + policy conflict fixtures in tests
- [ ] `MC-009-T13` Require architecture/security/SRE review for semantics that can affect authorization, durability, or production availability.  
  **→ OPEN_HUMAN** — review not performed

### Definition of done / acceptance evidence

- [x] `MC-009-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-009-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-009-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-009-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-009-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-009-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-009-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-010 — Offline/partition semantics

> **4.3.0 disposition: PARTIAL** · DONE 13 · OPEN_HUMAN 2 · PARTIAL 5  
> Evidence: `docs/PARTITION_SEMANTICS.md` (exists), `tests.fault.test_faults` (11/11 pass), `tests.disaster.test_disaster.DisasterTest.test_partition_from_deployment_manager_then_reconnect` (1/1 pass)

**Severity:** High  
**Related controls:** C018, C048, C055-C057, C089  
**Gap statement:** No documented or implemented behavior exists for identity, policy, registry, signer, deployment-manager, or audit-store unavailability.  

### Implementation checklist

- [ ] `MC-010-T01` Document behavior independently for identity, policy, registry, signer/provenance, deployment manager, audit store, configuration store, DNS/time, and network unavailability.  
  **→ PARTIAL** — identity/policy/registry/signer/deployment/journal/config/clock covered; DNS not specified
- [x] `MC-010-T02` Classify each dependency as security-critical, durability-critical, availability-enhancing, or optional and define fail-closed/fail-open/degraded behavior accordingly.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-010-T03` Define cache eligibility, maximum staleness, signed cache artifacts, revocation behavior, and clock assumptions for offline operation.  
  **→ PARTIAL** — no signed offline cache artifacts; offline admission unsupported by design
- [x] `MC-010-T04` Define whether previously admitted workloads may continue, whether new admissions are blocked, and whether administrative changes are accepted during partitions.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-010-T05` Define local-site autonomy and reconciliation semantics when connectivity returns, including conflict resolution and duplicate-event handling.  
  **→ PARTIAL** — local-site autonomy unsupported; reconnect = follower refresh + redelivery
- [x] `MC-010-T06` Define operator-visible degraded states and explicit reason codes for denials caused by unavailable trust dependencies.  
  **→ DONE** — implemented; see evidence
- [x] `MC-010-T07` Prevent stale identity/policy/provenance data from silently extending beyond configured TTL/lease periods.  
  **→ DONE** — implemented; see evidence
- [x] `MC-010-T08` Create partition/reconnect tests that exercise each dependency independently and in correlated failure combinations.  
  **→ DONE** — implemented; see evidence
- [x] `MC-010-T09` Express externally observable behavior deterministically, including success, denial, degraded, retryable, and terminal outcomes.  
  **→ DONE** — typed decision states + error registry
- [x] `MC-010-T10` Define tenant/site/environment scope and any consistency, ordering, freshness, time, or isolation guarantees needed to remove ambiguity.  
  **→ DONE** — scope paths + site/environment in config
- [x] `MC-010-T11` Assign stable normative IDs and map each behavior to implementation and verification evidence in the RTM.  
  **→ DONE** — requirements/requirements.json REQ-INV66-*
- [x] `MC-010-T12` Create executable/golden fixtures for boundary cases and conflicting inputs rather than relying solely on prose.  
  **→ DONE** — fixtures/compat + policy conflict fixtures in tests
- [ ] `MC-010-T13` Require architecture/security/SRE review for semantics that can affect authorization, durability, or production availability.  
  **→ OPEN_HUMAN** — review not performed

### Definition of done / acceptance evidence

- [x] `MC-010-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-010-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-010-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-010-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-010-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-010-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-010-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-011 — Constraint precedence policy

> **4.3.0 disposition: PARTIAL** · DONE 14 · OPEN_HUMAN 2 · PARTIAL 4  
> Evidence: `docs/PRECEDENCE.md` (exists), `production/policy_engine.py` (exists), `tests.unit.test_units.PolicyEngineTest` (2/2 pass)

**Severity:** Medium  
**Related controls:** C019  
**Gap statement:** No deterministic precedence rules resolve security, residency, SLO, availability, and cost conflicts.  

### Implementation checklist

- [x] `MC-011-T01` Define a deterministic precedence lattice for security, legal/residency, tenant isolation, safety, availability/SLO, performance, and cost constraints.  
  **→ DONE** — implemented; see evidence
- [x] `MC-011-T02` Specify non-overridable controls such as signature/provenance trust, tenant isolation, and legal residency where applicable.  
  **→ DONE** — implemented; see evidence
- [x] `MC-011-T03` Define how conflicts are surfaced as stable machine-readable reason codes rather than relying on evaluation order or free-form text.  
  **→ DONE** — implemented; see evidence
- [x] `MC-011-T04` Define policy-composition semantics for organisation, tenant, environment, site, lattice, and workload scopes including deny-overrides/permit-overrides rules.  
  **→ DONE** — implemented; see evidence
- [x] `MC-011-T05` Define tie-breaking for equally scoped policies using explicit priority/version rather than incidental storage or iteration order.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-011-T06` Document emergency exceptions, who may authorize them, their maximum TTL, required compensating controls, and mandatory audit fields.  
  **→ PARTIAL** — emergency lever is restrictive freeze only; no TTL-bounded emergency exception mechanism
- [ ] `MC-011-T07` Create conflict fixtures that prove the same input produces the same outcome across nodes, versions, and policy-engine deployment modes.  
  **→ PARTIAL** — determinism proven in-process under shuffled rule order; not across nodes/versions/engine modes
- [x] `MC-011-T08` Link precedence rules to the operator explain view so rejected constraints and winning constraints are visible.  
  **→ DONE** — implemented; see evidence
- [x] `MC-011-T09` Express externally observable behavior deterministically, including success, denial, degraded, retryable, and terminal outcomes.  
  **→ DONE** — typed decision states + error registry
- [x] `MC-011-T10` Define tenant/site/environment scope and any consistency, ordering, freshness, time, or isolation guarantees needed to remove ambiguity.  
  **→ DONE** — scope paths + site/environment in config
- [x] `MC-011-T11` Assign stable normative IDs and map each behavior to implementation and verification evidence in the RTM.  
  **→ DONE** — requirements/requirements.json REQ-INV66-*
- [x] `MC-011-T12` Create executable/golden fixtures for boundary cases and conflicting inputs rather than relying solely on prose.  
  **→ DONE** — fixtures/compat + policy conflict fixtures in tests
- [ ] `MC-011-T13` Require architecture/security/SRE review for semantics that can affect authorization, durability, or production availability.  
  **→ OPEN_HUMAN** — review not performed

### Definition of done / acceptance evidence

- [x] `MC-011-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-011-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-011-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-011-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-011-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-011-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-011-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

# Interfaces and integration

## MC-012 — Versioned typed admission schema

> **4.3.0 disposition: PARTIAL_EXTERNAL_DEPENDENCY** · DONE 15 · OPEN_EXTERNAL 1 · OPEN_HUMAN 1 · PARTIAL 3  
> Evidence: `schemas/PK_ECP_ADMIT_REQUEST_1.schema.json` (exists), `schemas/PK_ECP_ADMIT_DECISION_1.schema.json` (exists), `fixtures/compat` (exists), `tests.contract.test_contracts` (10/10 pass), `tests.fuzz.test_fuzz` (4/4 pass)

**Severity:** Critical  
**Related controls:** C021-C022, C082  
**Gap statement:** `PK_ECP_ADMIT/1` is named but no JSON Schema, protobuf, WIT, OpenAPI, or equivalent typed contract is included.  
**Key cross-component dependencies:** MC-015, MC-016, MC-017, MC-018, MC-055  

### Implementation checklist

- [x] `MC-012-T01` Define the canonical `PK_ECP_ADMIT/1` request/response schema in JSON Schema, protobuf, WIT, or another versioned IDL and publish generated language bindings where used.  
  **→ DONE** — implemented; see evidence
- [x] `MC-012-T02` Model authenticated principal separately from user-supplied payload fields; include tenant/organisation, lattice, environment/site context, manifest, request ID, idempotency key, and deadline metadata.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-012-T03` Define manifest schema or reference the authoritative INV-64 schema by immutable version/digest rather than using unconstrained generic JSON.  
  **→ OPEN_EXTERNAL** — authoritative INV-64 manifest schema not available; local component schema used
- [x] `MC-012-T04` Define a typed decision response with admitted/denied status, stable reason/error codes, policy/config/provenance versions, manifest digest, audit event ID, and forwarding disposition.  
  **→ DONE** — implemented; see evidence
- [x] `MC-012-T05` Add explicit maximum sizes/counts, required fields, canonicalization rules, unknown-field behavior, and Unicode/identifier normalization rules.  
  **→ DONE** — implemented; see evidence
- [x] `MC-012-T06` Publish compatibility rules for adding/removing fields and version negotiation for `PK_ECP_ADMIT/1` successors.  
  **→ DONE** — implemented; see evidence
- [x] `MC-012-T07` Generate positive, negative, boundary, and malicious conformance fixtures from the schema.  
  **→ DONE** — implemented; see evidence
- [x] `MC-012-T08` Validate every externally received request against the schema before security-sensitive evaluation and fail closed on validation failure.  
  **→ DONE** — implemented; see evidence
- [x] `MC-012-T09` Document the trust boundary, caller/callee identity, required capability, data classification, and network exposure for the interface.  
  **→ DONE** — docs/INTERFACES.md, docs/TOPOLOGY.md
- [x] `MC-012-T10` Define deterministic timeouts, limits, backpressure, idempotency/retry rules, and dependency-failure behavior.  
  **→ DONE** — docs/INTERFACES.md, production/resilience.py
- [x] `MC-012-T11` Propagate request/correlation/trace identifiers and record the protocol/schema version used for each security-sensitive operation.  
  **→ DONE** — request_id/decision_id/traceparent + protocol recorded per decision
- [ ] `MC-012-T12` Instrument rate/error/latency/saturation metrics and structured error/log events for the interface without leaking secrets.  
  **→ PARTIAL** — service-level metrics; no per-interface stage metrics
- [x] `MC-012-T13` Provide schema-driven contract tests, malicious/negative fixtures, compatibility tests, and protocol-faithful integration tests.  
  **→ DONE** — tests/contract + fixtures/compat

### Definition of done / acceptance evidence

- [x] `MC-012-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-012-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-012-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-012-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-012-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-012-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-012-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-013 — Versioned RBAC administration schema/API

> **4.3.0 disposition: PARTIAL** · DONE 13 · OPEN_HUMAN 1 · PARTIAL 6  
> Evidence: `schemas/PK_ECP_RBAC_REQUEST_1.schema.json` (exists), `production/service.py` (exists), `tests.contract.test_contracts.SchemaContractTest.test_rbac_api_contract` (1/1 pass), `tests.unit.test_additions.AdditionsTest.test_rbac_optimistic_concurrency` (1/1 pass), `tests.security.test_threats.ThreatTest.test_T08_privilege_escalation_by_delegation` (1/1 pass)

**Severity:** Critical  
**Related controls:** C021-C024  
**Gap statement:** `PK_ECP_RBAC/1` has no typed request/response contract or administration surface.  
**Key cross-component dependencies:** MC-025, MC-031, MC-032, MC-055  

### Implementation checklist

- [x] `MC-013-T01` Define a typed `PK_ECP_RBAC/1` administration API covering bindings, groups/service principals, organisation/tenant/lattice scopes, roles/capabilities, conditions, and deny rules.  
  **→ DONE** — implemented; see evidence
- [x] `MC-013-T02` Separate read/query operations from mutation operations and define distinct authorization capabilities for each.  
  **→ DONE** — implemented; see evidence
- [x] `MC-013-T03` Include optimistic concurrency/version preconditions so concurrent administrators cannot silently overwrite newer policy state.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-013-T04` Require change reason, actor, approver where applicable, desired activation time, expiry for temporary grants, and idempotency key on mutations.  
  **→ PARTIAL** — reason + if_revision + request_id on mutations; approver/activation time/expiry for grants not implemented
- [ ] `MC-013-T05` Define bulk import/export semantics with transactional validation and per-entry error reporting without partial unsafe application.  
  **→ PARTIAL** — bulk import/export only via whole config generations (transactional), no per-entry RBAC bulk API
- [ ] `MC-013-T06` Define pagination/filtering and high-cardinality protections for query surfaces.  
  **→ PARTIAL** — list filtered by scope; no pagination on RBAC list
- [x] `MC-013-T07` Define stable error codes for conflict, stale version, forbidden scope, invalid principal, invalid capability, dependency unavailable, and policy lock/freeze.  
  **→ DONE** — implemented; see evidence
- [x] `MC-013-T08` Add contract tests and authorization-negative tests for every operation and scope boundary.  
  **→ DONE** — implemented; see evidence
- [x] `MC-013-T09` Document the trust boundary, caller/callee identity, required capability, data classification, and network exposure for the interface.  
  **→ DONE** — docs/INTERFACES.md, docs/TOPOLOGY.md
- [x] `MC-013-T10` Define deterministic timeouts, limits, backpressure, idempotency/retry rules, and dependency-failure behavior.  
  **→ DONE** — docs/INTERFACES.md, production/resilience.py
- [x] `MC-013-T11` Propagate request/correlation/trace identifiers and record the protocol/schema version used for each security-sensitive operation.  
  **→ DONE** — request_id/decision_id/traceparent + protocol recorded per decision
- [ ] `MC-013-T12` Instrument rate/error/latency/saturation metrics and structured error/log events for the interface without leaking secrets.  
  **→ PARTIAL** — service-level metrics; no per-interface stage metrics
- [x] `MC-013-T13` Provide schema-driven contract tests, malicious/negative fixtures, compatibility tests, and protocol-faithful integration tests.  
  **→ DONE** — tests/contract + fixtures/compat

### Definition of done / acceptance evidence

- [x] `MC-013-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-013-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-013-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-013-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-013-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-013-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-013-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-014 — Versioned audit event schema/API

> **4.3.0 disposition: PARTIAL** · DONE 14 · OPEN_HUMAN 1 · PARTIAL 5  
> Evidence: `schemas/PK_ECP_AUDIT_RECORD_2.schema.json` (exists), `schemas/PK_ECP_AUDIT_QUERY_1.schema.json` (exists), `production/journal.py` (exists), `production/siem.py` (exists), `tests.unit.test_additions` (7/7 pass), `tests.unit.test_units.JournalTest.test_query_export` (1/1 pass)

**Severity:** Critical  
**Related controls:** C021-C022, C027  
**Gap statement:** `PK_ECP_AUDIT/1` has no formal external event schema, query/export API, or compatibility rules.  
**Key cross-component dependencies:** MC-035, MC-038, MC-070  

### Implementation checklist

- [x] `MC-014-T01` Define `PK_ECP_AUDIT/1` as a versioned immutable event schema with event ID, sequence/order scope, timestamp, actor, subject, tenant/lattice/workload IDs, action, outcome, reason, request/trace IDs, policy/config/provenance digests, and integrity metadata.  
  **→ DONE** — implemented; see evidence
- [x] `MC-014-T02` Define query API semantics for time range, actor, tenant, lattice, workload, action, decision, event ID, and correlation/request ID filters.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-014-T03` Define pagination/cursor stability, maximum query windows, rate limits, and authorization controls for audit reads.  
  **→ PARTIAL** — cursor pagination + authorization; no rate limits on audit reads
- [x] `MC-014-T04` Define immutable export format and cryptographic verification metadata so exported batches can be independently validated.  
  **→ DONE** — implemented; see evidence
- [x] `MC-014-T05` Define retention class, legal hold, archival tier, deletion restrictions, and privacy/redaction behavior without mutating integrity-critical source records.  
  **→ DONE** — implemented; see evidence
- [x] `MC-014-T06` Define schema evolution rules that preserve old event readability and chain/hash verification.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-014-T07` Create SIEM/export adapter contract with delivery acknowledgements, retry/idempotency, backpressure, and dead-letter semantics.  
  **→ PARTIAL** — SIEM exporter contract + checkpoint + dead-letter implemented; no real SIEM integrated
- [x] `MC-014-T08` Add conformance fixtures proving events emitted by every security-sensitive operation validate against the schema.  
  **→ DONE** — implemented; see evidence
- [x] `MC-014-T09` Document the trust boundary, caller/callee identity, required capability, data classification, and network exposure for the interface.  
  **→ DONE** — docs/INTERFACES.md, docs/TOPOLOGY.md
- [x] `MC-014-T10` Define deterministic timeouts, limits, backpressure, idempotency/retry rules, and dependency-failure behavior.  
  **→ DONE** — docs/INTERFACES.md, production/resilience.py
- [x] `MC-014-T11` Propagate request/correlation/trace identifiers and record the protocol/schema version used for each security-sensitive operation.  
  **→ DONE** — request_id/decision_id/traceparent + protocol recorded per decision
- [ ] `MC-014-T12` Instrument rate/error/latency/saturation metrics and structured error/log events for the interface without leaking secrets.  
  **→ PARTIAL** — service-level metrics; no per-interface stage metrics
- [x] `MC-014-T13` Provide schema-driven contract tests, malicious/negative fixtures, compatibility tests, and protocol-faithful integration tests.  
  **→ DONE** — tests/contract + fixtures/compat

### Definition of done / acceptance evidence

- [x] `MC-014-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-014-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-014-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-014-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-014-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-014-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-014-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-015 — Authentication boundary

> **4.3.0 disposition: PARTIAL** · DONE 13 · OPEN_HUMAN 1 · PARTIAL 6  
> Evidence: `production/identity.py` (exists), `production/http_api.py` (exists), `tests.unit.test_units.IdentityTest` (5/5 pass), `tests.integration.test_e2e.MutualTlsTest` (1/1 pass), `tests.security.test_threats.ThreatTest.test_T04_spoofed_identity_and_forged_token` (1/1 pass)

**Severity:** Critical  
**Related controls:** C023, C044  
**Gap statement:** `admit()` accepts a caller-supplied user string; there is no authenticated principal, token validation, mTLS, workload identity, or peer attestation.  
**Key cross-component dependencies:** MC-031, MC-034  

### Implementation checklist

- [x] `MC-015-T01` Place authentication at the transport/service boundary so `ControlPlane.admit()` never treats a caller-supplied username as proof of identity.  
  **→ DONE** — implemented; see evidence
- [x] `MC-015-T02` Select supported mechanisms such as OIDC/JWT for humans/services, SPIFFE/SPIRE or mTLS workload identities for service-to-service calls, and document where each is valid.  
  **→ DONE** — implemented; see evidence
- [x] `MC-015-T03` Validate issuer, audience, signature algorithm, key ID, expiry/not-before, nonce/replay characteristics, and tenant/organisation claims before authorization.  
  **→ DONE** — implemented; see evidence
- [x] `MC-015-T04` Bind the authenticated principal to request context and prohibit overriding it through manifest or API fields.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-015-T05` Define certificate/token revocation, signing-key rotation, JWKS caching/refresh, clock-skew limits, and fail-closed behavior when trust material is unavailable.  
  **→ PARTIAL** — JWKS rotation via config generation; no live JWKS refresh, token revocation list or CRL/OCSP
- [ ] `MC-015-T06` Map external identities into stable internal principal IDs so email/display-name changes do not rewrite authorization history.  
  **→ PARTIAL** — RBAC keys on subject; issuer is not part of the binding key (multi-issuer collision possible)
- [x] `MC-015-T07` Emit authentication outcome and principal metadata into the audit trail without logging credentials/tokens.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-015-T08` Add negative tests for forged tokens, wrong audience, expired/not-yet-valid tokens, revoked credentials, unknown CA, tenant mismatch, and credential replay.  
  **→ PARTIAL** — revoked-credential and unknown-CA covered for mTLS; token revocation absent
- [x] `MC-015-T09` Document the trust boundary, caller/callee identity, required capability, data classification, and network exposure for the interface.  
  **→ DONE** — docs/INTERFACES.md, docs/TOPOLOGY.md
- [x] `MC-015-T10` Define deterministic timeouts, limits, backpressure, idempotency/retry rules, and dependency-failure behavior.  
  **→ DONE** — docs/INTERFACES.md, production/resilience.py
- [x] `MC-015-T11` Propagate request/correlation/trace identifiers and record the protocol/schema version used for each security-sensitive operation.  
  **→ DONE** — request_id/decision_id/traceparent + protocol recorded per decision
- [ ] `MC-015-T12` Instrument rate/error/latency/saturation metrics and structured error/log events for the interface without leaking secrets.  
  **→ PARTIAL** — service-level metrics; no per-interface stage metrics
- [x] `MC-015-T13` Provide schema-driven contract tests, malicious/negative fixtures, compatibility tests, and protocol-faithful integration tests.  
  **→ DONE** — tests/contract + fixtures/compat

### Definition of done / acceptance evidence

- [x] `MC-015-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-015-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-015-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-015-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-015-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-015-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-015-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-016 — Structured machine-readable error model

> **4.3.0 disposition: PARTIAL** · DONE 16 · OPEN_HUMAN 1 · PARTIAL 3  
> Evidence: `production/errors.py` (exists), `schemas/PK_ECP_ERROR_1.schema.json` (exists), `schemas/ERROR_REGISTRY.pin` (exists), `tests.unit.test_units.ErrorRegistryTest` (3/3 pass), `tests.contract.test_contracts.SchemaContractTest.test_every_denial_reason_uses_registered_code` (1/1 pass)

**Severity:** High  
**Related controls:** C026  
**Gap statement:** Denials are free-form strings rather than stable error codes with typed details and remediation metadata.  

### Implementation checklist

- [x] `MC-016-T01` Define a stable namespaced error-code taxonomy separating validation, authentication, authorization, policy, provenance, quota, conflict, dependency, timeout, overload, and internal failures.  
  **→ DONE** — implemented; see evidence
- [x] `MC-016-T02` For each code, define HTTP/gRPC/WIT mapping, retryability, operator severity, client remediation guidance, and whether details are safe to expose.  
  **→ DONE** — implemented; see evidence
- [x] `MC-016-T03` Return machine-readable field violations with JSON pointer/path and bounded sanitized context for invalid requests.  
  **→ DONE** — implemented; see evidence
- [x] `MC-016-T04` Include correlation/request ID and dependency identifier where relevant, but never secret values, raw tokens, or cross-tenant data.  
  **→ DONE** — implemented; see evidence
- [x] `MC-016-T05` Keep human-readable messages non-normative; clients must branch on stable codes, not string matching.  
  **→ DONE** — implemented; see evidence
- [x] `MC-016-T06` Define nested causes only when they preserve abstraction boundaries and cannot leak infrastructure internals.  
  **→ DONE** — implemented; see evidence
- [x] `MC-016-T07` Version the error catalog and test backward compatibility for existing codes.  
  **→ DONE** — implemented; see evidence
- [x] `MC-016-T08` Add tests asserting every documented failure path maps to an approved code and unknown exceptions become a safe generic internal error.  
  **→ DONE** — implemented; see evidence
- [x] `MC-016-T09` Document the trust boundary, caller/callee identity, required capability, data classification, and network exposure for the interface.  
  **→ DONE** — docs/INTERFACES.md, docs/TOPOLOGY.md
- [x] `MC-016-T10` Define deterministic timeouts, limits, backpressure, idempotency/retry rules, and dependency-failure behavior.  
  **→ DONE** — docs/INTERFACES.md, production/resilience.py
- [x] `MC-016-T11` Propagate request/correlation/trace identifiers and record the protocol/schema version used for each security-sensitive operation.  
  **→ DONE** — request_id/decision_id/traceparent + protocol recorded per decision
- [ ] `MC-016-T12` Instrument rate/error/latency/saturation metrics and structured error/log events for the interface without leaking secrets.  
  **→ PARTIAL** — service-level metrics; no per-interface stage metrics
- [x] `MC-016-T13` Provide schema-driven contract tests, malicious/negative fixtures, compatibility tests, and protocol-faithful integration tests.  
  **→ DONE** — tests/contract + fixtures/compat

### Definition of done / acceptance evidence

- [x] `MC-016-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-016-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-016-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-016-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-016-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-016-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-016-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-017 — Idempotency, timeout, cancellation, retry, and backpressure contract

> **4.3.0 disposition: PARTIAL** · DONE 15 · OPEN_HUMAN 1 · PARTIAL 4  
> Evidence: `production/resilience.py` (exists), `docs/INTERFACES.md` (exists), `tests.unit.test_units.SmallPartsTest` (10/10 pass), `tests.fault.test_faults.FaultTest.test_deadline_exceeded_records_nothing` (1/1 pass), `tests.concurrency.test_races.RaceTest.test_idempotent_duplicates_under_race_yield_one_decision` (1/1 pass)

**Severity:** High  
**Related controls:** C025, C028, C053-C054  
**Gap statement:** No request IDs/idempotency keys, deadlines, retry classifications, queue semantics, or overload protocol are defined.  

### Implementation checklist

- [x] `MC-017-T01` Define globally unique request IDs and client-supplied idempotency keys, including scope, retention window, collision behavior, and replayed-response semantics.  
  **→ DONE** — implemented; see evidence
- [x] `MC-017-T02` Define end-to-end deadlines and per-dependency timeout budgets derived from the p99 admission SLO rather than independent arbitrary defaults.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-017-T03` Propagate cancellation to in-flight dependency calls when safe, while preserving already-committed authoritative audit/state writes.  
  **→ PARTIAL** — cancellation checked between stages; in-flight HTTP calls bounded by timeout, not actively cancelled
- [x] `MC-017-T04` Classify each remote operation as safely retryable, conditionally retryable with idempotency, or non-retryable; encode this in client middleware.  
  **→ DONE** — implemented; see evidence
- [x] `MC-017-T05` Define bounded exponential backoff with jitter, maximum attempts/time, and retry budgets to prevent synchronized retry storms.  
  **→ DONE** — implemented; see evidence
- [x] `MC-017-T06` Define admission queues/backpressure, maximum queue depth, maximum age, overload rejection code, and client retry-after behavior.  
  **→ DONE** — implemented; see evidence
- [x] `MC-017-T07` Define duplicate forwarding prevention for INV-63 using idempotency keys/outbox semantics.  
  **→ DONE** — implemented; see evidence
- [x] `MC-017-T08` Create timeout/cancellation/retry tests that inject lost responses, partial commits, duplicate requests, slow dependencies, and overload.  
  **→ DONE** — implemented; see evidence
- [x] `MC-017-T09` Document the trust boundary, caller/callee identity, required capability, data classification, and network exposure for the interface.  
  **→ DONE** — docs/INTERFACES.md, docs/TOPOLOGY.md
- [x] `MC-017-T10` Define deterministic timeouts, limits, backpressure, idempotency/retry rules, and dependency-failure behavior.  
  **→ DONE** — docs/INTERFACES.md, production/resilience.py
- [x] `MC-017-T11` Propagate request/correlation/trace identifiers and record the protocol/schema version used for each security-sensitive operation.  
  **→ DONE** — request_id/decision_id/traceparent + protocol recorded per decision
- [ ] `MC-017-T12` Instrument rate/error/latency/saturation metrics and structured error/log events for the interface without leaking secrets.  
  **→ PARTIAL** — service-level metrics; no per-interface stage metrics
- [x] `MC-017-T13` Provide schema-driven contract tests, malicious/negative fixtures, compatibility tests, and protocol-faithful integration tests.  
  **→ DONE** — tests/contract + fixtures/compat

### Definition of done / acceptance evidence

- [x] `MC-017-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-017-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-017-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-017-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-017-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-017-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-017-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-018 — Protocol/version compatibility matrix and negotiation

> **4.3.0 disposition: PARTIAL** · DONE 12 · OPEN_HUMAN 1 · PARTIAL 7  
> Evidence: `release/compatibility.json` (exists), `fixtures/compat` (exists), `tests.contract.test_contracts.CompatibilityTest` (3/3 pass)

**Severity:** High  
**Related controls:** C016, C027, C093  
**Gap statement:** No supported-version matrix or cross-version test fixtures are bundled.  

### Implementation checklist

- [ ] `MC-018-T01` Maintain a version matrix for admission, RBAC, audit, INV-64 manifest schema, GAP-13 policy API, GAP-07 provenance API, INV-63 deployment API, and supported wasmCloud/Cosmonic releases.  
  **→ PARTIAL** — INV-64/GAP-07/wasmCloud/Cosmonic versions not pinned (no integration)
- [x] `MC-018-T02` Define minimum/maximum compatible versions and semantic differences that require translation adapters or block interoperability.  
  **→ DONE** — implemented; see evidence
- [x] `MC-018-T03` Implement explicit protocol negotiation or capability discovery where multiple wire versions may coexist.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-018-T04` Define rolling-upgrade rules so N and N-1 replicas do not make divergent security decisions on the same request.  
  **→ PARTIAL** — N/N-1 replica rule documented (schema additive-minor); no mixed-version replica test
- [x] `MC-018-T05` Keep golden cross-version fixtures proving old clients can read required fields and new clients handle old responses safely.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-018-T06` Define deprecation lifecycle, telemetry for old-version use, warning period, and hard removal policy.  
  **→ PARTIAL** — deprecation policy written; no telemetry of old-version use
- [x] `MC-018-T07` Pin dependency versions in release metadata and expose active protocol capabilities through the version/status endpoint.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-018-T08` Run the compatibility matrix in CI and block release when a claimed supported combination fails.  
  **→ PARTIAL** — matrix defined in CI workflow; only py3.11/linux/x86_64 executed
- [x] `MC-018-T09` Document the trust boundary, caller/callee identity, required capability, data classification, and network exposure for the interface.  
  **→ DONE** — docs/INTERFACES.md, docs/TOPOLOGY.md
- [x] `MC-018-T10` Define deterministic timeouts, limits, backpressure, idempotency/retry rules, and dependency-failure behavior.  
  **→ DONE** — docs/INTERFACES.md, production/resilience.py
- [x] `MC-018-T11` Propagate request/correlation/trace identifiers and record the protocol/schema version used for each security-sensitive operation.  
  **→ DONE** — request_id/decision_id/traceparent + protocol recorded per decision
- [ ] `MC-018-T12` Instrument rate/error/latency/saturation metrics and structured error/log events for the interface without leaking secrets.  
  **→ PARTIAL** — service-level metrics; no per-interface stage metrics
- [x] `MC-018-T13` Provide schema-driven contract tests, malicious/negative fixtures, compatibility tests, and protocol-faithful integration tests.  
  **→ DONE** — tests/contract + fixtures/compat

### Definition of done / acceptance evidence

- [x] `MC-018-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-018-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-018-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-018-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-018-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-018-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-018-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-019 — Real deployment-manager adapter

> **4.3.0 disposition: PARTIAL** · DONE 13 · OPEN_HUMAN 1 · PARTIAL 6  
> Evidence: `production/adapters.py` (exists), `tests.integration.test_e2e.EndToEndTest.test_deployment_manager_outage_and_refusal` (1/1 pass), `tests.fault.test_faults.FaultTest.test_deployment_outage_then_reconnect_delivers_once` (1/1 pass)

**Severity:** Critical  
**Related controls:** C003, C021, C030, C051  
**Gap statement:** Admitted manifests are appended to an in-memory `forwarded` list; no INV-63 client, transport, acknowledgement, failure handling, or delivery semantics exist.  
**Key cross-component dependencies:** MC-017, MC-038, MC-041  

### Implementation checklist

- [x] `MC-019-T01` Implement an INV-63 client adapter behind an interface that can be replaced with test doubles without changing admission logic.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-019-T02` Define the exact INV-63 request schema, endpoint/service discovery, authentication identity, authorization capability, timeout, and TLS requirements.  
  **→ PARTIAL** — PK_ECP_DELIVER/1 defined by INV-66; not agreed with INV-63 owners; auth identity via adapter config
- [x] `MC-019-T03` Forward only after the authoritative admitted decision/audit record is durably committed; use transactional outbox or equivalent to avoid lost/duplicate delivery.  
  **→ DONE** — implemented; see evidence
- [x] `MC-019-T04` Attach admission event ID, manifest digest, tenant/lattice, desired-state revision, and idempotency key to downstream requests.  
  **→ DONE** — implemented; see evidence
- [x] `MC-019-T05` Define acknowledgement semantics distinguishing accepted-for-reconciliation from deployed/healthy.  
  **→ DONE** — implemented; see evidence
- [x] `MC-019-T06` Handle timeout, rejection, conflict, unavailable, and duplicate acknowledgements with typed state transitions and bounded retries.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-019-T07` Persist forwarding state and last error so restart/replay resumes safely without re-authorizing a different artifact under the same request.  
  **→ PARTIAL** — pending state persisted; last retryable error is not journaled
- [ ] `MC-019-T08` Add integration tests against a real or protocol-faithful INV-63 test service covering success, duplicate delivery, lost ack, rejection, restart, and backpressure.  
  **→ PARTIAL** — protocol-faithful stub, not real INV-63
- [x] `MC-019-T09` Document the trust boundary, caller/callee identity, required capability, data classification, and network exposure for the interface.  
  **→ DONE** — docs/INTERFACES.md, docs/TOPOLOGY.md
- [x] `MC-019-T10` Define deterministic timeouts, limits, backpressure, idempotency/retry rules, and dependency-failure behavior.  
  **→ DONE** — docs/INTERFACES.md, production/resilience.py
- [x] `MC-019-T11` Propagate request/correlation/trace identifiers and record the protocol/schema version used for each security-sensitive operation.  
  **→ DONE** — request_id/decision_id/traceparent + protocol recorded per decision
- [ ] `MC-019-T12` Instrument rate/error/latency/saturation metrics and structured error/log events for the interface without leaking secrets.  
  **→ PARTIAL** — service-level metrics; no per-interface stage metrics
- [x] `MC-019-T13` Provide schema-driven contract tests, malicious/negative fixtures, compatibility tests, and protocol-faithful integration tests.  
  **→ DONE** — tests/contract + fixtures/compat

### Definition of done / acceptance evidence

- [x] `MC-019-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-019-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-019-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-019-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-019-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-019-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-019-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-020 — Adjacent-layer integration adapters/tests

> **4.3.0 disposition: PARTIAL_EXTERNAL_DEPENDENCY** · DONE 12 · OPEN_EXTERNAL 1 · OPEN_HUMAN 1 · PARTIAL 6  
> Evidence: `production/adapters.py` (exists), `production/policy_engine.py` (exists), `docs/INTERFACES.md` (exists), `tests.integration.test_e2e` (6/6 pass)

**Severity:** Critical  
**Related controls:** C003, C030, C083  
**Gap statement:** No executable integrations exist for INV-64 application model, GAP-13 policy engine, GAP-07 provenance/signing, identity, registry, or audit storage.  
**Key cross-component dependencies:** MC-012, MC-015, MC-033, MC-019  

### Implementation checklist

- [x] `MC-020-T01` Create an adapter boundary and executable integration contract for INV-64 application model, GAP-13 policy engine, GAP-07 provenance/signing, identity provider, registry metadata, audit storage, and INV-63 deployment manager.  
  **→ DONE** — implemented; see evidence
- [x] `MC-020-T02` For each adapter, document ownership, request/response schema, authentication identity, timeout, retryability, consistency requirement, and failure classification.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-020-T03` Use protocol-faithful test containers/fakes generated from the actual schema; avoid mocks that bypass serialization, authentication, or error behavior.  
  **→ PARTIAL** — stubs serialise over real HTTP; IdP/registry/INV-64/GAP-07 adapters are interfaces only
- [x] `MC-020-T04` Create a dependency matrix that states which adapters are required for admission and which can degrade without violating security or durability.  
  **→ DONE** — implemented; see evidence
- [x] `MC-020-T05` Propagate request/trace/correlation IDs and stable tenant/workload identifiers across every adapter call.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-020-T06` Pin supported adapter/protocol versions and prove version-skew behavior.  
  **→ OPEN_EXTERNAL** — no peer versions to pin
- [ ] `MC-020-T07` Create end-to-end negative tests where each dependency returns malformed, stale, unauthorized, unavailable, slow, or conflicting data.  
  **→ PARTIAL** — malformed/unavailable/version-mismatch covered for policy, unavailable/refusal for delivery; not every dependency x mode
- [ ] `MC-020-T08` Add CI jobs that execute all supported adjacent-layer integrations and retain machine-readable evidence.  
  **→ PARTIAL** — CI runs stub integrations only
- [x] `MC-020-T09` Document the trust boundary, caller/callee identity, required capability, data classification, and network exposure for the interface.  
  **→ DONE** — docs/INTERFACES.md, docs/TOPOLOGY.md
- [x] `MC-020-T10` Define deterministic timeouts, limits, backpressure, idempotency/retry rules, and dependency-failure behavior.  
  **→ DONE** — docs/INTERFACES.md, production/resilience.py
- [x] `MC-020-T11` Propagate request/correlation/trace identifiers and record the protocol/schema version used for each security-sensitive operation.  
  **→ DONE** — request_id/decision_id/traceparent + protocol recorded per decision
- [ ] `MC-020-T12` Instrument rate/error/latency/saturation metrics and structured error/log events for the interface without leaking secrets.  
  **→ PARTIAL** — service-level metrics; no per-interface stage metrics
- [x] `MC-020-T13` Provide schema-driven contract tests, malicious/negative fixtures, compatibility tests, and protocol-faithful integration tests.  
  **→ DONE** — tests/contract + fixtures/compat

### Definition of done / acceptance evidence

- [x] `MC-020-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-020-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-020-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-020-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-020-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-020-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-020-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-021 — GitOps ingestion/change-controller integration

> **4.3.0 disposition: PARTIAL_EXTERNAL_DEPENDENCY** · DONE 9 · OPEN_EXTERNAL 2 · OPEN_HUMAN 1 · PARTIAL 8  
> Evidence: `production/adapters.py` (exists), `tests.integration.test_e2e.EndToEndTest.test_gitops_ingestion_goes_through_admission` (1/1 pass)

**Severity:** High  
**Related controls:** C011-C012, C021, C030  
**Gap statement:** The source function names enterprise management/GitOps, but no Git repository watcher, desired-state ingestion interface, webhook, change-set model, or handoff to reconciliation is present.  

### Implementation checklist

- [ ] `MC-021-T01` Define a GitOps source abstraction covering repository URL/project identity, branch/ref, path, commit SHA, change-set ID, author, signature status, and desired-state revision.  
  **→ PARTIAL** — repo/rev/path/idempotency recorded; branch, author, signature status, change-set id not modelled
- [ ] `MC-021-T02` Support secure ingestion by webhook and/or polling with authenticated source events, replay protection, and deduplication by immutable commit/change ID.  
  **→ OPEN_EXTERNAL** — webhook ingestion + authenticated source events not built (waiver W-08)
- [ ] `MC-021-T03` Parse desired state using the authoritative INV-64 application model and reject ambiguous or mutable artifact references before admission.  
  **→ OPEN_EXTERNAL** — INV-64 application model schema not available
- [x] `MC-021-T04` Define reconciliation handoff semantics: detect new revision, validate, authorize, admit, persist, forward, and record terminal/retry state.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-021-T05` Handle force-push, branch deletion, superseded commits, revert commits, and concurrent changes deterministically.  
  **→ PARTIAL** — force-push/branch deletion/revert handled only as new revisions; no explicit detection
- [ ] `MC-021-T06` Define repository credentials as secret references and enforce least-privilege read-only access where write-back is not required.  
  **→ PARTIAL** — repo credentials would be secret refs; ingestor reads a local checkout
- [ ] `MC-021-T07` Expose GitOps status linked to commit SHA, admission event, deployment-manager acknowledgement, and live inventory.  
  **→ PARTIAL** — commit SHA linked via decision source + explain; no GitOps status endpoint
- [ ] `MC-021-T08` Add integration tests using an actual Git server fixture for webhook replay, invalid signature, rapid successive commits, rollback commit, and repository outage.  
  **→ PARTIAL** — local git fixture; no git server/webhook replay tests
- [x] `MC-021-T09` Document the trust boundary, caller/callee identity, required capability, data classification, and network exposure for the interface.  
  **→ DONE** — docs/INTERFACES.md, docs/TOPOLOGY.md
- [x] `MC-021-T10` Define deterministic timeouts, limits, backpressure, idempotency/retry rules, and dependency-failure behavior.  
  **→ DONE** — docs/INTERFACES.md, production/resilience.py
- [x] `MC-021-T11` Propagate request/correlation/trace identifiers and record the protocol/schema version used for each security-sensitive operation.  
  **→ DONE** — request_id/decision_id/traceparent + protocol recorded per decision
- [ ] `MC-021-T12` Instrument rate/error/latency/saturation metrics and structured error/log events for the interface without leaking secrets.  
  **→ PARTIAL** — service-level metrics; no per-interface stage metrics
- [x] `MC-021-T13` Provide schema-driven contract tests, malicious/negative fixtures, compatibility tests, and protocol-faithful integration tests.  
  **→ DONE** — tests/contract + fixtures/compat

### Definition of done / acceptance evidence

- [x] `MC-021-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-021-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-021-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-021-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-021-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-021-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-021-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

# Configuration and state management

## MC-022 — Declarative production configuration schema/loader

> **4.3.0 disposition: PARTIAL** · DONE 16 · OPEN_HUMAN 1 · PARTIAL 3  
> Evidence: `schemas/PK_ECP_CONFIG_1.schema.json` (exists), `production/config.py` (exists), `config/examples` (exists), `tests.unit.test_units.ConfigTest` (5/5 pass)

**Severity:** Critical  
**Related controls:** C033-C035  
**Gap statement:** RBAC, registries, signers, limits, endpoints, and environment/site overrides are constructor arguments only.  
**Key cross-component dependencies:** MC-023, MC-024, MC-027  

### Implementation checklist

- [x] `MC-022-T01` Define a versioned declarative configuration schema for identity, policy, RBAC store, registry/signer policy, audit store, deployment adapter, limits, telemetry, retention, and site/environment metadata.  
  **→ DONE** — implemented; see evidence
- [x] `MC-022-T02` Separate non-secret configuration values from secret references; prohibit inline credentials in normal config.  
  **→ DONE** — implemented; see evidence
- [x] `MC-022-T03` Provide secure defaults with deny-by-default trust policy, bounded limits, disabled unauthenticated admin surfaces, and explicit production-mode requirements.  
  **→ DONE** — implemented; see evidence
- [x] `MC-022-T04` Implement layered configuration precedence (built-in defaults < environment/site profile < deployment override) with deterministic merge semantics.  
  **→ DONE** — implemented; see evidence
- [x] `MC-022-T05` Validate syntax, types, ranges, endpoint schemes, duplicate/conflicting policy, and cross-field constraints before activation.  
  **→ DONE** — implemented; see evidence
- [x] `MC-022-T06` Reject unknown security-critical fields to prevent typo-driven bypass; define controlled forward-compatibility behavior for noncritical extensions.  
  **→ DONE** — implemented; see evidence
- [x] `MC-022-T07` Expose the active configuration version/digest while redacting secret references/values.  
  **→ DONE** — implemented; see evidence
- [x] `MC-022-T08` Add schema tests, invalid-fixture tests, and golden configuration examples for development, single-site production, and multi-site production.  
  **→ DONE** — implemented; see evidence
- [x] `MC-022-T09` Define ownership and durability class for the data, including source of truth, consistency, retention, backup, restore, and migration requirements.  
  **→ DONE** — docs/TOPOLOGY.md#persisted-datasets
- [x] `MC-022-T10` Validate all inputs before activation/commit and fail closed on security-critical ambiguity or partial state.  
  **→ DONE** — schema + semantic validation before stage/activate/admit
- [x] `MC-022-T11` Record immutable revision/digest/provenance metadata and link active versions to admission/audit decisions.  
  **→ DONE** — config generation digest recorded on every decision
- [x] `MC-022-T12` Provide concurrency control, atomicity, rollback/recovery, and startup-integrity checks appropriate to the state.  
  **→ DONE** — CAS activation, flock journal, startup integrity check
- [ ] `MC-022-T13` Instrument state age/version, update failures, lag/backlog, storage saturation, and rollback/recovery events.  
  **→ PARTIAL** — generation + journal head exposed; no state-age/lag metrics

### Definition of done / acceptance evidence

- [x] `MC-022-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-022-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-022-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-022-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-022-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-022-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-022-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-023 — Configuration provenance and version history

> **4.3.0 disposition: PARTIAL** · DONE 13 · OPEN_HUMAN 2 · PARTIAL 5  
> Evidence: `production/config.py` (exists), `tests.unit.test_units.ConfigTest.test_dual_authorization_and_author_exclusion` (1/1 pass)

**Severity:** High  
**Related controls:** C036  
**Gap statement:** No author, source revision, approval, activation timestamp, or configuration digest is retained.  

### Implementation checklist

- [x] `MC-023-T01` Assign every configuration revision a unique immutable ID and cryptographic digest over canonicalized non-secret content plus secret-reference identifiers.  
  **→ DONE** — implemented; see evidence
- [x] `MC-023-T02` Record author/principal, source system/repository, source revision/commit, approver(s), creation time, activation time, previous version, and reason/ticket.  
  **→ DONE** — implemented; see evidence
- [x] `MC-023-T03` Persist provenance independently of process memory and link every admission decision to the exact active configuration revision.  
  **→ DONE** — implemented; see evidence
- [x] `MC-023-T04` Retain historical versions for the defined audit/rollback period and prevent silent mutation of previously activated revisions.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-023-T05` Capture automated transformations/default injection so the stored effective configuration can be reproduced exactly.  
  **→ PARTIAL** — defaults are code-versioned and merged at build_policy; effective config not stored separately
- [ ] `MC-023-T06` Expose provenance through administrative query APIs with scope-aware authorization.  
  **→ PARTIAL** — config.history() available in-process; no authorised HTTP query endpoint
- [x] `MC-023-T07` Add integrity checks that detect configuration history gaps or digest mismatch.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-023-T08` Produce release evidence showing the production config revision used for each environment/site.  
  **→ OPEN_HUMAN** — per-environment production config revisions do not exist yet
- [x] `MC-023-T09` Define ownership and durability class for the data, including source of truth, consistency, retention, backup, restore, and migration requirements.  
  **→ DONE** — docs/TOPOLOGY.md#persisted-datasets
- [x] `MC-023-T10` Validate all inputs before activation/commit and fail closed on security-critical ambiguity or partial state.  
  **→ DONE** — schema + semantic validation before stage/activate/admit
- [x] `MC-023-T11` Record immutable revision/digest/provenance metadata and link active versions to admission/audit decisions.  
  **→ DONE** — config generation digest recorded on every decision
- [x] `MC-023-T12` Provide concurrency control, atomicity, rollback/recovery, and startup-integrity checks appropriate to the state.  
  **→ DONE** — CAS activation, flock journal, startup integrity check
- [ ] `MC-023-T13` Instrument state age/version, update failures, lag/backlog, storage saturation, and rollback/recovery events.  
  **→ PARTIAL** — generation + journal head exposed; no state-age/lag metrics

### Definition of done / acceptance evidence

- [x] `MC-023-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-023-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-023-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-023-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-023-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-023-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-023-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-024 — Atomic configuration activation and rollback

> **4.3.0 disposition: PARTIAL** · DONE 13 · OPEN_HUMAN 1 · PARTIAL 6  
> Evidence: `production/config.py` (exists), `tests.unit.test_units.ConfigTest` (5/5 pass), `tests.concurrency.test_races.RaceTest.test_config_activation_race_one_winner` (1/1 pass)

**Severity:** Critical  
**Related controls:** C037-C038  
**Gap statement:** No transaction/staging mechanism protects partial policy updates or enables safe rollback.  

### Implementation checklist

- [x] `MC-024-T01` Implement staged configuration lifecycle: draft -> validated -> approved -> staged -> activated, with atomic switch of the active revision.  
  **→ DONE** — implemented; see evidence
- [x] `MC-024-T02` Validate the complete candidate configuration and all referenced policy/secret/dependency prerequisites before changing active state.  
  **→ DONE** — implemented; see evidence
- [x] `MC-024-T03` Use transactional storage/CAS so partial writes across RBAC, registry/signer policy, and service settings cannot create mixed revisions.  
  **→ DONE** — implemented; see evidence
- [x] `MC-024-T04` Give every admission operation a consistent configuration snapshot/version so an in-flight request is not evaluated against multiple revisions.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-024-T05` Define automatic rollback triggers for activation failure and operator-driven rollback to a known prior revision.  
  **→ PARTIAL** — operator rollback implemented; no automatic rollback trigger on activation failure (activation is atomic, so failure leaves previous active)
- [ ] `MC-024-T06` Prevent rollback to cryptographically/semantically incompatible or revoked configuration without explicit break-glass approval.  
  **→ PARTIAL** — rollback target is re-validated against the current schema; no semantic-compat/break-glass check for revoked trust
- [x] `MC-024-T07` Audit activation, rejection, rollback, and failed rollback with actor, reason, old/new digests, and correlation ID.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-024-T08` Create concurrency tests for simultaneous activations, stale-version writers, process crash mid-activation, and dependency failure during validation.  
  **→ PARTIAL** — concurrent activation + stale writer tested; crash mid-activation and dependency failure during validation not tested
- [x] `MC-024-T09` Define ownership and durability class for the data, including source of truth, consistency, retention, backup, restore, and migration requirements.  
  **→ DONE** — docs/TOPOLOGY.md#persisted-datasets
- [x] `MC-024-T10` Validate all inputs before activation/commit and fail closed on security-critical ambiguity or partial state.  
  **→ DONE** — schema + semantic validation before stage/activate/admit
- [x] `MC-024-T11` Record immutable revision/digest/provenance metadata and link active versions to admission/audit decisions.  
  **→ DONE** — config generation digest recorded on every decision
- [x] `MC-024-T12` Provide concurrency control, atomicity, rollback/recovery, and startup-integrity checks appropriate to the state.  
  **→ DONE** — CAS activation, flock journal, startup integrity check
- [ ] `MC-024-T13` Instrument state age/version, update failures, lag/backlog, storage saturation, and rollback/recovery events.  
  **→ PARTIAL** — generation + journal head exposed; no state-age/lag metrics

### Definition of done / acceptance evidence

- [x] `MC-024-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-024-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-024-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-024-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-024-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-024-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-024-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-025 — Persistent RBAC/policy store

> **4.3.0 disposition: PARTIAL** · DONE 13 · OPEN_HUMAN 1 · PARTIAL 6  
> Evidence: `production/config.py` (exists), `production/rbac.py` (exists), `production/journal.py` (exists), `tests.unit.test_units.RbacTest` (5/5 pass), `tests.unit.test_additions.AdditionsTest.test_rbac_optimistic_concurrency` (1/1 pass)

**Severity:** Critical  
**Related controls:** C032-C038, C098  
**Gap statement:** Roles are process-local immutable mappings with no enterprise persistence, synchronization, review, or delegated administration.  
**Key cross-component dependencies:** MC-013, MC-032, MC-038  

### Implementation checklist

- [ ] `MC-025-T01` Select a durable RBAC/policy datastore with transactional updates, high availability, backup/restore, encryption, and supported consistency semantics.  
  **→ PARTIAL** — journal-backed store; HA/backup/encryption as per journal; no separate RBAC database
- [x] `MC-025-T02` Define normalized entities for organisations, tenants, principals, groups, service principals, lattices, roles/capabilities, conditions, bindings, denials, and expirations.  
  **→ DONE** — implemented; see evidence
- [x] `MC-025-T03` Enforce referential integrity and uniqueness rules that prevent ambiguous duplicate grants or orphaned principals/scopes.  
  **→ DONE** — implemented; see evidence
- [x] `MC-025-T04` Use optimistic concurrency or serializable transactions for administrative updates; reject stale writers.  
  **→ DONE** — implemented; see evidence
- [x] `MC-025-T05` Support delegated administration bounded by scope and capability, never by arbitrary record-level write access.  
  **→ DONE** — implemented; see evidence
- [x] `MC-025-T06` Publish change events or revision tokens so all admission replicas observe a controlled, monotonic policy version.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-025-T07` Implement cache invalidation/TTL behavior that never extends revoked privilege beyond an approved maximum window.  
  **→ PARTIAL** — no caches of privilege beyond in-process projection rebuilt per generation; multi-replica propagation relies on follower refresh
- [ ] `MC-025-T08` Add migration, backup/restore, audit, and multi-replica consistency tests including revocation propagation.  
  **→ PARTIAL** — backup/restore + replay tested; migration and multi-replica revocation propagation not tested
- [x] `MC-025-T09` Define ownership and durability class for the data, including source of truth, consistency, retention, backup, restore, and migration requirements.  
  **→ DONE** — docs/TOPOLOGY.md#persisted-datasets
- [x] `MC-025-T10` Validate all inputs before activation/commit and fail closed on security-critical ambiguity or partial state.  
  **→ DONE** — schema + semantic validation before stage/activate/admit
- [x] `MC-025-T11` Record immutable revision/digest/provenance metadata and link active versions to admission/audit decisions.  
  **→ DONE** — config generation digest recorded on every decision
- [x] `MC-025-T12` Provide concurrency control, atomicity, rollback/recovery, and startup-integrity checks appropriate to the state.  
  **→ DONE** — CAS activation, flock journal, startup integrity check
- [ ] `MC-025-T13` Instrument state age/version, update failures, lag/backlog, storage saturation, and rollback/recovery events.  
  **→ PARTIAL** — generation + journal head exposed; no state-age/lag metrics

### Definition of done / acceptance evidence

- [x] `MC-025-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-025-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-025-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-025-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-025-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-025-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-025-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-026 — Approved registry/signer policy administration store

> **4.3.0 disposition: PARTIAL** · DONE 12 · OPEN_HUMAN 1 · PARTIAL 7  
> Evidence: `production/config.py` (exists), `production/provenance.py` (exists), `tests.unit.test_units.ProvenanceTest` (4/4 pass), `tests.unit.test_additions.AdditionsTest.test_policy_impact_dry_run` (1/1 pass)

**Severity:** Critical  
**Related controls:** C032-C038, C045  
**Gap statement:** Registry and signer allowlists have no persistent control plane, approval workflow, versioning, or distribution mechanism.  
**Key cross-component dependencies:** MC-029, MC-030, MC-035  

### Implementation checklist

- [ ] `MC-026-T01` Define persistent entities for approved registries, repository namespaces, signer identities/keys/certificates, allowed artifact types, environments, scopes, and validity windows.  
  **→ PARTIAL** — registries(host,scope), signers(id,key,scope,revoked,not_after); artifact types/environments/namespaces absent
- [ ] `MC-026-T02` Support policy conditions such as exact registry host, namespace/repository prefix, immutable digest requirement, signer key ID, issuer, certificate constraints, and approved version ranges.  
  **→ PARTIAL** — exact host + scope + digest + signer key id; no namespace prefixes, issuer/cert constraints or version ranges
- [x] `MC-026-T03` Require dual control or designated approval for trust-root/signer changes in production environments.  
  **→ DONE** — implemented; see evidence
- [x] `MC-026-T04` Record provenance, justification, ticket, author, approver, activation/expiry, and superseded revision for every policy change.  
  **→ DONE** — implemented; see evidence
- [x] `MC-026-T05` Implement atomic distribution/versioning so all admission replicas evaluate against a coherent registry/signer policy revision.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-026-T06` Handle signer/key rotation with overlap windows and explicit revocation semantics.  
  **→ PARTIAL** — rotation by overlapping signer ids + revoked flag; no automatic overlap-window enforcement
- [x] `MC-026-T07` Provide a dry-run impact query showing which currently deployed/inventory artifacts would violate a proposed policy.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-026-T08` Add tests for namespace confusion, registry case/canonicalization, expired signer, revoked key, overlapping rotations, and stale replica policy.  
  **→ PARTIAL** — expired/revoked/scope tested; namespace confusion, overlapping rotations, stale replica not tested
- [x] `MC-026-T09` Define ownership and durability class for the data, including source of truth, consistency, retention, backup, restore, and migration requirements.  
  **→ DONE** — docs/TOPOLOGY.md#persisted-datasets
- [x] `MC-026-T10` Validate all inputs before activation/commit and fail closed on security-critical ambiguity or partial state.  
  **→ DONE** — schema + semantic validation before stage/activate/admit
- [x] `MC-026-T11` Record immutable revision/digest/provenance metadata and link active versions to admission/audit decisions.  
  **→ DONE** — config generation digest recorded on every decision
- [x] `MC-026-T12` Provide concurrency control, atomicity, rollback/recovery, and startup-integrity checks appropriate to the state.  
  **→ DONE** — CAS activation, flock journal, startup integrity check
- [ ] `MC-026-T13` Instrument state age/version, update failures, lag/backlog, storage saturation, and rollback/recovery events.  
  **→ PARTIAL** — generation + journal head exposed; no state-age/lag metrics

### Definition of done / acceptance evidence

- [x] `MC-026-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-026-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-026-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-026-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-026-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-026-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-026-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-027 — Secret-management integration

> **4.3.0 disposition: PARTIAL_EXTERNAL_DEPENDENCY** · DONE 9 · OPEN_EXTERNAL 1 · OPEN_HUMAN 1 · PARTIAL 9  
> Evidence: `production/keys.py` (exists), `tests.unit.test_units.KeysTest.test_secret_provider` (1/1 pass)

**Severity:** Critical  
**Related controls:** C039, C047-C048  
**Gap statement:** No KMS/Vault/secret provider integration or secret-reference model exists.  

### Implementation checklist

- [ ] `MC-027-T01` Define a provider-agnostic secret-reference interface for KMS/Vault/cloud secret managers and workload identity based retrieval.  
  **→ PARTIAL** — provider interface + env/file provider; KMS/Vault providers not built (OPEN_EXTERNAL)
- [ ] `MC-027-T02` Keep secret material out of environment dumps, config files, command-line arguments, logs, traces, metrics labels, audit payloads, and crash reports.  
  **→ PARTIAL** — never logged/journaled/in errors; env-var provider means secrets exist in process environment
- [ ] `MC-027-T03` Grant each service identity only the exact secret/key paths required and separate read, unwrap/sign, and administrative capabilities.  
  **→ OPEN_EXTERNAL** — per-path secret permissions belong to the secret manager
- [ ] `MC-027-T04` Define secret caching in memory, TTL, zeroization/best-effort lifecycle, renewal, and behavior when the provider is unavailable.  
  **→ PARTIAL** — no caching/TTL/zeroisation layer beyond the provider call
- [ ] `MC-027-T05` Support rotation without service restart where practical and verify old credentials stop being accepted after the overlap window.  
  **→ PARTIAL** — TLS cert reload + keyring rotation without restart; secret-manager lease rotation absent
- [x] `MC-027-T06` Differentiate secret references from ordinary strings in configuration schemas to prevent accidental inline values.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-027-T07` Audit secret-reference configuration changes and access failures without recording secret contents.  
  **→ PARTIAL** — secret-ref changes audited via config generations; provider access failures not audited
- [ ] `MC-027-T08` Add tests using ephemeral secret-manager fixtures for rotation, revoked access, expired lease, provider outage, and log-redaction guarantees.  
  **→ PARTIAL** — local fixtures only; no ephemeral secret-manager fixture
- [x] `MC-027-T09` Define ownership and durability class for the data, including source of truth, consistency, retention, backup, restore, and migration requirements.  
  **→ DONE** — docs/TOPOLOGY.md#persisted-datasets
- [x] `MC-027-T10` Validate all inputs before activation/commit and fail closed on security-critical ambiguity or partial state.  
  **→ DONE** — schema + semantic validation before stage/activate/admit
- [x] `MC-027-T11` Record immutable revision/digest/provenance metadata and link active versions to admission/audit decisions.  
  **→ DONE** — config generation digest recorded on every decision
- [x] `MC-027-T12` Provide concurrency control, atomicity, rollback/recovery, and startup-integrity checks appropriate to the state.  
  **→ DONE** — CAS activation, flock journal, startup integrity check
- [ ] `MC-027-T13` Instrument state age/version, update failures, lag/backlog, storage saturation, and rollback/recovery events.  
  **→ PARTIAL** — generation + journal head exposed; no state-age/lag metrics

### Definition of done / acceptance evidence

- [x] `MC-027-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-027-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-027-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-027-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-027-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-027-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-027-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-028 — Deterministic bootstrap/install packaging

> **4.3.0 disposition: PARTIAL** · DONE 12 · OPEN_HUMAN 1 · PARTIAL 7  
> Evidence: `pyproject.toml` (exists), `constraints.txt` (exists), `deploy/Dockerfile` (exists), `deploy/inv66.service` (exists), `tools/check_package.py` (exists), `lane:package` (PASS), `release/sbom.cdx.json` (exists)

**Severity:** High  
**Related controls:** C031, C040  
**Gap statement:** No `pyproject.toml`, dependency lock/constraints, install command, container image, service unit, or reproducible bootstrap artifact is bundled.  

### Implementation checklist

- [x] `MC-028-T01` Add `pyproject.toml` with explicit package metadata, supported Python versions, dependency groups, entry points, and deterministic build backend configuration.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-028-T02` Pin direct dependencies and produce a lock/constraints artifact with hashes; document controlled update workflow.  
  **→ PARTIAL** — exact pins; hashes pending (PyPI unreachable, W-09)
- [ ] `MC-028-T03` Build a minimal production container/image or equivalent service package with non-root execution, read-only filesystem where feasible, and explicit runtime user/group.  
  **→ PARTIAL** — Dockerfile written, base digest unpinned, image not built here
- [x] `MC-028-T04` Provide reproducible build commands and record compiler/interpreter/base-image versions plus artifact digests.  
  **→ DONE** — implemented; see evidence
- [x] `MC-028-T05` Add service bootstrap manifests (container orchestration/system service as applicable), health checks, resource requests/limits, and required volumes/network policies.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-028-T06` Provide environment bootstrap validation for DNS, certificates, storage, KMS, identity, policy, and deployment-manager reachability.  
  **→ PARTIAL** — validate-config + /readyz dependency view; no DNS/KMS reachability probe tool
- [ ] `MC-028-T07` Generate SBOM and provenance/attestation for the built artifact and sign the release artifact.  
  **→ PARTIAL** — SBOM + unattested provenance generated; artifact signed with ephemeral key only
- [x] `MC-028-T08` Add CI that installs from a clean environment and proves the produced package starts and passes smoke tests without undeclared local dependencies.  
  **→ DONE** — implemented; see evidence
- [x] `MC-028-T09` Define ownership and durability class for the data, including source of truth, consistency, retention, backup, restore, and migration requirements.  
  **→ DONE** — docs/TOPOLOGY.md#persisted-datasets
- [x] `MC-028-T10` Validate all inputs before activation/commit and fail closed on security-critical ambiguity or partial state.  
  **→ DONE** — schema + semantic validation before stage/activate/admit
- [x] `MC-028-T11` Record immutable revision/digest/provenance metadata and link active versions to admission/audit decisions.  
  **→ DONE** — config generation digest recorded on every decision
- [x] `MC-028-T12` Provide concurrency control, atomicity, rollback/recovery, and startup-integrity checks appropriate to the state.  
  **→ DONE** — CAS activation, flock journal, startup integrity check
- [ ] `MC-028-T13` Instrument state age/version, update failures, lag/backlog, storage saturation, and rollback/recovery events.  
  **→ PARTIAL** — generation + journal head exposed; no state-age/lag metrics

### Definition of done / acceptance evidence

- [x] `MC-028-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-028-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-028-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-028-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-028-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-028-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-028-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

# Security, trust, and isolation

## MC-029 — Cryptographic artifact signature verification

> **4.3.0 disposition: PARTIAL** · DONE 14 · OPEN_HUMAN 1 · PARTIAL 5  
> Evidence: `production/provenance.py` (exists), `tests.unit.test_units.ProvenanceTest` (4/4 pass), `tests.security.test_threats.ThreatTest.test_T05_signature_replay_onto_other_component_or_digest` (1/1 pass)

**Severity:** Critical  
**Related controls:** C045  
**Gap statement:** Admission trusts the manifest’s signer string; it does not verify an actual signature, certificate/key identity, transparency record, or signing policy.  
**Key cross-component dependencies:** MC-026, MC-030, MC-034  

### Implementation checklist

- [ ] `MC-029-T01` Define accepted signature formats and trust roots for Wasm/components/manifests, including signer identity binding, algorithm policy, key IDs, and certificate/attestation chains.  
  **→ PARTIAL** — Ed25519 over a defined payload; no certificate chains / Sigstore-style attestation
- [x] `MC-029-T02` Resolve the actual artifact bytes or immutable digest before verification; never accept a manifest `signer` string as evidence.  
  **→ DONE** — implemented; see evidence
- [x] `MC-029-T03` Verify signature cryptographically against the exact digest/content and enforce approved algorithm/key-size/curve requirements.  
  **→ DONE** — implemented; see evidence
- [x] `MC-029-T04` Validate signer authorization for tenant/environment/repository/artifact scope and validity window.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-029-T05` Check revocation/expiration and signer trust-root rotation; define offline behavior when revocation or trust services are unavailable.  
  **→ PARTIAL** — revocation via config generation; no online revocation/trust service
- [x] `MC-029-T06` Protect against signature wrapping/substitution and canonicalization ambiguity by signing/verifying a precisely defined payload format.  
  **→ DONE** — implemented; see evidence
- [x] `MC-029-T07` Record verification result, signer key/cert fingerprint, algorithm, trust policy revision, and artifact digest in the admission audit event.  
  **→ DONE** — implemented; see evidence
- [x] `MC-029-T08` Add negative tests for forged signature, altered artifact, wrong key, expired/revoked cert, unapproved signer, algorithm downgrade, and replayed metadata.  
  **→ DONE** — implemented; see evidence
- [x] `MC-029-T09` Define attacker capabilities, protected assets, trust roots, security invariants, and fail-closed conditions specific to this control.  
  **→ DONE** — docs/THREAT_MODEL.md
- [x] `MC-029-T10` Apply least privilege, explicit scopes, tenant isolation, secure defaults, and separation of duties to every administrative and runtime identity involved.  
  **→ DONE** — production/rbac.py (deny-overrides, default deny, delegation subset)
- [ ] `MC-029-T11` Use approved cryptography/key management where authenticity, integrity, or confidentiality is required; define rotation and revocation.  
  **→ PARTIAL** — Ed25519/AES-256-GCM/SHA-256 via cryptography; no managed KMS (W-05)
- [x] `MC-029-T12` Ensure security-sensitive successes and failures produce tamper-evident audit records with stable reason codes and correlation IDs.  
  **→ DONE** — admit.decision/admit.refused/config.*/rbac.*/quarantine.* journal records with codes
- [x] `MC-029-T13` Derive negative/adversarial tests directly from the threat model and retain machine-readable results as release evidence.  
  **→ DONE** — tests/security/test_threats.py T01-T17

### Definition of done / acceptance evidence

- [x] `MC-029-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-029-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-029-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-029-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-029-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-029-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-029-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-030 — Artifact digest/provenance verification

> **4.3.0 disposition: PARTIAL_EXTERNAL_DEPENDENCY** · DONE 10 · OPEN_EXTERNAL 2 · OPEN_HUMAN 1 · PARTIAL 7  
> Evidence: `production/provenance.py` (exists), `tests.unit.test_units.ProvenanceTest.test_attestation` (1/1 pass), `tests.unit.test_units.ProvenanceTest.test_registry_resolver` (1/1 pass)

**Severity:** Critical  
**Related controls:** C045  
**Gap statement:** The engine hashes the admission manifest but does not resolve/verify immutable artifact digests, SBOM/provenance attestations, approved versions, or registry metadata.  
**Key cross-component dependencies:** MC-029, MC-062  

### Implementation checklist

- [x] `MC-030-T01` Require immutable artifact references by digest for production admission or resolve tags to immutable digests before policy evaluation.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-030-T02` Fetch and validate registry metadata using authenticated registry clients with tenant-scoped credentials and TLS verification.  
  **→ OPEN_EXTERNAL** — no authenticated OCI registry client
- [ ] `MC-030-T03` Verify artifact digest against downloaded/resolved content and reject digest mismatch or mutable-reference race conditions.  
  **→ OPEN_EXTERNAL** — artifact bytes not fetched; digest pinning only
- [x] `MC-030-T04` Consume signed provenance attestations (for example SLSA/in-toto compatible) and verify builder identity, source revision, build recipe, and subject digest.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-030-T05` Associate SBOMs with artifact digest and enforce required SBOM/provenance presence, freshness, and policy constraints.  
  **→ PARTIAL** — attestation carries sbom_digest; SBOM presence/freshness policy not enforced
- [ ] `MC-030-T06` Validate approved version/channel policy without trusting unverified semantic-version labels embedded in manifests.  
  **→ PARTIAL** — no version-channel policy
- [ ] `MC-030-T07` Cache verified metadata by immutable digest with bounded lifetime and revocation invalidation.  
  **→ PARTIAL** — no verified-metadata cache (nothing fetched)
- [ ] `MC-030-T08` Add TOCTOU tests where tag contents change between resolution and forwarding, plus malformed provenance/SBOM and registry compromise simulations.  
  **→ PARTIAL** — mutable-tag refused by digest requirement; registry-compromise simulation limited to resolver index
- [x] `MC-030-T09` Define attacker capabilities, protected assets, trust roots, security invariants, and fail-closed conditions specific to this control.  
  **→ DONE** — docs/THREAT_MODEL.md
- [x] `MC-030-T10` Apply least privilege, explicit scopes, tenant isolation, secure defaults, and separation of duties to every administrative and runtime identity involved.  
  **→ DONE** — production/rbac.py (deny-overrides, default deny, delegation subset)
- [ ] `MC-030-T11` Use approved cryptography/key management where authenticity, integrity, or confidentiality is required; define rotation and revocation.  
  **→ PARTIAL** — Ed25519/AES-256-GCM/SHA-256 via cryptography; no managed KMS (W-05)
- [x] `MC-030-T12` Ensure security-sensitive successes and failures produce tamper-evident audit records with stable reason codes and correlation IDs.  
  **→ DONE** — admit.decision/admit.refused/config.*/rbac.*/quarantine.* journal records with codes
- [x] `MC-030-T13` Derive negative/adversarial tests directly from the threat model and retain machine-readable results as release evidence.  
  **→ DONE** — tests/security/test_threats.py T01-T17

### Definition of done / acceptance evidence

- [x] `MC-030-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-030-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-030-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-030-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-030-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-030-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-030-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-031 — Enterprise identity federation

> **4.3.0 disposition: PARTIAL_EXTERNAL_DEPENDENCY** · DONE 10 · OPEN_EXTERNAL 1 · OPEN_HUMAN 2 · PARTIAL 7  
> Evidence: `production/identity.py` (exists), `tests.unit.test_units.IdentityTest` (5/5 pass)

**Severity:** Critical  
**Related controls:** C023, C044  
**Gap statement:** No OIDC/SAML/workload identity integration, token audience/issuer validation, session policy, or identity lifecycle exists.  
**Key cross-component dependencies:** MC-015, MC-032  

### Implementation checklist

- [ ] `MC-031-T01` Integrate supported enterprise identity providers using OIDC/OAuth2 and/or SAML for human administrators and workload identity for services.  
  **→ OPEN_EXTERNAL** — no enterprise IdP/SAML integration; EdDSA JWT issuer + static JWKS only
- [x] `MC-031-T02` Define tenant/organisation discovery and claim mapping rules; prohibit arbitrary caller-controlled tenant selection without authorization.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-031-T03` Use stable immutable subject IDs plus issuer rather than mutable usernames/email addresses as authorization keys.  
  **→ PARTIAL** — subject used as key without issuer (see MC-015-T06)
- [ ] `MC-031-T04` Validate token audience, issuer, signature, lifetime, authentication strength/AMR where required, and group/role claims against approved mappings.  
  **→ PARTIAL** — iss/aud/sig/lifetime/groups validated; AMR/auth-strength not checked
- [ ] `MC-031-T05` Define joiner/mover/leaver lifecycle, account disable propagation, group-change latency, and emergency revocation targets.  
  **→ OPEN_HUMAN** — joiner/mover/leaver and revocation targets are an IAM owner decision
- [x] `MC-031-T06` Support service principals/non-human identities with independent credentials and lifecycle controls.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-031-T07` Define session duration, refresh, step-up authentication for privileged admin operations, and break-glass accounts with enhanced audit.  
  **→ PARTIAL** — max token lifetime enforced; no refresh/step-up/break-glass accounts
- [ ] `MC-031-T08` Add federation tests against representative identity providers and negative fixtures for tenant confusion, stale groups, token substitution, and deprovisioned users.  
  **→ PARTIAL** — local issuer only; no representative IdP tests
- [x] `MC-031-T09` Define attacker capabilities, protected assets, trust roots, security invariants, and fail-closed conditions specific to this control.  
  **→ DONE** — docs/THREAT_MODEL.md
- [x] `MC-031-T10` Apply least privilege, explicit scopes, tenant isolation, secure defaults, and separation of duties to every administrative and runtime identity involved.  
  **→ DONE** — production/rbac.py (deny-overrides, default deny, delegation subset)
- [ ] `MC-031-T11` Use approved cryptography/key management where authenticity, integrity, or confidentiality is required; define rotation and revocation.  
  **→ PARTIAL** — Ed25519/AES-256-GCM/SHA-256 via cryptography; no managed KMS (W-05)
- [x] `MC-031-T12` Ensure security-sensitive successes and failures produce tamper-evident audit records with stable reason codes and correlation IDs.  
  **→ DONE** — admit.decision/admit.refused/config.*/rbac.*/quarantine.* journal records with codes
- [x] `MC-031-T13` Derive negative/adversarial tests directly from the threat model and retain machine-readable results as release evidence.  
  **→ DONE** — tests/security/test_threats.py T01-T17

### Definition of done / acceptance evidence

- [x] `MC-031-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-031-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-031-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-031-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-031-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-031-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-031-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-032 — Organisation/tenant RBAC hierarchy and capability model

> **4.3.0 disposition: PARTIAL** · DONE 13 · OPEN_HUMAN 1 · PARTIAL 6  
> Evidence: `production/rbac.py` (exists), `tests.unit.test_units.RbacTest` (5/5 pass), `tests.security.test_threats.ThreatTest.test_T08_privilege_escalation_by_delegation` (1/1 pass)

**Severity:** Critical  
**Related controls:** C006, C024, C042, C046  
**Gap statement:** The current `(user, lattice) -> role` map does not implement organisation scope, tenant delegation, groups, service principals, capability scoping, or deny policy.  

### Implementation checklist

- [ ] `MC-032-T01` Define hierarchy and inheritance across organisation -> tenant -> environment/site -> lattice -> workload/resource scopes.  
  **→ PARTIAL** — org -> tenant -> lattice; environment/site/workload scope levels absent
- [x] `MC-032-T02` Define roles as explicit capability sets; avoid hard-coded role-name checks inside decision code where capability evaluation is required.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-032-T03` Support groups, service principals, automation identities, temporary elevation, scoped delegation, and explicit deny rules.  
  **→ PARTIAL** — groups, service/workload principals, delegation, deny implemented; temporary elevation absent
- [x] `MC-032-T04` Define inheritance/override semantics and deny precedence so grants at broader scope cannot silently bypass narrower restrictions.  
  **→ DONE** — implemented; see evidence
- [x] `MC-032-T05` Enforce least privilege and separation of duties for policy admin, deployment, audit read, security admin, and emergency control capabilities.  
  **→ DONE** — implemented; see evidence
- [x] `MC-032-T06` Define privilege-escalation prevention: administrators may not grant capabilities they do not possess unless explicitly authorized by a higher-level control.  
  **→ DONE** — implemented; see evidence
- [x] `MC-032-T07` Provide access-review queries showing effective permissions and the complete chain of grants/denials that produced them.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-032-T08` Add property tests and adversarial tests for cross-tenant access, scope confusion, wildcard abuse, group nesting, expired grants, and delegation escalation.  
  **→ PARTIAL** — cross-tenant, scope, delegation tested; no wildcards/group nesting/expiring grants exist to test
- [x] `MC-032-T09` Define attacker capabilities, protected assets, trust roots, security invariants, and fail-closed conditions specific to this control.  
  **→ DONE** — docs/THREAT_MODEL.md
- [x] `MC-032-T10` Apply least privilege, explicit scopes, tenant isolation, secure defaults, and separation of duties to every administrative and runtime identity involved.  
  **→ DONE** — production/rbac.py (deny-overrides, default deny, delegation subset)
- [ ] `MC-032-T11` Use approved cryptography/key management where authenticity, integrity, or confidentiality is required; define rotation and revocation.  
  **→ PARTIAL** — Ed25519/AES-256-GCM/SHA-256 via cryptography; no managed KMS (W-05)
- [x] `MC-032-T12` Ensure security-sensitive successes and failures produce tamper-evident audit records with stable reason codes and correlation IDs.  
  **→ DONE** — admit.decision/admit.refused/config.*/rbac.*/quarantine.* journal records with codes
- [x] `MC-032-T13` Derive negative/adversarial tests directly from the threat model and retain machine-readable results as release evidence.  
  **→ DONE** — tests/security/test_threats.py T01-T17

### Definition of done / acceptance evidence

- [x] `MC-032-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-032-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-032-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-032-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-032-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-032-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-032-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-033 — External policy-engine enforcement

> **4.3.0 disposition: PARTIAL** · DONE 12 · OPEN_HUMAN 1 · PARTIAL 7  
> Evidence: `production/policy_engine.py` (exists), `tests.fault.test_faults.FaultTest.test_policy_engine_down_fails_closed_or_uses_fresh_cache` (1/1 pass), `tests.fault.test_faults.FaultTest.test_policy_version_pin` (1/1 pass), `tests.integration.test_e2e.EndToEndTest.test_policy_engine_outage_fails_closed` (1/1 pass)

**Severity:** Critical  
**Related controls:** C019, C030, C048  
**Gap statement:** GAP-13 is declared as a dependency but is not queried, cached, version-pinned, or failure-handled.  
**Key cross-component dependencies:** MC-010, MC-011, MC-040  

### Implementation checklist

- [ ] `MC-033-T01` Define the GAP-13 policy decision contract including input document, policy bundle/version/digest, decision, obligations, reason codes, and evaluation metadata.  
  **→ PARTIAL** — input, allow, reasons, policy_version; no obligations or bundle digest
- [ ] `MC-033-T02` Pass authenticated principal, tenant/lattice/environment, artifact digest/provenance, registry/signer results, quotas, topology/site, and desired operation into policy evaluation.  
  **→ PARTIAL** — tenant/lattice/org/digest/components passed; principal, provenance results, quotas not passed
- [x] `MC-033-T03` Pin or record the exact policy revision used for every admission; do not permit untraceable “latest” policy during decision evaluation.  
  **→ DONE** — implemented; see evidence
- [x] `MC-033-T04` Define fail-closed behavior when the policy engine is unavailable for security-critical decisions and explicitly identify any safe cached-policy mode.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-033-T05` If caching compiled policy, verify bundle signature/digest, maximum age, revocation, and deterministic evaluation equivalence.  
  **→ PARTIAL** — caches answers (not compiled bundles); no bundle signature verification
- [x] `MC-033-T06` Prevent policy-engine responses from granting capabilities beyond the authenticated request context or bypassing non-overridable control-plane invariants.  
  **→ DONE** — implemented; see evidence
- [x] `MC-033-T07` Propagate policy reason/obligation data into machine-readable decisions and the operator explain view.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-033-T08` Add integration and differential tests comparing local/cached vs remote evaluation, stale policy, malformed response, engine timeout, and conflicting policy versions.  
  **→ PARTIAL** — outage/version/malformed tested; no local-vs-remote differential test
- [x] `MC-033-T09` Define attacker capabilities, protected assets, trust roots, security invariants, and fail-closed conditions specific to this control.  
  **→ DONE** — docs/THREAT_MODEL.md
- [x] `MC-033-T10` Apply least privilege, explicit scopes, tenant isolation, secure defaults, and separation of duties to every administrative and runtime identity involved.  
  **→ DONE** — production/rbac.py (deny-overrides, default deny, delegation subset)
- [ ] `MC-033-T11` Use approved cryptography/key management where authenticity, integrity, or confidentiality is required; define rotation and revocation.  
  **→ PARTIAL** — Ed25519/AES-256-GCM/SHA-256 via cryptography; no managed KMS (W-05)
- [x] `MC-033-T12` Ensure security-sensitive successes and failures produce tamper-evident audit records with stable reason codes and correlation IDs.  
  **→ DONE** — admit.decision/admit.refused/config.*/rbac.*/quarantine.* journal records with codes
- [x] `MC-033-T13` Derive negative/adversarial tests directly from the threat model and retain machine-readable results as release evidence.  
  **→ DONE** — tests/security/test_threats.py T01-T17

### Definition of done / acceptance evidence

- [x] `MC-033-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-033-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-033-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-033-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-033-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-033-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-033-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-034 — Transport/storage encryption and key rotation

> **4.3.0 disposition: PARTIAL** · DONE 10 · OPEN_HUMAN 2 · PARTIAL 8  
> Evidence: `production/keys.py` (exists), `production/http_api.py` (exists), `tests.unit.test_units.KeysTest` (2/2 pass), `tests.unit.test_units.JournalTest.test_encrypted_bodies` (1/1 pass), `tests.integration.test_e2e.MutualTlsTest` (1/1 pass)

**Severity:** Critical  
**Related controls:** C047  
**Gap statement:** There is no network transport or durable storage layer and therefore no implemented TLS/mTLS, at-rest encryption, managed keys, or rotation workflow.  

### Implementation checklist

- [x] `MC-034-T01` Require TLS 1.2+ or approved organisational baseline for every remote boundary and prefer mTLS for service-to-service control-plane traffic.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-034-T02` Define certificate identities, trust roots, SAN expectations, rotation cadence, revocation, and automated renewal for each service.  
  **→ OPEN_HUMAN** — certificate identities/rotation cadence are security-owner decisions; reload path implemented
- [ ] `MC-034-T03` Encrypt durable RBAC/config/audit/inventory data at rest using managed KMS keys and document envelope-encryption design where applicable.  
  **→ PARTIAL** — AES-256-GCM sealing of journal bodies; keys local, no managed KMS
- [ ] `MC-034-T04` Separate keys by environment/tenant/data class where risk and compliance require it; restrict decrypt/sign capabilities by workload identity.  
  **→ PARTIAL** — keyring per journal; no per-tenant/data-class key separation
- [ ] `MC-034-T05` Define key rotation and re-encryption process, overlap window, rollback, and handling of unavailable/revoked keys.  
  **→ PARTIAL** — rotate/retire implemented; no re-encryption tool (debt D-04)
- [x] `MC-034-T06` Disable insecure protocols/ciphers and enforce hostname/service-identity verification; prohibit plaintext downgrade paths.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-034-T07` Expose key/certificate expiry health signals without exposing private material.  
  **→ PARTIAL** — no certificate/key expiry health signal
- [ ] `MC-034-T08` Add automated TLS configuration tests, expired/revoked certificate tests, wrong-service identity tests, and restore tests for encrypted backups.  
  **→ PARTIAL** — mTLS wrong-CA/no-cert tested; expired/revoked cert and encrypted-backup restore not tested
- [x] `MC-034-T09` Define attacker capabilities, protected assets, trust roots, security invariants, and fail-closed conditions specific to this control.  
  **→ DONE** — docs/THREAT_MODEL.md
- [x] `MC-034-T10` Apply least privilege, explicit scopes, tenant isolation, secure defaults, and separation of duties to every administrative and runtime identity involved.  
  **→ DONE** — production/rbac.py (deny-overrides, default deny, delegation subset)
- [ ] `MC-034-T11` Use approved cryptography/key management where authenticity, integrity, or confidentiality is required; define rotation and revocation.  
  **→ PARTIAL** — Ed25519/AES-256-GCM/SHA-256 via cryptography; no managed KMS (W-05)
- [x] `MC-034-T12` Ensure security-sensitive successes and failures produce tamper-evident audit records with stable reason codes and correlation IDs.  
  **→ DONE** — admit.decision/admit.refused/config.*/rbac.*/quarantine.* journal records with codes
- [x] `MC-034-T13` Derive negative/adversarial tests directly from the threat model and retain machine-readable results as release evidence.  
  **→ DONE** — tests/security/test_threats.py T01-T17

### Definition of done / acceptance evidence

- [x] `MC-034-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-034-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-034-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-034-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-034-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-034-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-034-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-035 — Durable tamper-evident audit anchoring

> **4.3.0 disposition: PARTIAL_EXTERNAL_DEPENDENCY** · DONE 12 · OPEN_EXTERNAL 1 · OPEN_HUMAN 1 · PARTIAL 6  
> Evidence: `production/journal.py` (exists), `tests.unit.test_units.JournalTest` (10/10 pass), `tests.unit.test_additions.AdditionsTest.test_background_verification_alerts` (1/1 pass)

**Severity:** Critical  
**Related controls:** C049  
**Gap statement:** The local SHA-256 chain detects accidental mutation but an actor able to rewrite all in-memory records can recompute it; no signed/WORM/remote anchor exists.  
**Key cross-component dependencies:** MC-014, MC-038, MC-070  

### Implementation checklist

- [ ] `MC-035-T01` Persist audit records in append-only/WORM-capable storage or an append-only log whose mutation controls are stronger than the service actor.  
  **→ PARTIAL** — append-only file journal + flock; no WORM storage
- [ ] `MC-035-T02` Anchor batches/segments using signed Merkle roots/hash-chain heads to an independent trust domain or transparency/timestamp service.  
  **→ PARTIAL** — signed anchors to a separate directory; no independent timestamp/transparency service
- [ ] `MC-035-T03` Use a dedicated audit-signing key protected by KMS/HSM with restricted signing policy and no general application read access to private key material.  
  **→ OPEN_EXTERNAL** — KMS/HSM-protected audit signing key not available
- [ ] `MC-035-T04` Define segment rotation, checkpoint frequency, chain continuity across replicas/restarts, and immutable export verification.  
  **→ PARTIAL** — segments + summaries + anchors defined; anchoring cadence is operator-driven (cli anchor), not scheduled
- [x] `MC-035-T05` Ensure an administrator who can modify application data cannot rewrite both records and anchors without detection.  
  **→ DONE** — implemented; see evidence
- [x] `MC-035-T06` Implement periodic background verification and alert on chain discontinuity, anchor mismatch, missing segment, timestamp anomaly, or verification failure.  
  **→ DONE** — implemented; see evidence
- [x] `MC-035-T07` Define recovery behavior when the audit sink/anchor is unavailable; security-sensitive operations must follow documented durability rules.  
  **→ DONE** — implemented; see evidence
- [x] `MC-035-T08` Create tamper tests that delete/reorder/edit/re-chain records, compromise one storage layer, replay old segments, and verify independent validation still detects manipulation.  
  **→ DONE** — implemented; see evidence
- [x] `MC-035-T09` Define attacker capabilities, protected assets, trust roots, security invariants, and fail-closed conditions specific to this control.  
  **→ DONE** — docs/THREAT_MODEL.md
- [x] `MC-035-T10` Apply least privilege, explicit scopes, tenant isolation, secure defaults, and separation of duties to every administrative and runtime identity involved.  
  **→ DONE** — production/rbac.py (deny-overrides, default deny, delegation subset)
- [ ] `MC-035-T11` Use approved cryptography/key management where authenticity, integrity, or confidentiality is required; define rotation and revocation.  
  **→ PARTIAL** — Ed25519/AES-256-GCM/SHA-256 via cryptography; no managed KMS (W-05)
- [x] `MC-035-T12` Ensure security-sensitive successes and failures produce tamper-evident audit records with stable reason codes and correlation IDs.  
  **→ DONE** — admit.decision/admit.refused/config.*/rbac.*/quarantine.* journal records with codes
- [x] `MC-035-T13` Derive negative/adversarial tests directly from the threat model and retain machine-readable results as release evidence.  
  **→ DONE** — tests/security/test_threats.py T01-T17

### Definition of done / acceptance evidence

- [x] `MC-035-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-035-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-035-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-035-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-035-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-035-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-035-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-036 — Formal threat model

> **4.3.0 disposition: PARTIAL** · DONE 14 · OPEN_HUMAN 3 · PARTIAL 3  
> Evidence: `docs/THREAT_MODEL.md` (exists), `tests.security.test_threats` (17/17 pass)

**Severity:** High  
**Related controls:** C041  
**Gap statement:** No STRIDE/attack-tree/abuse-case artifact covers malicious tenants, compromised control-plane actors, replay, confused deputy, dependency compromise, or supply-chain attacks.  

### Implementation checklist

- [x] `MC-036-T01` Create a formal threat model with assets, trust boundaries, data flows, entry points, actors, attacker capabilities, and security assumptions.  
  **→ DONE** — implemented; see evidence
- [x] `MC-036-T02` Cover malicious tenant/admin, compromised service identity, compromised admission replica, registry compromise, signer-key compromise, policy-engine compromise, storage compromise, and network attacker scenarios.  
  **→ DONE** — implemented; see evidence
- [x] `MC-036-T03` Use STRIDE plus abuse cases/attack trees for spoofing, tampering, repudiation, information disclosure, denial of service, elevation of privilege, replay, confused deputy, and supply-chain attacks.  
  **→ DONE** — implemented; see evidence
- [x] `MC-036-T04` Identify security invariants such as “unadmitted manifests are never forwarded” and “cross-tenant data is never disclosed.”  
  **→ DONE** — implemented; see evidence
- [x] `MC-036-T05` Map each threat to preventative, detective, and recovery controls and to concrete test IDs.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-036-T06` Rate residual risk using the organisation’s approved methodology and create owned remediation items for unacceptable risk.  
  **→ OPEN_HUMAN** — residual risk uses a provisional L/M/H scale; organisation methodology and owners needed
- [x] `MC-036-T07` Include boundary-specific threats for OIDC/mTLS, GitOps webhooks, registry resolution, provenance verification, policy calls, audit export, and admin APIs.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-036-T08` Review the threat model on material architecture/interface changes and before every production exit gate.  
  **→ OPEN_HUMAN** — review cadence defined; reviews must be performed by people
- [x] `MC-036-T09` Define attacker capabilities, protected assets, trust roots, security invariants, and fail-closed conditions specific to this control.  
  **→ DONE** — docs/THREAT_MODEL.md
- [x] `MC-036-T10` Apply least privilege, explicit scopes, tenant isolation, secure defaults, and separation of duties to every administrative and runtime identity involved.  
  **→ DONE** — production/rbac.py (deny-overrides, default deny, delegation subset)
- [ ] `MC-036-T11` Use approved cryptography/key management where authenticity, integrity, or confidentiality is required; define rotation and revocation.  
  **→ PARTIAL** — Ed25519/AES-256-GCM/SHA-256 via cryptography; no managed KMS (W-05)
- [x] `MC-036-T12` Ensure security-sensitive successes and failures produce tamper-evident audit records with stable reason codes and correlation IDs.  
  **→ DONE** — admit.decision/admit.refused/config.*/rbac.*/quarantine.* journal records with codes
- [x] `MC-036-T13` Derive negative/adversarial tests directly from the threat model and retain machine-readable results as release evidence.  
  **→ DONE** — tests/security/test_threats.py T01-T17

### Definition of done / acceptance evidence

- [x] `MC-036-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-036-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-036-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-036-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-036-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-036-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-036-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-037 — Security adversarial and fuzz suite

> **4.3.0 disposition: PARTIAL** · DONE 11 · OPEN_HUMAN 2 · PARTIAL 7  
> Evidence: `tests/fuzz/test_fuzz.py` (exists), `tests/security/test_threats.py` (exists), `tests.fuzz.test_fuzz` (4/4 pass), `tests.security.test_threats` (17/17 pass), `lane:lint` (PASS)

**Severity:** High  
**Related controls:** C050, C085, C087  
**Gap statement:** No fuzzing or dedicated tests cover replay, spoofing, injection, privilege escalation, side channels, parser bombs, or resource exhaustion.  

### Implementation checklist

- [ ] `MC-037-T01` Create grammar/schema-aware fuzzers for admission manifests, RBAC/admin payloads, audit query parameters, configuration documents, and any protocol decoders.  
  **→ PARTIAL** — admission, token and config fuzzers; RBAC/audit-query payloads not fuzzed
- [x] `MC-037-T02` Add property-based tests for authorization monotonicity, tenant isolation, idempotency, deterministic canonicalization, and audit-chain invariants.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-037-T03` Build replay/spoofing tests for tokens, webhooks, idempotency keys, signed artifacts, policy responses, and downstream acknowledgements.  
  **→ PARTIAL** — tokens, idempotency keys, signatures, policy versions, duplicate acks; webhooks absent
- [x] `MC-037-T04` Create injection tests for JSON/string fields, log forging, path/URL parsing, registry references, Unicode confusables, and control characters.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-037-T05` Create resource-exhaustion cases: oversized/deep JSON, huge lists, compression bombs if supported, slow clients, many concurrent requests, and pathological policy sets.  
  **→ PARTIAL** — oversized/deep/many components/overload; slow clients and pathological policy sets not tested
- [x] `MC-037-T06` Exercise privilege-escalation paths including scope confusion, wildcard roles, stale caches, race conditions, and admin API abuse.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-037-T07` Run sanitizers/static analysis/dependency scanning appropriate to implementation languages and retain crash corpora/minimized reproducers.  
  **→ PARTIAL** — ruff/mypy static analysis run; no sanitizers, no persistent crash corpus
- [ ] `MC-037-T08` Gate releases on zero unresolved high-severity fuzz/security findings or a time-bounded approved waiver with compensating controls.  
  **→ OPEN_HUMAN** — release gating on zero high findings is wired in the exit gate; waiver approvals needed
- [x] `MC-037-T09` Define attacker capabilities, protected assets, trust roots, security invariants, and fail-closed conditions specific to this control.  
  **→ DONE** — docs/THREAT_MODEL.md
- [x] `MC-037-T10` Apply least privilege, explicit scopes, tenant isolation, secure defaults, and separation of duties to every administrative and runtime identity involved.  
  **→ DONE** — production/rbac.py (deny-overrides, default deny, delegation subset)
- [ ] `MC-037-T11` Use approved cryptography/key management where authenticity, integrity, or confidentiality is required; define rotation and revocation.  
  **→ PARTIAL** — Ed25519/AES-256-GCM/SHA-256 via cryptography; no managed KMS (W-05)
- [x] `MC-037-T12` Ensure security-sensitive successes and failures produce tamper-evident audit records with stable reason codes and correlation IDs.  
  **→ DONE** — admit.decision/admit.refused/config.*/rbac.*/quarantine.* journal records with codes
- [x] `MC-037-T13` Derive negative/adversarial tests directly from the threat model and retain machine-readable results as release evidence.  
  **→ DONE** — tests/security/test_threats.py T01-T17

### Definition of done / acceptance evidence

- [x] `MC-037-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-037-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-037-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-037-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-037-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-037-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-037-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

# Resilience and distributed-systems behavior

## MC-038 — Durable state store with crash recovery/replay

> **4.3.0 disposition: PARTIAL** · DONE 13 · OPEN_HUMAN 1 · PARTIAL 6  
> Evidence: `production/journal.py` (exists), `production/service.py` (exists), `tests.unit.test_units.JournalTest.test_torn_tail_recovered_interior_corruption_fails` (1/1 pass), `tests.fault.test_faults.FaultTest.test_crash_between_decision_and_delivery_resumes_after_restart` (1/1 pass)

**Severity:** Critical  
**Related controls:** C057, C095  
**Gap statement:** Audit and forwarded state disappear on process restart; no journal, transaction log, snapshot, or replay protocol exists.  
**Key cross-component dependencies:** MC-005, MC-039, MC-063  

### Implementation checklist

- [x] `MC-038-T01` Persist admission decisions, audit events, forwarding/outbox state, config/policy revision references, and inventory state in durable storage before acknowledging committed operations.  
  **→ DONE** — implemented; see evidence
- [x] `MC-038-T02` Define transaction boundaries that prevent an admitted decision from existing without its corresponding authoritative audit/state record.  
  **→ DONE** — implemented; see evidence
- [x] `MC-038-T03` Implement write-ahead journal/outbox semantics for downstream forwarding and replay unfinished deliveries after restart.  
  **→ DONE** — implemented; see evidence
- [x] `MC-038-T04` Define snapshot/checkpoint creation and deterministic replay from log plus snapshot, including schema-version migration.  
  **→ DONE** — implemented; see evidence
- [x] `MC-038-T05` Use fsync/commit durability settings consistent with stated RPO; document acknowledged-loss guarantees explicitly.  
  **→ DONE** — implemented; see evidence
- [x] `MC-038-T06` Detect and reject corrupted/incomplete journal segments rather than silently skipping them.  
  **→ DONE** — implemented; see evidence
- [x] `MC-038-T07` Implement startup recovery that verifies state integrity before becoming ready for traffic.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-038-T08` Add crash-point tests at every persistence/forwarding boundary and prove restart yields exactly-once logical state even when physical delivery is at-least-once.  
  **→ PARTIAL** — crash points at journal append and decision->delivery; not every persistence boundary
- [ ] `MC-038-T09` Define RPO/RTO, consistency, availability, ordering, duplicate-handling, and failover behavior for each relevant failure domain.  
  **→ PARTIAL** — consistency/ordering/duplicates defined; RPO/RTO values PROPOSED
- [x] `MC-038-T10` Bound retries, queues, memory, concurrency, and recovery work to prevent cascades during dependency or site failure.  
  **→ DONE** — retry policy, shedder, bounded caches, compaction
- [x] `MC-038-T11` Preserve authorization, isolation, audit integrity, and idempotency invariants during crash, replay, failover, partition, and reconnect.  
  **→ DONE** — tested in fault/disaster/concurrency suites
- [ ] `MC-038-T12` Expose health/degraded/lag/backlog/ownership state and give operators an audited safe-control action when automation is insufficient.  
  **→ PARTIAL** — health/degraded/backlog/ownership via /readyz + freeze controls; no replication-lag signal
- [ ] `MC-038-T13` Prove the design using repeatable multi-process/fault-injection tests in a production-like environment.  
  **→ PARTIAL** — repeatable multi-process + fault tests on one host; not production-like

### Definition of done / acceptance evidence

- [x] `MC-038-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-038-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-038-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-038-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-038-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-038-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-038-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-039 — HA replication/consensus/leader election

> **4.3.0 disposition: PARTIAL** · DONE 10 · OPEN_HUMAN 1 · PARTIAL 9  
> Evidence: `production/replication.py` (exists), `tests.concurrency.test_races.RaceTest.test_stale_leader_is_fenced` (1/1 pass), `tests.concurrency.test_races.RaceTest.test_multiprocess_fenced_writers_keep_one_chain` (1/1 pass), `tests.disaster.test_disaster.DisasterTest.test_site_loss_failover_with_fencing` (1/1 pass)

**Severity:** Critical  
**Related controls:** C055, C058  
**Gap statement:** No multi-instance coordination, fencing, epoch/lease model, duplicate-owner protection, or split-brain handling exists.  
**Key cross-component dependencies:** MC-038, MC-043, MC-058  

### Implementation checklist

- [x] `MC-039-T01` Define whether admission replicas are stateless over a shared strongly consistent store or coordinate through leader/partition ownership; document the chosen consistency model.  
  **→ DONE** — implemented; see evidence
- [x] `MC-039-T02` If leaders/owners exist, implement leases/epochs/fencing tokens so stale controllers cannot commit after losing ownership.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-039-T03` Define replication factor, quorum/consistency settings, placement across failure domains, and behavior during quorum loss.  
  **→ PARTIAL** — shared storage replication delegated to the storage layer; no quorum settings in INV-66
- [x] `MC-039-T04` Prevent duplicate forwarding and duplicate config activation across replicas using durable idempotency/outbox coordination.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-039-T05` Define read consistency for policy/config changes and maximum propagation delay for revocation-sensitive data.  
  **→ PARTIAL** — followers converge on refresh(); no bounded propagation-delay guarantee
- [x] `MC-039-T06` Define split-brain detection and safe mode; never allow both partitions to make mutually incompatible authoritative changes when consistency is required.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-039-T07` Expose leader/epoch/replica health and replication lag metrics.  
  **→ PARTIAL** — role/epoch exposed in /readyz; no replication-lag metric
- [ ] `MC-039-T08` Create multi-node tests for leader loss, delayed packets, asymmetric partition, clock skew, stale owner, concurrent writers, and healing after split-brain.  
  **→ PARTIAL** — multi-process on one host; no multi-node, packet-delay or clock-skew tests
- [ ] `MC-039-T09` Define RPO/RTO, consistency, availability, ordering, duplicate-handling, and failover behavior for each relevant failure domain.  
  **→ PARTIAL** — consistency/ordering/duplicates defined; RPO/RTO values PROPOSED
- [x] `MC-039-T10` Bound retries, queues, memory, concurrency, and recovery work to prevent cascades during dependency or site failure.  
  **→ DONE** — retry policy, shedder, bounded caches, compaction
- [x] `MC-039-T11` Preserve authorization, isolation, audit integrity, and idempotency invariants during crash, replay, failover, partition, and reconnect.  
  **→ DONE** — tested in fault/disaster/concurrency suites
- [ ] `MC-039-T12` Expose health/degraded/lag/backlog/ownership state and give operators an audited safe-control action when automation is insufficient.  
  **→ PARTIAL** — health/degraded/backlog/ownership via /readyz + freeze controls; no replication-lag signal
- [ ] `MC-039-T13` Prove the design using repeatable multi-process/fault-injection tests in a production-like environment.  
  **→ PARTIAL** — repeatable multi-process + fault tests on one host; not production-like

### Definition of done / acceptance evidence

- [x] `MC-039-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-039-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-039-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-039-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-039-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-039-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-039-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-040 — Dependency health and degraded-mode controller

> **4.3.0 disposition: PARTIAL** · DONE 9 · OPEN_HUMAN 1 · PARTIAL 10  
> Evidence: `production/resilience.py` (exists), `production/service.py` (exists), `tests.fault.test_faults.FaultTest.test_journal_failure_admits_nothing_and_degrades` (1/1 pass)

**Severity:** High  
**Related controls:** C052, C056  
**Gap statement:** No health model tracks identity, policy, registry, signing, deployment, or storage dependencies or decides safe degraded behavior.  

### Implementation checklist

- [ ] `MC-040-T01` Create a dependency registry describing criticality, endpoint, active protocol version, health method, timeout budget, and degraded-mode policy for each external dependency.  
  **→ PARTIAL** — dependency registry with required flag; endpoint/timeout in config; no protocol-version/health-method per dependency
- [ ] `MC-040-T02` Implement active/passive health checks that distinguish unreachable, unauthorized, stale, overloaded, semantically unhealthy, and version-incompatible states.  
  **→ PARTIAL** — up/down with last error; no distinction of unauthorized/stale/version-incompatible states
- [x] `MC-040-T03` Aggregate dependency state into readiness without marking the service ready when a security-critical dependency cannot support safe admission.  
  **→ DONE** — implemented; see evidence
- [x] `MC-040-T04` Define explicit degraded capabilities: e.g., audit queries available while new admissions are blocked, or cached read-only inventory while deployment forwarding is unavailable.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-040-T05` Use hysteresis/debounce to prevent readiness flapping and synchronized reconnect storms.  
  **→ PARTIAL** — no hysteresis/debounce on readiness
- [ ] `MC-040-T06` Expose last-success/last-error, latency, breaker state, and staleness age without leaking sensitive details.  
  **→ PARTIAL** — last error + since; no latency/staleness age per dependency
- [ ] `MC-040-T07` Emit audit/operator events when entering or leaving degraded mode.  
  **→ PARTIAL** — degraded transitions visible in health; no audit event on enter/leave
- [x] `MC-040-T08` Add tests for each dependency failing independently and correlated failures, verifying readiness and allowed operation set match policy.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-040-T09` Define RPO/RTO, consistency, availability, ordering, duplicate-handling, and failover behavior for each relevant failure domain.  
  **→ PARTIAL** — consistency/ordering/duplicates defined; RPO/RTO values PROPOSED
- [x] `MC-040-T10` Bound retries, queues, memory, concurrency, and recovery work to prevent cascades during dependency or site failure.  
  **→ DONE** — retry policy, shedder, bounded caches, compaction
- [x] `MC-040-T11` Preserve authorization, isolation, audit integrity, and idempotency invariants during crash, replay, failover, partition, and reconnect.  
  **→ DONE** — tested in fault/disaster/concurrency suites
- [ ] `MC-040-T12` Expose health/degraded/lag/backlog/ownership state and give operators an audited safe-control action when automation is insufficient.  
  **→ PARTIAL** — health/degraded/backlog/ownership via /readyz + freeze controls; no replication-lag signal
- [ ] `MC-040-T13` Prove the design using repeatable multi-process/fault-injection tests in a production-like environment.  
  **→ PARTIAL** — repeatable multi-process + fault tests on one host; not production-like

### Definition of done / acceptance evidence

- [x] `MC-040-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-040-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-040-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-040-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-040-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-040-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-040-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-041 — Bounded retry/circuit breaker/load shedding

> **4.3.0 disposition: PARTIAL** · DONE 11 · OPEN_HUMAN 1 · PARTIAL 8  
> Evidence: `production/resilience.py` (exists), `tests.unit.test_units.SmallPartsTest.test_breaker` (1/1 pass), `tests.unit.test_units.SmallPartsTest.test_retry_classification` (1/1 pass), `tests.fault.test_faults.FaultTest.test_breaker_opens_on_repeated_delivery_failure` (1/1 pass)

**Severity:** High  
**Related controls:** C053-C054  
**Gap statement:** No remote calls exist yet, and no reusable retry/backoff/jitter, breaker, queue, or shedding policy is implemented for future adapters.  

### Implementation checklist

- [ ] `MC-041-T01` Implement shared retry middleware with operation-specific policy, exponential backoff, full/equal jitter, retry budget, maximum elapsed time, and cancellation support.  
  **→ PARTIAL** — backoff + full jitter + max attempts + deadline; no retry budget
- [x] `MC-041-T02` Retry only idempotent operations or operations protected by idempotency keys/transaction IDs; document exceptions explicitly.  
  **→ DONE** — implemented; see evidence
- [x] `MC-041-T03` Implement circuit breakers per dependency/endpoint with closed/open/half-open states and bounded probe traffic.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-041-T04` Add concurrency and queue limits per dependency to stop slow downstreams from exhausting worker/thread/socket pools.  
  **→ PARTIAL** — global + per-tenant in-flight limits; no per-dependency concurrency pools
- [x] `MC-041-T05` Implement load shedding before resource exhaustion and return stable overload errors plus safe retry-after hints.  
  **→ DONE** — implemented; see evidence
- [x] `MC-041-T06` Honor upstream deadlines so retries cannot exceed the caller’s end-to-end budget.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-041-T07` Export retry count, breaker transitions, shed requests, queue depth, wait time, and dependency saturation metrics.  
  **→ PARTIAL** — shed, breaker-open, pending exported; retry counts and breaker transitions not counted
- [x] `MC-041-T08` Create failure-injection tests for timeouts, resets, 429/503, lost acknowledgements, partial outages, and recovery without retry amplification.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-041-T09` Define RPO/RTO, consistency, availability, ordering, duplicate-handling, and failover behavior for each relevant failure domain.  
  **→ PARTIAL** — consistency/ordering/duplicates defined; RPO/RTO values PROPOSED
- [x] `MC-041-T10` Bound retries, queues, memory, concurrency, and recovery work to prevent cascades during dependency or site failure.  
  **→ DONE** — retry policy, shedder, bounded caches, compaction
- [x] `MC-041-T11` Preserve authorization, isolation, audit integrity, and idempotency invariants during crash, replay, failover, partition, and reconnect.  
  **→ DONE** — tested in fault/disaster/concurrency suites
- [ ] `MC-041-T12` Expose health/degraded/lag/backlog/ownership state and give operators an audited safe-control action when automation is insufficient.  
  **→ PARTIAL** — health/degraded/backlog/ownership via /readyz + freeze controls; no replication-lag signal
- [ ] `MC-041-T13` Prove the design using repeatable multi-process/fault-injection tests in a production-like environment.  
  **→ PARTIAL** — repeatable multi-process + fault tests on one host; not production-like

### Definition of done / acceptance evidence

- [x] `MC-041-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-041-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-041-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-041-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-041-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-041-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-041-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-042 — Quarantine/freeze/emergency-disable control

> **4.3.0 disposition: PARTIAL** · DONE 11 · OPEN_HUMAN 1 · PARTIAL 8  
> Evidence: `production/quarantine.py` (exists), `tests.security.test_threats.ThreatTest.test_T13_quarantine_needs_two_to_release` (1/1 pass), `tests.security.test_threats.ThreatTest.test_T14_freeze_survives_restart` (1/1 pass)

**Severity:** High  
**Related controls:** C059, C092  
**Gap statement:** README mentions package removal, but no runtime administrative freeze, tenant/lattice quarantine, kill switch, or audited emergency action exists.  

### Implementation checklist

- [ ] `MC-042-T01` Implement audited administrative controls to freeze all admissions, quarantine a tenant/organisation, quarantine a lattice/site, disable forwarding, and revoke a specific artifact/signer/registry.  
  **→ PARTIAL** — freeze global/tenant/lattice implemented; per-artifact/signer/registry revocation via config generation
- [ ] `MC-042-T02` Define separate capabilities for invoking, approving, and clearing emergency controls; use dual control for high-impact production actions where required.  
  **→ PARTIAL** — freeze and release share the quarantine capability; release requires 2 distinct holders
- [x] `MC-042-T03` Persist emergency state durably and replicate it consistently so a restart or failover cannot silently clear the control.  
  **→ DONE** — implemented; see evidence
- [x] `MC-042-T04` Define precedence so emergency deny/freeze controls override ordinary allow policy and cached decisions.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-042-T05` Include reason, incident/ticket, actor, approver, activation time, expiry/TTL, scope, and recovery criteria in every action.  
  **→ PARTIAL** — reason + actor + approvers recorded; no expiry/TTL or structured ticket field
- [x] `MC-042-T06` Expose current emergency state prominently through health/status and operator interfaces.  
  **→ DONE** — implemented; see evidence
- [x] `MC-042-T07` Define safe unfreeze procedure including policy/config refresh and verification that the unsafe condition is resolved.  
  **→ DONE** — implemented; see evidence
- [x] `MC-042-T08` Add tests proving quarantined scopes cannot forward while unaffected tenants remain isolated and operational as intended.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-042-T09` Define RPO/RTO, consistency, availability, ordering, duplicate-handling, and failover behavior for each relevant failure domain.  
  **→ PARTIAL** — consistency/ordering/duplicates defined; RPO/RTO values PROPOSED
- [x] `MC-042-T10` Bound retries, queues, memory, concurrency, and recovery work to prevent cascades during dependency or site failure.  
  **→ DONE** — retry policy, shedder, bounded caches, compaction
- [x] `MC-042-T11` Preserve authorization, isolation, audit integrity, and idempotency invariants during crash, replay, failover, partition, and reconnect.  
  **→ DONE** — tested in fault/disaster/concurrency suites
- [ ] `MC-042-T12` Expose health/degraded/lag/backlog/ownership state and give operators an audited safe-control action when automation is insufficient.  
  **→ PARTIAL** — health/degraded/backlog/ownership via /readyz + freeze controls; no replication-lag signal
- [ ] `MC-042-T13` Prove the design using repeatable multi-process/fault-injection tests in a production-like environment.  
  **→ PARTIAL** — repeatable multi-process + fault tests on one host; not production-like

### Definition of done / acceptance evidence

- [x] `MC-042-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-042-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-042-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-042-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-042-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-042-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-042-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-043 — Fault-injection/recovery test suite

> **4.3.0 disposition: PARTIAL_EXTERNAL_DEPENDENCY** · DONE 8 · OPEN_EXTERNAL 1 · OPEN_HUMAN 1 · PARTIAL 10  
> Evidence: `tests/fault/test_faults.py` (exists), `tests.fault.test_faults` (11/11 pass)

**Severity:** High  
**Related controls:** C060, C089  
**Gap statement:** No dependency failure, crash, disk-full, timeout, partition, reconnect, or stale-controller tests exist.  

### Implementation checklist

- [ ] `MC-043-T01` Build a deterministic fault-injection harness capable of failing each dependency with timeout, connection reset, stale data, malformed response, authorization error, overload, and partial success.  
  **→ PARTIAL** — timeouts, errors, overload, malformed, auth for policy/delivery; not every dependency x every mode
- [ ] `MC-043-T02` Inject process crash/kill at persistence boundaries, config activation, audit append, outbox enqueue, forwarding send, and acknowledgement handling.  
  **→ PARTIAL** — crash points: journal append, between decision and delivery; not every boundary
- [ ] `MC-043-T03` Test storage failure modes including disk/full quota, read-only volume, corruption, high latency, lost quorum, and restore from backup.  
  **→ PARTIAL** — disk full + corruption + restore; read-only volume, latency, lost quorum not injected
- [ ] `MC-043-T04` Test network partitions, asymmetric reachability, DNS failure, certificate expiry, KMS outage, and clock skew/time-service loss.  
  **→ PARTIAL** — partition + mTLS failures; DNS/KMS/clock-skew not injected
- [ ] `MC-043-T05` Define expected safe state, data-loss bound, recovery-time objective, and operator signal for every fault scenario.  
  **→ PARTIAL** — expected safe state documented per dependency; data-loss bound/RTO per scenario not tabulated
- [x] `MC-043-T06` Automate recovery assertions so tests verify no unauthorized forward, no silent audit gap, no cross-tenant leakage, and no duplicate logical operation.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-043-T07` Run representative chaos tests in a production-like multi-replica environment rather than only unit-test mocks.  
  **→ OPEN_EXTERNAL** — no production-like multi-replica environment
- [x] `MC-043-T08` Retain machine-readable fault-injection results and link them to resilience requirements in the RTM.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-043-T09` Define RPO/RTO, consistency, availability, ordering, duplicate-handling, and failover behavior for each relevant failure domain.  
  **→ PARTIAL** — consistency/ordering/duplicates defined; RPO/RTO values PROPOSED
- [x] `MC-043-T10` Bound retries, queues, memory, concurrency, and recovery work to prevent cascades during dependency or site failure.  
  **→ DONE** — retry policy, shedder, bounded caches, compaction
- [x] `MC-043-T11` Preserve authorization, isolation, audit integrity, and idempotency invariants during crash, replay, failover, partition, and reconnect.  
  **→ DONE** — tested in fault/disaster/concurrency suites
- [ ] `MC-043-T12` Expose health/degraded/lag/backlog/ownership state and give operators an audited safe-control action when automation is insufficient.  
  **→ PARTIAL** — health/degraded/backlog/ownership via /readyz + freeze controls; no replication-lag signal
- [ ] `MC-043-T13` Prove the design using repeatable multi-process/fault-injection tests in a production-like environment.  
  **→ PARTIAL** — repeatable multi-process + fault tests on one host; not production-like

### Definition of done / acceptance evidence

- [x] `MC-043-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-043-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-043-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-043-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-043-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-043-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-043-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

# Performance and resource efficiency

## MC-044 — Reproducible benchmark/load/soak suite

> **4.3.0 disposition: PARTIAL_EXTERNAL_DEPENDENCY** · DONE 6 · OPEN_EXTERNAL 1 · OPEN_HUMAN 1 · PARTIAL 12  
> Evidence: `tools/bench.py` (exists), `release/bench.json` (exists)

**Severity:** High  
**Related controls:** C061-C064, C088  
**Gap statement:** No latency, throughput, startup, CPU, memory, storage, network, burst, overload, recovery, or fleet-scale measurements are packaged.  
**Key cross-component dependencies:** MC-045, MC-047  

### Implementation checklist

- [x] `MC-044-T01` Create a reproducible benchmark harness with fixed hardware/runtime metadata, dataset seeds, dependency versions, and workload profiles.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-044-T02` Measure admission end-to-end and internal-stage latency (authn, authz, policy, provenance, persistence, forwarding enqueue), throughput, CPU, RSS/heap, I/O, network, and storage write rates.  
  **→ PARTIAL** — end-to-end latency, throughput, heap, FDs, journal bytes; no per-stage breakdown, CPU or network
- [ ] `MC-044-T03` Cover cold start, warm steady state, burst, overload, scale-out, scale-in, dependency slowdown, restart/recovery, and multi-tenant noisy-neighbor scenarios.  
  **→ PARTIAL** — no scale-out/in or dependency-slowdown scenarios
- [x] `MC-044-T04` Include manifest-size/component-count distributions representative of expected production workloads plus adversarial boundary sizes.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-044-T05` Run soak tests long enough to expose leaks, queue growth, compaction pauses, key/token refresh effects, and periodic background tasks.  
  **→ PARTIAL** — 60 s soak recorded; longer soaks needed
- [ ] `MC-044-T06` Record p50/p95/p99/p99.9/max and confidence/sample counts; do not report only averages.  
  **→ PARTIAL** — p50/p95/p99/max; no p99.9 or confidence intervals
- [ ] `MC-044-T07` Publish baseline artifacts and compare new runs against an approved reference using statistical/absolute regression thresholds.  
  **→ PARTIAL** — comparison implemented; no approved baseline recorded yet
- [ ] `MC-044-T08` Automate execution in a controlled CI/performance environment and store raw results plus environment fingerprints.  
  **→ OPEN_EXTERNAL** — no controlled performance environment
- [ ] `MC-044-T09` Define representative workload distributions, environment fingerprints, warmup, run length, sample count, and statistical reporting methodology.  
  **→ PARTIAL** — 1/3/20-component workloads + env fingerprint; not production-derived
- [ ] `MC-044-T10` Measure end-to-end and per-stage latency plus throughput, CPU, memory, I/O/network/storage, queueing, and saturation.  
  **→ PARTIAL** — end-to-end, throughput, heap, FDs; no per-stage/CPU/network
- [x] `MC-044-T11` Define hard bounds for memory/queue/concurrency and verify graceful overload rather than unbounded degradation.  
  **→ DONE** — in-flight caps, quotas, bounded caches; overload test
- [ ] `MC-044-T12` Link measured capacity to autoscaling/resource sizing and document dependency bottlenecks/assumptions.  
  **→ PARTIAL** — docs/CAPACITY.md model; scaling thresholds PROPOSED
- [ ] `MC-044-T13` Publish raw benchmark artifacts and enforce approved regression thresholds in CI/release gating.  
  **→ PARTIAL** — release/bench.json + perf gate; thresholds PROPOSED, no approved baseline

### Definition of done / acceptance evidence

- [x] `MC-044-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-044-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-044-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [ ] `MC-044-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ PARTIAL** — verification is document review only, or evidence did not verify
- [ ] `MC-044-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-044-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-044-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-045 — Performance thresholds and regression gate

> **4.3.0 disposition: PARTIAL** · DONE 10 · OPEN_HUMAN 1 · PARTIAL 9  
> Evidence: `tools/perf_gate.py` (exists), `release/perf_thresholds.json` (exists), `release/perf_gate.json` (exists), `lane:perf-gate` (PASS)

**Severity:** High  
**Related controls:** C062, C070  
**Gap statement:** The contract declares p99 <50 ms, but no benchmark enforces p50/p95/p99/worst-case or blocks regressions.  

### Implementation checklist

- [ ] `MC-045-T01` Define explicit latency thresholds for successful admission, policy denial, authentication denial, dependency-degraded denial, and administrative operations.  
  **→ PARTIAL** — thresholds for successful admission only
- [x] `MC-045-T02` Derive a stage budget from the contract p99 <50 ms target, reserving margin for network and storage variability.  
  **→ DONE** — implemented; see evidence
- [x] `MC-045-T03` Define minimum sustained throughput and maximum resource cost at target load, plus overload behavior and recovery thresholds.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-045-T04` Define startup/readiness, failover, config-propagation, and revocation-propagation performance objectives where operationally relevant.  
  **→ PARTIAL** — replay rate only; no failover/propagation objectives
- [x] `MC-045-T05` Use both absolute ceilings and percentage regression limits so a fast baseline cannot degrade materially while remaining under a loose maximum.  
  **→ DONE** — implemented; see evidence
- [x] `MC-045-T06` Gate on tail latency with sufficient sample size and stable environment; exclude warmup using documented methodology rather than cherry-picking.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-045-T07` Create waiver workflow for intentional regressions with owner, rationale, expiry, and compensating capacity plan.  
  **→ PARTIAL** — waiver workflow documented; approvals human
- [x] `MC-045-T08` Publish pass/fail benchmark evidence into the release acceptance bundle.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-045-T09` Define representative workload distributions, environment fingerprints, warmup, run length, sample count, and statistical reporting methodology.  
  **→ PARTIAL** — 1/3/20-component workloads + env fingerprint; not production-derived
- [ ] `MC-045-T10` Measure end-to-end and per-stage latency plus throughput, CPU, memory, I/O/network/storage, queueing, and saturation.  
  **→ PARTIAL** — end-to-end, throughput, heap, FDs; no per-stage/CPU/network
- [x] `MC-045-T11` Define hard bounds for memory/queue/concurrency and verify graceful overload rather than unbounded degradation.  
  **→ DONE** — in-flight caps, quotas, bounded caches; overload test
- [ ] `MC-045-T12` Link measured capacity to autoscaling/resource sizing and document dependency bottlenecks/assumptions.  
  **→ PARTIAL** — docs/CAPACITY.md model; scaling thresholds PROPOSED
- [ ] `MC-045-T13` Publish raw benchmark artifacts and enforce approved regression thresholds in CI/release gating.  
  **→ PARTIAL** — release/bench.json + perf gate; thresholds PROPOSED, no approved baseline

### Definition of done / acceptance evidence

- [x] `MC-045-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-045-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-045-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-045-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-045-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-045-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-045-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-046 — Bounded audit/forwarded retention

> **4.3.0 disposition: PARTIAL** · DONE 12 · OPEN_HUMAN 1 · PARTIAL 7  
> Evidence: `production/journal.py` (exists), `tests.unit.test_units.JournalTest.test_compaction_holds_and_summary_chain` (1/1 pass), `tests.disaster.test_disaster.DisasterTest.test_retention_compaction_then_restart_preserves_state` (1/1 pass)

**Severity:** Critical  
**Related controls:** C067  
**Gap statement:** Both in-memory collections grow without retention, compaction, persistence, or backpressure, creating unbounded memory growth.  

### Implementation checklist

- [x] `MC-046-T01` Remove unbounded process-local `_audit` and `_forwarded` growth from production code paths; replace with durable bounded interfaces/projections.  
  **→ DONE** — implemented; see evidence
- [x] `MC-046-T02` Define audit retention tiers, hot-query window, archival policy, compaction/index lifecycle, and legal-hold exceptions.  
  **→ DONE** — implemented; see evidence
- [x] `MC-046-T03` Define forwarded/outbox retention by terminal acknowledgement, retry horizon, deduplication window, and incident-forensics needs.  
  **→ DONE** — implemented; see evidence
- [x] `MC-046-T04` Apply hard memory/queue bounds and backpressure before process memory can grow with traffic indefinitely.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-046-T05` Use streaming/paginated audit export instead of loading entire history into memory.  
  **→ PARTIAL** — SIEM exporter streams in batches; audit_export returns a whole range in memory
- [x] `MC-046-T06` Expose retained count/bytes, queue depth, oldest pending age, compaction lag, and storage capacity/saturation metrics.  
  **→ DONE** — implemented; see evidence
- [x] `MC-046-T07` Define behavior when retention/archival sinks are full or unavailable; never silently drop security audit events.  
  **→ DONE** — implemented; see evidence
- [x] `MC-046-T08` Add long-duration tests proving memory reaches steady state under sustained load and retention/compaction does not break audit verification.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-046-T09` Define representative workload distributions, environment fingerprints, warmup, run length, sample count, and statistical reporting methodology.  
  **→ PARTIAL** — 1/3/20-component workloads + env fingerprint; not production-derived
- [ ] `MC-046-T10` Measure end-to-end and per-stage latency plus throughput, CPU, memory, I/O/network/storage, queueing, and saturation.  
  **→ PARTIAL** — end-to-end, throughput, heap, FDs; no per-stage/CPU/network
- [x] `MC-046-T11` Define hard bounds for memory/queue/concurrency and verify graceful overload rather than unbounded degradation.  
  **→ DONE** — in-flight caps, quotas, bounded caches; overload test
- [ ] `MC-046-T12` Link measured capacity to autoscaling/resource sizing and document dependency bottlenecks/assumptions.  
  **→ PARTIAL** — docs/CAPACITY.md model; scaling thresholds PROPOSED
- [ ] `MC-046-T13` Publish raw benchmark artifacts and enforce approved regression thresholds in CI/release gating.  
  **→ PARTIAL** — release/bench.json + perf gate; thresholds PROPOSED, no approved baseline

### Definition of done / acceptance evidence

- [x] `MC-046-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-046-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-046-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-046-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-046-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-046-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-046-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-047 — Capacity/saturation model

> **4.3.0 disposition: PARTIAL** · DONE 8 · OPEN_HUMAN 3 · PARTIAL 9  
> Evidence: `docs/CAPACITY.md` (exists), `release/bench.json` (exists)

**Severity:** High  
**Related controls:** C069  
**Gap statement:** No capacity formula or signal identifies safe admission QPS, queue depth, audit growth, tenant count, or scaling thresholds.  

### Implementation checklist

- [x] `MC-047-T01` Derive a capacity model from per-admission CPU time, memory, external-call concurrency, audit write throughput, outbox throughput, and datastore limits.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-047-T02` Model capacity by tenant count, lattices, active policies/bindings, manifest size, admission QPS, audit events/sec, inventory objects, and query load.  
  **→ PARTIAL** — QPS, components, journal growth, replay modelled; tenant/binding/query-load dimensions not
- [x] `MC-047-T03` Define saturation indicators such as CPU, runnable queue, request queue depth/age, connection pool utilization, datastore latency, replication lag, breaker state, and audit backlog.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-047-T04` Define scale-up/out trigger thresholds with hysteresis and minimum/maximum replica bounds.  
  **→ OPEN_HUMAN** — scaling thresholds PROPOSED, need SRE approval
- [x] `MC-047-T05` Account for dependency bottlenecks so control-plane replicas are not scaled beyond safe identity/policy/registry/store throughput.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-047-T06` Include failure-headroom requirements (for example N+1/N+AZ-loss capacity) in sizing.  
  **→ OPEN_HUMAN** — N+1 policy needs approval
- [x] `MC-047-T07` Validate the model using measured benchmark data and document known nonlinearities.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-047-T08` Create an operator capacity dashboard and runbook mapping saturation signals to scaling/remediation actions.  
  **→ PARTIAL** — dashboard panels exist; no dedicated capacity dashboard
- [ ] `MC-047-T09` Define representative workload distributions, environment fingerprints, warmup, run length, sample count, and statistical reporting methodology.  
  **→ PARTIAL** — 1/3/20-component workloads + env fingerprint; not production-derived
- [ ] `MC-047-T10` Measure end-to-end and per-stage latency plus throughput, CPU, memory, I/O/network/storage, queueing, and saturation.  
  **→ PARTIAL** — end-to-end, throughput, heap, FDs; no per-stage/CPU/network
- [x] `MC-047-T11` Define hard bounds for memory/queue/concurrency and verify graceful overload rather than unbounded degradation.  
  **→ DONE** — in-flight caps, quotas, bounded caches; overload test
- [ ] `MC-047-T12` Link measured capacity to autoscaling/resource sizing and document dependency bottlenecks/assumptions.  
  **→ PARTIAL** — docs/CAPACITY.md model; scaling thresholds PROPOSED
- [ ] `MC-047-T13` Publish raw benchmark artifacts and enforce approved regression thresholds in CI/release gating.  
  **→ PARTIAL** — release/bench.json + perf gate; thresholds PROPOSED, no approved baseline

### Definition of done / acceptance evidence

- [x] `MC-047-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-047-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-047-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [ ] `MC-047-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ PARTIAL** — verification is document review only, or evidence did not verify
- [ ] `MC-047-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-047-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-047-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-048 — Edge power/thermal characterization

> **4.3.0 disposition: NA_PROPOSED** · DONE 4 · NA_PROPOSED 7 · OPEN_HUMAN 2 · PARTIAL 7  
> Evidence: `docs/EDGE_POWER_THERMAL.md` (exists)

**Severity:** Low/conditional  
**Related controls:** C068  
**Gap statement:** No constrained-edge power or thermal measurements establish whether this central control-plane component is suitable where C068 applies.  

### Implementation checklist

- [ ] `MC-048-T01` Determine whether INV-66 is ever deployed on constrained near/far-edge nodes; if not, document C068 as not applicable with architecture approval.  
  **→ NA_PROPOSED** — proposed NOT APPLICABLE; architecture approval missing
- [ ] `MC-048-T02` If applicable, define representative hardware classes, ambient/thermal conditions, power modes, and baseline idle/load measurements.  
  **→ NA_PROPOSED** — n/a if T01 approved
- [ ] `MC-048-T03` Measure incremental CPU package/system power, energy per admission, memory pressure, storage I/O, and thermal throttling under representative loads.  
  **→ NA_PROPOSED** — n/a if T01 approved
- [ ] `MC-048-T04` Measure cold-start and reconnect behavior under battery/power-saving modes where relevant.  
  **→ NA_PROPOSED** — n/a if T01 approved
- [ ] `MC-048-T05` Define power/thermal budgets and overload/thermal-throttling behavior that preserves security semantics.  
  **→ NA_PROPOSED** — n/a if T01 approved
- [ ] `MC-048-T06` Compare local admission vs remote-control-plane architecture to quantify energy/network tradeoffs for disconnected sites.  
  **→ NA_PROPOSED** — n/a if T01 approved
- [ ] `MC-048-T07` Record measurement tooling, calibration, sampling period, and environmental conditions for reproducibility.  
  **→ NA_PROPOSED** — n/a if T01 approved
- [ ] `MC-048-T08` Add the resulting applicability decision or benchmark evidence to the production acceptance bundle.  
  **→ OPEN_HUMAN** — decision must be approved
- [ ] `MC-048-T09` Define representative workload distributions, environment fingerprints, warmup, run length, sample count, and statistical reporting methodology.  
  **→ PARTIAL** — 1/3/20-component workloads + env fingerprint; not production-derived
- [ ] `MC-048-T10` Measure end-to-end and per-stage latency plus throughput, CPU, memory, I/O/network/storage, queueing, and saturation.  
  **→ PARTIAL** — end-to-end, throughput, heap, FDs; no per-stage/CPU/network
- [x] `MC-048-T11` Define hard bounds for memory/queue/concurrency and verify graceful overload rather than unbounded degradation.  
  **→ DONE** — in-flight caps, quotas, bounded caches; overload test
- [ ] `MC-048-T12` Link measured capacity to autoscaling/resource sizing and document dependency bottlenecks/assumptions.  
  **→ PARTIAL** — docs/CAPACITY.md model; scaling thresholds PROPOSED
- [ ] `MC-048-T13` Publish raw benchmark artifacts and enforce approved regression thresholds in CI/release gating.  
  **→ PARTIAL** — release/bench.json + perf gate; thresholds PROPOSED, no approved baseline

### Definition of done / acceptance evidence

- [x] `MC-048-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-048-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-048-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [ ] `MC-048-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ PARTIAL** — verification is document review only, or evidence did not verify
- [ ] `MC-048-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-048-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-048-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

# Observability and explainability

## MC-049 — Health/readiness/version/config/dependency endpoint

> **4.3.0 disposition: PARTIAL** · DONE 15 · OPEN_HUMAN 1 · PARTIAL 4  
> Evidence: `production/service.py` (exists), `production/http_api.py` (exists), `schemas/PK_ECP_HEALTH_1.schema.json` (exists), `tests.unit.test_additions.AdditionsTest.test_health_runtime_gauges` (1/1 pass), `tests.integration.test_e2e.EndToEndTest.test_full_admission_path_over_http` (1/1 pass)

**Severity:** High  
**Related controls:** C071  
**Gap statement:** No operator endpoint exposes health, readiness, active version, config digest, dependency state, or capability set.  
**Key cross-component dependencies:** MC-050, MC-051, MC-052  

### Implementation checklist

- [ ] `MC-049-T01` Implement unauthenticated-minimal liveness and authenticated/appropriately exposed readiness/status endpoints with clearly separated semantics.  
  **→ PARTIAL** — /healthz unauthenticated-minimal; /readyz unauthenticated and must be network-restricted
- [x] `MC-049-T02` Report service build/version, protocol versions, active config/policy revision digests, feature/capability set, replica/site identity, and start time.  
  **→ DONE** — implemented; see evidence
- [x] `MC-049-T03` Report dependency health for identity, policy, provenance/registry, audit store, config/RBAC store, deployment manager, KMS, and telemetry/export sinks.  
  **→ DONE** — implemented; see evidence
- [x] `MC-049-T04` Include degraded-mode flags, emergency freeze/quarantine status, replication/leader state if applicable, and last successful authoritative write.  
  **→ DONE** — implemented; see evidence
- [x] `MC-049-T05` Keep readiness fail-closed when the service cannot safely admit/record/forward according to production requirements.  
  **→ DONE** — implemented; see evidence
- [x] `MC-049-T06` Bound endpoint latency and output size; do not perform unbounded dependency fan-out per scrape.  
  **→ DONE** — implemented; see evidence
- [x] `MC-049-T07` Redact secrets, tokens, internal credentials, and cross-tenant data; expose only operator-safe diagnostic metadata.  
  **→ DONE** — implemented; see evidence
- [x] `MC-049-T08` Add conformance tests for healthy, degraded, dependency-failed, stale-config, audit-store-failed, and emergency-freeze states.  
  **→ DONE** — implemented; see evidence
- [x] `MC-049-T09` Define a stable telemetry schema with data classification, tenant isolation, privacy/redaction, retention, and export controls.  
  **→ DONE** — PK_ECP_LOG/1 + docs/TELEMETRY_POLICY.md
- [x] `MC-049-T10` Correlate metrics/logs/traces/audit/explain records using bounded stable identifiers without making high-cardinality values default metric labels.  
  **→ DONE** — trace_id/decision_id/request_id across all four
- [x] `MC-049-T11` Instrument normal, denied, degraded, dependency-failed, overloaded, security-attack, and software-defect states distinctly.  
  **→ DONE** — outcome/code counters + degraded mode + shed/quota/authn counters
- [x] `MC-049-T12` Ensure telemetry/export failure cannot block security-critical processing or cause unbounded resource growth; audit data follows its stronger durability rule.  
  **→ DONE** — in-process metrics; synchronous bounded logging; SIEM exporter decoupled
- [ ] `MC-049-T13` Provide dashboards/alerts or operator views with explicit ownership and linked runbooks, then test them synthetically.  
  **→ PARTIAL** — dashboards/alerts with runbooks; ownership UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-049-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-049-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-049-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-049-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-049-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-049-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-049-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-050 — Metrics instrumentation/export

> **4.3.0 disposition: PARTIAL** · DONE 11 · OPEN_HUMAN 1 · PARTIAL 8  
> Evidence: `production/telemetry.py` (exists), `tests.unit.test_units.TelemetryTest` (3/3 pass), `lane:observability` (PASS)

**Severity:** High  
**Related controls:** C072  
**Gap statement:** Contract signal names are declarations only; no counters/histograms/gauges or Prometheus/OTel exporter exist.  

### Implementation checklist

- [ ] `MC-050-T01` Instrument request counters by operation/outcome/reason class, authorization denials, policy denials, provenance failures, audit appends, forwarding outcomes, retries, and dependency errors.  
  **→ PARTIAL** — outcome/code/rbac/authn/quota/shed/delivery/audit counters; policy/provenance failures not separately labelled
- [ ] `MC-050-T02` Instrument histograms for end-to-end admission and each major stage, using buckets appropriate to the <50 ms p99 target.  
  **→ PARTIAL** — end-to-end histogram only; no per-stage histograms
- [ ] `MC-050-T03` Export gauges for in-flight requests, queue depth/age, outbox backlog, policy/config age, cache size/hit ratio, replication lag, and dependency breaker state.  
  **→ PARTIAL** — in-flight, pending, breaker, frozen, idempotency gauges; no policy/config age gauge or cache hit ratio
- [ ] `MC-050-T04` Export process/runtime CPU, memory, GC, file descriptor/socket, thread/task, network, and storage metrics.  
  **→ PARTIAL** — RSS/threads/FDs; no GC/network/storage metrics
- [x] `MC-050-T05` Use OpenTelemetry/Prometheus-compatible naming, units, descriptions, and stable semantic conventions.  
  **→ DONE** — implemented; see evidence
- [x] `MC-050-T06` Control label cardinality: tenant/workload identifiers must not be unbounded default labels; use exemplars/log correlation for high-cardinality detail.  
  **→ DONE** — implemented; see evidence
- [x] `MC-050-T07` Add metric schema/version tests and dashboard queries that fail visibly if required series disappear.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-050-T08` Validate instrumentation overhead under benchmark load and ensure metrics export failure cannot block admission.  
  **→ PARTIAL** — metrics are in-process and cannot block; overhead not isolated in benchmark
- [x] `MC-050-T09` Define a stable telemetry schema with data classification, tenant isolation, privacy/redaction, retention, and export controls.  
  **→ DONE** — PK_ECP_LOG/1 + docs/TELEMETRY_POLICY.md
- [x] `MC-050-T10` Correlate metrics/logs/traces/audit/explain records using bounded stable identifiers without making high-cardinality values default metric labels.  
  **→ DONE** — trace_id/decision_id/request_id across all four
- [x] `MC-050-T11` Instrument normal, denied, degraded, dependency-failed, overloaded, security-attack, and software-defect states distinctly.  
  **→ DONE** — outcome/code counters + degraded mode + shed/quota/authn counters
- [x] `MC-050-T12` Ensure telemetry/export failure cannot block security-critical processing or cause unbounded resource growth; audit data follows its stronger durability rule.  
  **→ DONE** — in-process metrics; synchronous bounded logging; SIEM exporter decoupled
- [ ] `MC-050-T13` Provide dashboards/alerts or operator views with explicit ownership and linked runbooks, then test them synthetically.  
  **→ PARTIAL** — dashboards/alerts with runbooks; ownership UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-050-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-050-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-050-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-050-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-050-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-050-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-050-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-051 — Structured operational logging

> **4.3.0 disposition: PARTIAL** · DONE 14 · OPEN_HUMAN 1 · PARTIAL 5  
> Evidence: `production/telemetry.py` (exists), `tests.unit.test_units.TelemetryTest.test_log_allowlist_and_redaction` (1/1 pass), `tests.security.test_threats.ThreatTest.test_T12_secrets_and_tokens_never_logged_or_journaled` (1/1 pass)

**Severity:** High  
**Related controls:** C073, C075  
**Gap statement:** No stable tenant/lattice/workload/request identifiers, structured log schema, redaction policy, or sink integration exists.  

### Implementation checklist

- [ ] `MC-051-T01` Define a structured JSON/log schema with timestamp, severity, event name, service/version, site/replica, request ID, trace/span ID, tenant/lattice/workload IDs, principal ID, and stable error/reason code.  
  **→ PARTIAL** — ts/level/event/request/trace/span/tenant/lattice/subject/codes; no service/version/site/replica fields per line
- [x] `MC-051-T02` Use explicit event IDs rather than free-form-only messages for security and operational state transitions.  
  **→ DONE** — implemented; see evidence
- [x] `MC-051-T03` Create redaction rules for tokens, secrets, headers, manifest sensitive fields, repository credentials, and PII; default to omission rather than masking unknown fields.  
  **→ DONE** — implemented; see evidence
- [x] `MC-051-T04` Prevent log forging by structured encoding and sanitizing embedded control characters/newlines from untrusted fields.  
  **→ DONE** — implemented; see evidence
- [x] `MC-051-T05` Define sampling rules that never sample away required security/audit events while controlling high-volume success logs.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-051-T06` Route logs to approved sinks with buffering/backpressure that cannot exhaust application memory.  
  **→ PARTIAL** — synchronous stream writer; sink routing external
- [x] `MC-051-T07` Include enough correlation to reconstruct an admission across authn, policy, provenance, persistence, and forwarding without logging entire sensitive payloads.  
  **→ DONE** — implemented; see evidence
- [x] `MC-051-T08` Add tests that inspect emitted logs for required fields, tenant isolation, secret absence, bounded field lengths, and malformed-input safety.  
  **→ DONE** — implemented; see evidence
- [x] `MC-051-T09` Define a stable telemetry schema with data classification, tenant isolation, privacy/redaction, retention, and export controls.  
  **→ DONE** — PK_ECP_LOG/1 + docs/TELEMETRY_POLICY.md
- [x] `MC-051-T10` Correlate metrics/logs/traces/audit/explain records using bounded stable identifiers without making high-cardinality values default metric labels.  
  **→ DONE** — trace_id/decision_id/request_id across all four
- [x] `MC-051-T11` Instrument normal, denied, degraded, dependency-failed, overloaded, security-attack, and software-defect states distinctly.  
  **→ DONE** — outcome/code counters + degraded mode + shed/quota/authn counters
- [x] `MC-051-T12` Ensure telemetry/export failure cannot block security-critical processing or cause unbounded resource growth; audit data follows its stronger durability rule.  
  **→ DONE** — in-process metrics; synchronous bounded logging; SIEM exporter decoupled
- [ ] `MC-051-T13` Provide dashboards/alerts or operator views with explicit ownership and linked runbooks, then test them synthetically.  
  **→ PARTIAL** — dashboards/alerts with runbooks; ownership UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-051-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-051-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-051-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-051-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-051-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-051-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-051-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-052 — Distributed tracing/context propagation

> **4.3.0 disposition: PARTIAL** · DONE 13 · OPEN_HUMAN 1 · PARTIAL 6  
> Evidence: `production/telemetry.py` (exists), `tests.unit.test_units.TelemetryTest.test_trace_propagation` (1/1 pass), `tests.integration.test_e2e.EndToEndTest.test_full_admission_path_over_http` (1/1 pass)

**Severity:** High  
**Related controls:** C074  
**Gap statement:** No trace/span propagation is present across admission, policy, provenance, audit, or deployment boundaries.  

### Implementation checklist

- [x] `MC-052-T01` Adopt W3C Trace Context/OpenTelemetry or organisational standard and propagate trace/span context through admission, identity, policy, provenance/registry, storage, audit, and INV-63 calls.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-052-T02` Create spans for major stages with stable names and attributes such as outcome/reason class, dependency, protocol version, and bounded manifest metadata.  
  **→ PARTIAL** — one span per admission; no per-stage child spans
- [x] `MC-052-T03` Never attach tokens, full manifests, secrets, or unbounded/high-cardinality payloads to spans.  
  **→ DONE** — implemented; see evidence
- [x] `MC-052-T04` Propagate baggage only for explicitly approved low-cardinality context; do not use baggage as an authorization source.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-052-T05` Link asynchronous outbox/forwarding spans to the originating admission trace/event using span links or persisted correlation IDs.  
  **→ PARTIAL** — delivery linked by trace id + decision id, not span links
- [ ] `MC-052-T06` Define trace sampling that keeps errors/security denials at adequate rates while controlling ordinary high-volume traffic.  
  **→ PARTIAL** — uniform ratio sampling; errors/denials not preferentially retained
- [x] `MC-052-T07` Export trace IDs in structured logs and audit metadata where policy permits correlation.  
  **→ DONE** — implemented; see evidence
- [x] `MC-052-T08` Add integration tests proving context survives each protocol boundary and is not trusted when supplied by unauthenticated callers.  
  **→ DONE** — implemented; see evidence
- [x] `MC-052-T09` Define a stable telemetry schema with data classification, tenant isolation, privacy/redaction, retention, and export controls.  
  **→ DONE** — PK_ECP_LOG/1 + docs/TELEMETRY_POLICY.md
- [x] `MC-052-T10` Correlate metrics/logs/traces/audit/explain records using bounded stable identifiers without making high-cardinality values default metric labels.  
  **→ DONE** — trace_id/decision_id/request_id across all four
- [x] `MC-052-T11` Instrument normal, denied, degraded, dependency-failed, overloaded, security-attack, and software-defect states distinctly.  
  **→ DONE** — outcome/code counters + degraded mode + shed/quota/authn counters
- [x] `MC-052-T12` Ensure telemetry/export failure cannot block security-critical processing or cause unbounded resource growth; audit data follows its stronger durability rule.  
  **→ DONE** — in-process metrics; synchronous bounded logging; SIEM exporter decoupled
- [ ] `MC-052-T13` Provide dashboards/alerts or operator views with explicit ownership and linked runbooks, then test them synthetically.  
  **→ PARTIAL** — dashboards/alerts with runbooks; ownership UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-052-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-052-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-052-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-052-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-052-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-052-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-052-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-053 — Decision explainability/policy linkage view

> **4.3.0 disposition: PARTIAL** · DONE 13 · OPEN_HUMAN 1 · PARTIAL 6  
> Evidence: `production/service.py` (exists), `tests.integration.test_e2e.EndToEndTest.test_full_admission_path_over_http` (1/1 pass)

**Severity:** High  
**Related controls:** C076-C078  
**Gap statement:** Reasons exist, but there is no operator explain view linking a decision to authenticated principal, exact policy/config version, provenance result, topology, and constraints.  
**Key cross-component dependencies:** MC-014, MC-033, MC-069  

### Implementation checklist

- [x] `MC-053-T01` Define an explain record keyed by admission/audit event ID that links authenticated principal, requested operation, tenant/lattice/workload, manifest/artifact digest, and source/GitOps revision.  
  **→ DONE** — implemented; see evidence
- [x] `MC-053-T02` Capture exact RBAC bindings/capabilities and policy bundle/config revision that were evaluated, including deny/allow precedence.  
  **→ DONE** — implemented; see evidence
- [x] `MC-053-T03` Capture registry/signer/provenance checks with immutable digest and verifier result, without exposing secrets or unnecessary certificate content.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-053-T04` Capture quota/capacity decisions, dependency/degraded state, topology/site constraints, and forwarding disposition.  
  **→ PARTIAL** — quota and dependency state at decision time not captured in explain
- [ ] `MC-053-T05` Provide a bounded operator query/view that renders machine reason codes into a human-readable decision tree while retaining raw structured evidence.  
  **→ PARTIAL** — structured explain JSON; no rendered human-readable decision tree
- [x] `MC-053-T06` Enforce tenant/scope-aware authorization for explain data because it may reveal policy and infrastructure metadata.  
  **→ DONE** — implemented; see evidence
- [x] `MC-053-T07` Make explain data immutable or derived from immutable audit/event state so retrospective explanations cannot drift after policy changes.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-053-T08` Add tests verifying every denial and admission can be explained completely and the explanation references the exact versions used at decision time.  
  **→ PARTIAL** — explain tested for admitted decisions; not for every denial class
- [x] `MC-053-T09` Define a stable telemetry schema with data classification, tenant isolation, privacy/redaction, retention, and export controls.  
  **→ DONE** — PK_ECP_LOG/1 + docs/TELEMETRY_POLICY.md
- [x] `MC-053-T10` Correlate metrics/logs/traces/audit/explain records using bounded stable identifiers without making high-cardinality values default metric labels.  
  **→ DONE** — trace_id/decision_id/request_id across all four
- [x] `MC-053-T11` Instrument normal, denied, degraded, dependency-failed, overloaded, security-attack, and software-defect states distinctly.  
  **→ DONE** — outcome/code counters + degraded mode + shed/quota/authn counters
- [x] `MC-053-T12` Ensure telemetry/export failure cannot block security-critical processing or cause unbounded resource growth; audit data follows its stronger durability rule.  
  **→ DONE** — in-process metrics; synchronous bounded logging; SIEM exporter decoupled
- [ ] `MC-053-T13` Provide dashboards/alerts or operator views with explicit ownership and linked runbooks, then test them synthetically.  
  **→ PARTIAL** — dashboards/alerts with runbooks; ownership UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-053-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-053-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-053-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-053-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-053-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-053-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-053-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-054 — Telemetry retention/privacy/export policy and dashboards/alerts

> **4.3.0 disposition: PARTIAL** · DONE 15 · OPEN_HUMAN 2 · PARTIAL 3  
> Evidence: `docs/TELEMETRY_POLICY.md` (exists), `ops/dashboards/inv66-overview.json` (exists), `ops/alerts/inv66-rules.yml` (exists), `lane:observability` (PASS)

**Severity:** High  
**Related controls:** C079-C080  
**Gap statement:** No retention/sampling/privacy configuration, dashboards, alerts, or incident-oriented signal separation exists.  

### Implementation checklist

- [x] `MC-054-T01` Classify telemetry by data sensitivity and define retention periods separately for metrics, logs, traces, audit events, and explain/inventory data.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-054-T02` Define tenant/PII handling, hashing/pseudonymization, permitted attributes, geographic residency, and access controls for telemetry backends.  
  **→ OPEN_HUMAN** — PII classes and residency need privacy-owner decision
- [x] `MC-054-T03` Define sampling rules for traces/logs and guarantee required security/audit evidence is not sampled away.  
  **→ DONE** — implemented; see evidence
- [x] `MC-054-T04` Configure export buffering, retry, disk spool limits, and drop policy so telemetry outages cannot cause unbounded resource growth or silently discard mandated audit data.  
  **→ DONE** — implemented; see evidence
- [x] `MC-054-T05` Create dashboards for admission volume/latency, denials by class, dependency health, audit/outbox backlog, saturation, policy/config revision, and emergency state.  
  **→ DONE** — implemented; see evidence
- [x] `MC-054-T06` Create alerts that distinguish capacity load, dependency degradation, policy rejection spikes, authentication attack signals, audit-integrity failure, and software defects.  
  **→ DONE** — implemented; see evidence
- [x] `MC-054-T07` Define alert thresholds, routing, severity, deduplication, runbook links, and SLO burn-rate alerts.  
  **→ DONE** — implemented; see evidence
- [x] `MC-054-T08` Periodically test dashboard queries and synthetic alerts in CI/staging to detect observability drift.  
  **→ DONE** — implemented; see evidence
- [x] `MC-054-T09` Define a stable telemetry schema with data classification, tenant isolation, privacy/redaction, retention, and export controls.  
  **→ DONE** — PK_ECP_LOG/1 + docs/TELEMETRY_POLICY.md
- [x] `MC-054-T10` Correlate metrics/logs/traces/audit/explain records using bounded stable identifiers without making high-cardinality values default metric labels.  
  **→ DONE** — trace_id/decision_id/request_id across all four
- [x] `MC-054-T11` Instrument normal, denied, degraded, dependency-failed, overloaded, security-attack, and software-defect states distinctly.  
  **→ DONE** — outcome/code counters + degraded mode + shed/quota/authn counters
- [x] `MC-054-T12` Ensure telemetry/export failure cannot block security-critical processing or cause unbounded resource growth; audit data follows its stronger durability rule.  
  **→ DONE** — in-process metrics; synchronous bounded logging; SIEM exporter decoupled
- [ ] `MC-054-T13` Provide dashboards/alerts or operator views with explicit ownership and linked runbooks, then test them synthetically.  
  **→ PARTIAL** — dashboards/alerts with runbooks; ownership UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-054-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-054-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-054-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-054-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-054-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-054-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-054-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

# Testing, certification, release, and operations

## MC-055 — Public-interface contract test suite

> **4.3.0 disposition: PARTIAL** · DONE 12 · OPEN_HUMAN 1 · PARTIAL 7  
> Evidence: `tests/contract/test_contracts.py` (exists), `tests.contract.test_contracts` (10/10 pass), `tests.integration.test_e2e.EndToEndTest.test_http_errors_are_typed_envelopes` (1/1 pass)

**Severity:** High  
**Related controls:** C082  
**Gap statement:** Local unit tests now cover the engine, but no schema-driven contract tests exist for admit/RBAC/audit APIs.  

### Implementation checklist

- [ ] `MC-055-T01` Generate contract tests directly from the published admission, RBAC, audit, status, and administrative schemas.  
  **→ PARTIAL** — tests use the schemas and a reference validator; not generated from them
- [x] `MC-055-T02` Test every required field, optional field, boundary size/count, enum, version, unknown-field rule, and canonicalization constraint.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-055-T03` Include authorization matrices proving each operation denies principals missing required capability/scope.  
  **→ PARTIAL** — authorization negatives per operation; not a full principal x operation x scope matrix
- [x] `MC-055-T04` Include stable error-code assertions for validation, authn/authz, conflict, quota, timeout, dependency, overload, and internal failures.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-055-T05` Run the same fixtures against in-process handlers and deployed service endpoints to detect adapter/serialization drift.  
  **→ PARTIAL** — same scenarios in-process and over HTTP, but not the same fixture set
- [x] `MC-055-T06` Maintain golden compatibility fixtures for supported older protocol versions.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-055-T07` Include malformed-wire and truncated-message cases rather than testing only deserialized application objects.  
  **→ PARTIAL** — non-JSON and deeply nested bodies tested; truncated bodies not
- [x] `MC-055-T08` Publish machine-readable contract-test results and fail release on any externally claimed interface mismatch.  
  **→ DONE** — implemented; see evidence
- [x] `MC-055-T09` Assign a stable test/evidence ID to each acceptance condition and link it to the RTM and release evidence bundle.  
  **→ DONE** — test ids in release/ci_report.json, linked in release/rtm.json
- [x] `MC-055-T10` Automate the control in CI/CD or scheduled operations wherever objective verification is possible; treat skipped tests as non-evidence unless explicitly approved.  
  **→ DONE** — tools/ci.py; NOT_RUN is never PASS
- [x] `MC-055-T11` Exercise positive, negative, boundary, security, failure, recovery, and upgrade/rollback paths appropriate to the component.  
  **→ DONE** — upgrade/rollback paths only where noted
- [x] `MC-055-T12` Retain machine-readable results, environment/tool versions, raw logs where needed, and cryptographic digests of critical evidence.  
  **→ DONE** — release/ci_report.json + environment + digests
- [ ] `MC-055-T13` Define ownership, review/renewal cadence, waiver handling, and release-blocking conditions for stale or failed evidence.  
  **→ PARTIAL** — cadence + release-blocking defined; owners UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-055-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-055-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-055-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-055-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-055-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-055-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-055-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-056 — End-to-end integration suite

> **4.3.0 disposition: PARTIAL_EXTERNAL_DEPENDENCY** · DONE 12 · OPEN_EXTERNAL 1 · OPEN_HUMAN 1 · PARTIAL 6  
> Evidence: `tests/integration/test_e2e.py` (exists), `tests.integration.test_e2e` (6/6 pass)

**Severity:** Critical  
**Related controls:** C030, C083  
**Gap statement:** No tests exercise a real identity provider, policy engine, provenance verifier, registry, audit store, deployment manager, or wasmCloud lattice.  
**Key cross-component dependencies:** MC-019, MC-020, MC-025, MC-029, MC-033, MC-038  

### Implementation checklist

- [ ] `MC-056-T01` Stand up an end-to-end test environment containing a real/test identity provider, GAP-13 policy engine, registry, GAP-07 provenance verifier, durable stores, INV-63 deployment manager, and representative wasmCloud lattice.  
  **→ OPEN_EXTERNAL** — no real IdP/policy engine/registry/provenance verifier/INV-63/lattice
- [x] `MC-056-T02` Exercise the entire path from authenticated GitOps/API request through admission, durable audit, downstream forwarding, reconciliation acknowledgement, and inventory visibility.  
  **→ DONE** — implemented; see evidence
- [x] `MC-056-T03` Verify a policy/registry/signer/RBAC denial never reaches the deployment manager and produces complete audit/explain evidence.  
  **→ DONE** — implemented; see evidence
- [x] `MC-056-T04` Verify admitted artifacts are identified by immutable digest and the same digest reaches the downstream deployment path.  
  **→ DONE** — implemented; see evidence
- [x] `MC-056-T05` Inject each adjacent dependency failure and assert documented degraded/fail-closed behavior.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-056-T06` Exercise configuration/RBAC/signer rotation and prove new revisions propagate without inconsistent mixed decisions.  
  **→ PARTIAL** — config rotation tested; signer rotation propagation across replicas not tested
- [ ] `MC-056-T07` Run tenant-isolation scenarios with at least two organisations/tenants/lattices concurrently.  
  **→ PARTIAL** — two tenants in one org; no two-organisation run
- [ ] `MC-056-T08` Retain service logs/traces/audit exports plus machine test results as release evidence.  
  **→ PARTIAL** — machine results in ci_report; service logs/traces/audit exports not archived per run
- [x] `MC-056-T09` Assign a stable test/evidence ID to each acceptance condition and link it to the RTM and release evidence bundle.  
  **→ DONE** — test ids in release/ci_report.json, linked in release/rtm.json
- [x] `MC-056-T10` Automate the control in CI/CD or scheduled operations wherever objective verification is possible; treat skipped tests as non-evidence unless explicitly approved.  
  **→ DONE** — tools/ci.py; NOT_RUN is never PASS
- [x] `MC-056-T11` Exercise positive, negative, boundary, security, failure, recovery, and upgrade/rollback paths appropriate to the component.  
  **→ DONE** — upgrade/rollback paths only where noted
- [x] `MC-056-T12` Retain machine-readable results, environment/tool versions, raw logs where needed, and cryptographic digests of critical evidence.  
  **→ DONE** — release/ci_report.json + environment + digests
- [ ] `MC-056-T13` Define ownership, review/renewal cadence, waiver handling, and release-blocking conditions for stale or failed evidence.  
  **→ PARTIAL** — cadence + release-blocking defined; owners UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-056-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-056-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-056-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-056-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-056-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-056-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-056-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-057 — Compatibility/platform test matrix

> **4.3.0 disposition: PARTIAL_EXTERNAL_DEPENDENCY** · DONE 10 · OPEN_EXTERNAL 2 · OPEN_HUMAN 1 · PARTIAL 7  
> Evidence: `.github/workflows/ci.yml` (exists), `release/compatibility.json` (exists)

**Severity:** High  
**Related controls:** C084, C093  
**Gap statement:** No CI matrix validates supported Python/runtime versions, OS/CPU architectures, wasmCloud/Cosmonic versions, or protocol combinations.  

### Implementation checklist

- [x] `MC-057-T01` Define the supported matrix for Python/runtime versions, OS distributions, CPU architectures, container runtime/orchestrator, wasmCloud/Cosmonic versions, and all adjacent protocol versions.  
  **→ DONE** — implemented; see evidence
- [x] `MC-057-T02` Classify combinations as fully supported, limited/experimental, or explicitly unsupported; do not imply support from untested portability.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-057-T03` Run unit/contract/integration smoke tests on every supported baseline combination in CI.  
  **→ PARTIAL** — CI workflow runs the matrix; only one combination executed here
- [ ] `MC-057-T04` Include at least the architectures/sites actually used in production, including ARM64 if edge deployments depend on it.  
  **→ OPEN_EXTERNAL** — ARM64 not run
- [ ] `MC-057-T05` Validate TLS/crypto/provider behavior across platforms where library/backend differences can affect security.  
  **→ OPEN_EXTERNAL** — no cross-platform crypto backend runs
- [ ] `MC-057-T06` Run schema/protocol compatibility tests across N/N-1 rolling upgrade combinations.  
  **→ PARTIAL** — compat fixtures; no N-1 replica run
- [x] `MC-057-T07` Record matrix results and exact dependency versions in release metadata.  
  **→ DONE** — implemented; see evidence
- [x] `MC-057-T08` Block publication of a support claim when its matrix job is missing, skipped, or failing.  
  **→ DONE** — implemented; see evidence
- [x] `MC-057-T09` Assign a stable test/evidence ID to each acceptance condition and link it to the RTM and release evidence bundle.  
  **→ DONE** — test ids in release/ci_report.json, linked in release/rtm.json
- [x] `MC-057-T10` Automate the control in CI/CD or scheduled operations wherever objective verification is possible; treat skipped tests as non-evidence unless explicitly approved.  
  **→ DONE** — tools/ci.py; NOT_RUN is never PASS
- [ ] `MC-057-T11` Exercise positive, negative, boundary, security, failure, recovery, and upgrade/rollback paths appropriate to the component.  
  **→ PARTIAL** — document/tooling only; no executable test for this MC
- [x] `MC-057-T12` Retain machine-readable results, environment/tool versions, raw logs where needed, and cryptographic digests of critical evidence.  
  **→ DONE** — release/ci_report.json + environment + digests
- [ ] `MC-057-T13` Define ownership, review/renewal cadence, waiver handling, and release-blocking conditions for stale or failed evidence.  
  **→ PARTIAL** — cadence + release-blocking defined; owners UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-057-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-057-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-057-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [ ] `MC-057-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ PARTIAL** — verification is document review only, or evidence did not verify
- [ ] `MC-057-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-057-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-057-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-058 — Distributed concurrency/race suite

> **4.3.0 disposition: PARTIAL** · DONE 13 · OPEN_HUMAN 1 · PARTIAL 6  
> Evidence: `tests/concurrency/test_races.py` (exists), `tests.concurrency.test_races` (6/6 pass)

**Severity:** High  
**Related controls:** C086  
**Gap statement:** v4.2.0 adds an in-process concurrency test, but no multi-process/multi-node race, failover, duplicate-delivery, or stale-leader suite exists.  

### Implementation checklist

- [ ] `MC-058-T01` Build multi-process/multi-node tests against the real durable coordination/store layer rather than only Python threads.  
  **→ PARTIAL** — multi-process on one host over the real journal/lease; not multi-node
- [x] `MC-058-T02` Test concurrent admissions for the same idempotency key, same manifest, different tenants, and conflicting policy/config revisions.  
  **→ DONE** — implemented; see evidence
- [x] `MC-058-T03` Test concurrent RBAC/config mutations with stale revision tokens and verify one deterministic outcome without lost updates.  
  **→ DONE** — implemented; see evidence
- [x] `MC-058-T04` Test leader failover/lease expiry/fencing where applicable, including delayed stale leader writes after failover.  
  **→ DONE** — implemented; see evidence
- [x] `MC-058-T05` Test duplicate downstream sends, lost acknowledgements, outbox replay, and deduplication across process restart.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-058-T06` Test concurrent audit appends/query/export and preserve global/partition ordering guarantees as specified.  
  **→ PARTIAL** — concurrent appends verified; concurrent query/export during appends not tested
- [ ] `MC-058-T07` Use stress/race tooling and repeated randomized scheduling to increase probability of exposing timing defects.  
  **→ PARTIAL** — repeated thread/process races; no schedule-perturbation tooling
- [x] `MC-058-T08` Assert invariants automatically: no unauthorized forward, no duplicate logical transition, no audit gap, no cross-tenant state leak.  
  **→ DONE** — implemented; see evidence
- [x] `MC-058-T09` Assign a stable test/evidence ID to each acceptance condition and link it to the RTM and release evidence bundle.  
  **→ DONE** — test ids in release/ci_report.json, linked in release/rtm.json
- [x] `MC-058-T10` Automate the control in CI/CD or scheduled operations wherever objective verification is possible; treat skipped tests as non-evidence unless explicitly approved.  
  **→ DONE** — tools/ci.py; NOT_RUN is never PASS
- [x] `MC-058-T11` Exercise positive, negative, boundary, security, failure, recovery, and upgrade/rollback paths appropriate to the component.  
  **→ DONE** — upgrade/rollback paths only where noted
- [x] `MC-058-T12` Retain machine-readable results, environment/tool versions, raw logs where needed, and cryptographic digests of critical evidence.  
  **→ DONE** — release/ci_report.json + environment + digests
- [ ] `MC-058-T13` Define ownership, review/renewal cadence, waiver handling, and release-blocking conditions for stale or failed evidence.  
  **→ PARTIAL** — cadence + release-blocking defined; owners UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-058-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-058-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-058-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-058-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-058-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-058-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-058-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-059 — Disaster/partition/reconnect certification suite

> **4.3.0 disposition: PARTIAL** · DONE 12 · OPEN_HUMAN 3 · PARTIAL 5  
> Evidence: `tests/disaster/test_disaster.py` (exists), `tests.disaster.test_disaster` (6/6 pass)

**Severity:** High  
**Related controls:** C089  
**Gap statement:** No restore, region/site loss, network partition, reconnect, or degraded-control-plane acceptance tests exist.  

### Implementation checklist

- [x] `MC-059-T01` Define certified recovery scenarios for process/node loss, datastore replica loss, zone/site loss, network partition, dependency region loss, and complete control-plane restore.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-059-T02` Specify RTO/RPO and acceptable degraded capabilities for each scenario.  
  **→ OPEN_HUMAN** — RTO/RPO values PROPOSED
- [x] `MC-059-T03` Test backup restore into clean infrastructure and verify audit chain, policy/config revisions, RBAC, inventory, and pending outbox state.  
  **→ DONE** — implemented; see evidence
- [x] `MC-059-T04` Test site partition where local control continues only within documented leases/staleness limits, then reconcile deterministically on reconnect.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-059-T05` Test DNS/time/KMS/identity/policy outages and recovery sequencing without security bypass.  
  **→ PARTIAL** — policy/deployment outages; DNS/time/KMS/IdP outages not injected
- [x] `MC-059-T06` Test stale controller and duplicate event conditions after reconnect/failover.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-059-T07` Validate operator runbooks by having a separate operator execute them from documented instructions rather than test-author knowledge.  
  **→ OPEN_HUMAN** — runbooks must be executed by a separate operator
- [ ] `MC-059-T08` Produce signed/machine-readable disaster-test evidence with timestamps, environment build, achieved RTO/RPO, and any deviations.  
  **→ PARTIAL** — machine-readable results in ci_report; not signed separately
- [x] `MC-059-T09` Assign a stable test/evidence ID to each acceptance condition and link it to the RTM and release evidence bundle.  
  **→ DONE** — test ids in release/ci_report.json, linked in release/rtm.json
- [x] `MC-059-T10` Automate the control in CI/CD or scheduled operations wherever objective verification is possible; treat skipped tests as non-evidence unless explicitly approved.  
  **→ DONE** — tools/ci.py; NOT_RUN is never PASS
- [x] `MC-059-T11` Exercise positive, negative, boundary, security, failure, recovery, and upgrade/rollback paths appropriate to the component.  
  **→ DONE** — upgrade/rollback paths only where noted
- [x] `MC-059-T12` Retain machine-readable results, environment/tool versions, raw logs where needed, and cryptographic digests of critical evidence.  
  **→ DONE** — release/ci_report.json + environment + digests
- [ ] `MC-059-T13` Define ownership, review/renewal cadence, waiver handling, and release-blocking conditions for stale or failed evidence.  
  **→ PARTIAL** — cadence + release-blocking defined; owners UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-059-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-059-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-059-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-059-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-059-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-059-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-059-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-060 — Full machine-readable production acceptance evidence bundle

> **4.3.0 disposition: PARTIAL_EXTERNAL_DEPENDENCY** · DONE 12 · OPEN_EXTERNAL 1 · OPEN_HUMAN 1 · PARTIAL 6  
> Evidence: `release/evidence.json` (exists), `tools/release.py` (exists)

**Severity:** High  
**Related controls:** C090  
**Gap statement:** v4.2.0 now includes `VERIFICATION.json` for local checks, but no generated 100-item production acceptance evidence exists; the bundled full conformance tests require external `pk_core` and skip when it is absent.  
**Key cross-component dependencies:** MC-007, MC-055, MC-056, MC-057, MC-058, MC-059  

### Implementation checklist

- [x] `MC-060-T01` Define a versioned acceptance-evidence schema covering all C001-C100 items and MC closure status.  
  **→ DONE** — implemented; see evidence
- [x] `MC-060-T02` For each control, include status, evidence type, artifact URI/path, cryptographic digest, test/run ID, environment, timestamp, tool version, and responsible owner/reviewer.  
  **→ DONE** — implemented; see evidence
- [x] `MC-060-T03` Distinguish PASS, FAIL, NOT_APPLICABLE, WAIVED, and BLOCKED states; require rationale/approval/expiry for non-PASS dispositions.  
  **→ DONE** — implemented; see evidence
- [x] `MC-060-T04` Generate evidence automatically from CI, security scans, benchmarks, compatibility matrix, chaos/disaster tests, and documentation checks where possible.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-060-T05` Include `pk_core` conformance evidence or vendor the required runtime so production certification cannot silently skip unavailable tests.  
  **→ OPEN_EXTERNAL** — pk_core not bundled; lane NOT_RUN (waiver W-01)
- [ ] `MC-060-T06` Chain/sign the evidence bundle or include it in signed release provenance to prevent post-release editing.  
  **→ PARTIAL** — bundle covered by the signed release manifest; signature is ephemeral
- [x] `MC-060-T07` Validate that every normative SHALL and every C001-C100 control is covered before producing a production gate result.  
  **→ DONE** — implemented; see evidence
- [x] `MC-060-T08` Archive evidence per release with enough environment metadata to reproduce or investigate the result.  
  **→ DONE** — implemented; see evidence
- [x] `MC-060-T09` Assign a stable test/evidence ID to each acceptance condition and link it to the RTM and release evidence bundle.  
  **→ DONE** — test ids in release/ci_report.json, linked in release/rtm.json
- [x] `MC-060-T10` Automate the control in CI/CD or scheduled operations wherever objective verification is possible; treat skipped tests as non-evidence unless explicitly approved.  
  **→ DONE** — tools/ci.py; NOT_RUN is never PASS
- [ ] `MC-060-T11` Exercise positive, negative, boundary, security, failure, recovery, and upgrade/rollback paths appropriate to the component.  
  **→ PARTIAL** — document/tooling only; no executable test for this MC
- [x] `MC-060-T12` Retain machine-readable results, environment/tool versions, raw logs where needed, and cryptographic digests of critical evidence.  
  **→ DONE** — release/ci_report.json + environment + digests
- [ ] `MC-060-T13` Define ownership, review/renewal cadence, waiver handling, and release-blocking conditions for stale or failed evidence.  
  **→ PARTIAL** — cadence + release-blocking defined; owners UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-060-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-060-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-060-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [ ] `MC-060-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ PARTIAL** — verification is document review only, or evidence did not verify
- [ ] `MC-060-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-060-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-060-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-061 — CI/CD release pipeline and staged rollout artifacts

> **4.3.0 disposition: PARTIAL_EXTERNAL_DEPENDENCY** · DONE 8 · OPEN_EXTERNAL 2 · OPEN_HUMAN 2 · PARTIAL 8  
> Evidence: `.github/workflows/ci.yml` (exists), `.github/workflows/release.yml` (exists), `deploy/rollout.json` (exists), `tools/ci.py` (exists), `tools/release.py` (exists)

**Severity:** Critical  
**Related controls:** C092  
**Gap statement:** No pipeline configuration, signed build, canary/staged rollout, deployment manifest, or automated rollback integration is included.  
**Key cross-component dependencies:** MC-060, MC-062, MC-068  

### Implementation checklist

- [x] `MC-061-T01` Create CI stages for lint/static analysis, unit/property/fuzz tests, schema/contract tests, integration tests, security scans, SBOM/provenance, compatibility matrix, performance gates, packaging, and evidence generation.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-061-T02` Build release artifacts from a clean, pinned environment and sign/attest them; never promote locally built untracked binaries.  
  **→ PARTIAL** — reproducible wheel; signed with ephemeral key; no managed signing identity
- [ ] `MC-061-T03` Define environment promotion dev -> test -> staging -> canary -> production with immutable artifact digests.  
  **→ OPEN_EXTERNAL** — no environments/promotion pipeline available
- [ ] `MC-061-T04` Implement staged/canary rollout using health/SLO/security signals and automatic halt/rollback thresholds.  
  **→ OPEN_EXTERNAL** — no orchestrator for canary
- [ ] `MC-061-T05` Pin deployment manifests/config schema versions and record exact config/policy revisions per rollout.  
  **→ PARTIAL** — schema versions pinned; no per-rollout record of config revision (no rollout system)
- [ ] `MC-061-T06` Implement automated rollback that restores the prior known-good artifact/config without bypassing audit or authorization.  
  **→ PARTIAL** — config rollback automated; artifact rollback procedural
- [ ] `MC-061-T07` Require protected-branch/code-owner approvals and production release authorization by designated roles.  
  **→ OPEN_HUMAN** — branch protection + release environment must be enabled by owners
- [x] `MC-061-T08` Emit machine-readable pipeline/release metadata into the acceptance evidence bundle.  
  **→ DONE** — implemented; see evidence
- [x] `MC-061-T09` Assign a stable test/evidence ID to each acceptance condition and link it to the RTM and release evidence bundle.  
  **→ DONE** — test ids in release/ci_report.json, linked in release/rtm.json
- [x] `MC-061-T10` Automate the control in CI/CD or scheduled operations wherever objective verification is possible; treat skipped tests as non-evidence unless explicitly approved.  
  **→ DONE** — tools/ci.py; NOT_RUN is never PASS
- [ ] `MC-061-T11` Exercise positive, negative, boundary, security, failure, recovery, and upgrade/rollback paths appropriate to the component.  
  **→ PARTIAL** — document/tooling only; no executable test for this MC
- [x] `MC-061-T12` Retain machine-readable results, environment/tool versions, raw logs where needed, and cryptographic digests of critical evidence.  
  **→ DONE** — release/ci_report.json + environment + digests
- [ ] `MC-061-T13` Define ownership, review/renewal cadence, waiver handling, and release-blocking conditions for stale or failed evidence.  
  **→ PARTIAL** — cadence + release-blocking defined; owners UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-061-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-061-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-061-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [ ] `MC-061-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ PARTIAL** — verification is document review only, or evidence did not verify
- [ ] `MC-061-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-061-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-061-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-062 — Vulnerability/SBOM/patch/EOL program artifacts

> **4.3.0 disposition: PARTIAL_EXTERNAL_DEPENDENCY** · DONE 8 · OPEN_EXTERNAL 2 · OPEN_HUMAN 3 · PARTIAL 7  
> Evidence: `release/sbom.cdx.json` (exists), `SECURITY.md` (exists), `docs/SUPPORT_POLICY.md` (exists)

**Severity:** High  
**Related controls:** C094  
**Gap statement:** No SBOM, dependency vulnerability policy, patch SLA, supported-version policy, or EOL schedule is packaged.  

### Implementation checklist

- [ ] `MC-062-T01` Generate SPDX or CycloneDX SBOMs for application dependencies, base image/OS packages, and bundled tools for every release artifact.  
  **→ PARTIAL** — application SBOM only; no base-image/OS package SBOM
- [ ] `MC-062-T02` Run SCA/vulnerability scanning with an approved severity/EPSS/exploitability policy and fail builds according to defined thresholds.  
  **→ OPEN_EXTERNAL** — pip-audit / advisory DB not reachable; lane NOT_RUN
- [ ] `MC-062-T03` Define patch SLAs by severity and exposure, including emergency out-of-band release procedures for actively exploited vulnerabilities.  
  **→ OPEN_HUMAN** — patch SLAs PROPOSED
- [ ] `MC-062-T04` Track supported release branches, minimum dependency versions, end-of-standard-support, and end-of-life dates.  
  **→ OPEN_HUMAN** — EOL dates PROPOSED
- [ ] `MC-062-T05` Monitor new CVEs continuously against released SBOMs, not only at build time.  
  **→ OPEN_EXTERNAL** — continuous CVE monitoring service needed
- [x] `MC-062-T06` Define exception/false-positive workflow with owner, technical rationale, compensating control, expiry, and re-evaluation.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-062-T07` Include license/third-party notice compliance scanning and provenance verification in dependency update workflows.  
  **→ PARTIAL** — license metadata in SBOM; no license scanner run
- [x] `MC-062-T08` Publish vulnerability/SBOM status and unresolved approved exceptions into release evidence.  
  **→ DONE** — implemented; see evidence
- [x] `MC-062-T09` Assign a stable test/evidence ID to each acceptance condition and link it to the RTM and release evidence bundle.  
  **→ DONE** — test ids in release/ci_report.json, linked in release/rtm.json
- [x] `MC-062-T10` Automate the control in CI/CD or scheduled operations wherever objective verification is possible; treat skipped tests as non-evidence unless explicitly approved.  
  **→ DONE** — tools/ci.py; NOT_RUN is never PASS
- [ ] `MC-062-T11` Exercise positive, negative, boundary, security, failure, recovery, and upgrade/rollback paths appropriate to the component.  
  **→ PARTIAL** — document/tooling only; no executable test for this MC
- [x] `MC-062-T12` Retain machine-readable results, environment/tool versions, raw logs where needed, and cryptographic digests of critical evidence.  
  **→ DONE** — release/ci_report.json + environment + digests
- [ ] `MC-062-T13` Define ownership, review/renewal cadence, waiver handling, and release-blocking conditions for stale or failed evidence.  
  **→ PARTIAL** — cadence + release-blocking defined; owners UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-062-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-062-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-062-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [ ] `MC-062-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ PARTIAL** — verification is document review only, or evidence did not verify
- [ ] `MC-062-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-062-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-062-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-063 — Backup/restore/migration/reconstruction runbook

> **4.3.0 disposition: PARTIAL** · DONE 12 · OPEN_HUMAN 3 · PARTIAL 5  
> Evidence: `docs/runbooks/RB-BACKUP.md` (exists), `production/cli.py` (exists), `tests.disaster.test_disaster.DisasterTest.test_backup_restore_reconstructs_identical_state` (1/1 pass), `tests.disaster.test_disaster.DisasterTest.test_restore_detects_tampered_backup` (1/1 pass)

**Severity:** Critical  
**Related controls:** C095  
**Gap statement:** No procedure or tooling exists because durable state itself is absent.  
**Key cross-component dependencies:** MC-038, MC-005  

### Implementation checklist

- [x] `MC-063-T01` Inventory every durable dataset: audit/event log, RBAC/policy data, registry/signer policy, configuration history, inventory projections, outbox/pending operations, and cryptographic metadata.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-063-T02` Define backup frequency, retention, encryption, immutability, geographic location, RPO/RTO, and dependency ordering for each dataset.  
  **→ OPEN_HUMAN** — backup frequency/retention/location PROPOSED
- [ ] `MC-063-T03` Implement automated backups with success/failure monitoring and independent verification that backup objects are readable and complete.  
  **→ PARTIAL** — backup verifies itself; scheduling/monitoring external
- [x] `MC-063-T04` Document restore sequencing, including keys/certificates, datastore schemas, snapshots/log replay, config activation, and dependency reattachment.  
  **→ DONE** — implemented; see evidence
- [x] `MC-063-T05` Define schema migration forward/backward compatibility and rollback strategy for persisted data.  
  **→ DONE** — implemented; see evidence
- [x] `MC-063-T06` Provide reconstruction procedure from authoritative event log when projections/indexes are lost.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-063-T07` Perform periodic clean-room restore tests and verify audit integrity plus pending/idempotency state after recovery.  
  **→ PARTIAL** — clean-room restore automated in tests; periodic schedule human
- [ ] `MC-063-T08` Record backup/restore test evidence, achieved RPO/RTO, and operator sign-off.  
  **→ OPEN_HUMAN** — operator sign-off
- [x] `MC-063-T09` Assign a stable test/evidence ID to each acceptance condition and link it to the RTM and release evidence bundle.  
  **→ DONE** — test ids in release/ci_report.json, linked in release/rtm.json
- [x] `MC-063-T10` Automate the control in CI/CD or scheduled operations wherever objective verification is possible; treat skipped tests as non-evidence unless explicitly approved.  
  **→ DONE** — tools/ci.py; NOT_RUN is never PASS
- [x] `MC-063-T11` Exercise positive, negative, boundary, security, failure, recovery, and upgrade/rollback paths appropriate to the component.  
  **→ DONE** — upgrade/rollback paths only where noted
- [x] `MC-063-T12` Retain machine-readable results, environment/tool versions, raw logs where needed, and cryptographic digests of critical evidence.  
  **→ DONE** — release/ci_report.json + environment + digests
- [ ] `MC-063-T13` Define ownership, review/renewal cadence, waiver handling, and release-blocking conditions for stale or failed evidence.  
  **→ PARTIAL** — cadence + release-blocking defined; owners UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-063-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-063-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-063-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-063-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-063-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-063-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-063-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-064 — Complete day-0/day-1/day-2 operational runbooks

> **4.3.0 disposition: PARTIAL** · DONE 13 · OPEN_HUMAN 2 · PARTIAL 5  
> Evidence: `docs/runbooks/RB-DAY0.md` (exists), `docs/runbooks/RB-DAY1.md` (exists), `docs/runbooks/RB-DAY2.md` (exists), `lane:docs` (PASS)

**Severity:** High  
**Related controls:** C096  
**Gap statement:** README provides only brief commands; production bootstrap, deployment, rotation, failure handling, capacity, restore, and maintenance procedures are missing.  

### Implementation checklist

- [x] `MC-064-T01` Create Day-0 runbook for infrastructure prerequisites, IAM/service identities, certificates, KMS/secrets, stores, DNS/network policy, installation, bootstrap config, and initial trust roots.  
  **→ DONE** — implemented; see evidence
- [x] `MC-064-T02` Create Day-1 runbook for deployment, smoke validation, staged rollout, config/policy activation, initial RBAC/registry/signer setup, and production readiness verification.  
  **→ DONE** — implemented; see evidence
- [x] `MC-064-T03` Create Day-2 runbooks for upgrades, scaling, certificate/key rotation, policy changes, tenant onboarding/offboarding, capacity management, backup verification, and routine maintenance.  
  **→ DONE** — implemented; see evidence
- [x] `MC-064-T04` Add troubleshooting procedures keyed to stable error codes, alerts, dependency health states, and common failure signatures.  
  **→ DONE** — implemented; see evidence
- [x] `MC-064-T05` Include explicit commands/API examples with expected outputs and safety checks; avoid destructive commands without preconditions/rollback.  
  **→ DONE** — implemented; see evidence
- [x] `MC-064-T06` Include rollback and emergency freeze/quarantine procedures, plus criteria for resuming normal operation.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-064-T07` Link every alert/dashboard to the relevant runbook section and owner/escalation route.  
  **→ PARTIAL** — alerts link runbooks; owner/escalation routes UNASSIGNED
- [ ] `MC-064-T08` Test runbooks during game days and update them from observed operator gaps.  
  **→ OPEN_HUMAN** — game days need operators
- [x] `MC-064-T09` Assign a stable test/evidence ID to each acceptance condition and link it to the RTM and release evidence bundle.  
  **→ DONE** — test ids in release/ci_report.json, linked in release/rtm.json
- [x] `MC-064-T10` Automate the control in CI/CD or scheduled operations wherever objective verification is possible; treat skipped tests as non-evidence unless explicitly approved.  
  **→ DONE** — tools/ci.py; NOT_RUN is never PASS
- [ ] `MC-064-T11` Exercise positive, negative, boundary, security, failure, recovery, and upgrade/rollback paths appropriate to the component.  
  **→ PARTIAL** — document/tooling only; no executable test for this MC
- [x] `MC-064-T12` Retain machine-readable results, environment/tool versions, raw logs where needed, and cryptographic digests of critical evidence.  
  **→ DONE** — release/ci_report.json + environment + digests
- [ ] `MC-064-T13` Define ownership, review/renewal cadence, waiver handling, and release-blocking conditions for stale or failed evidence.  
  **→ PARTIAL** — cadence + release-blocking defined; owners UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-064-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-064-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-064-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-064-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-064-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-064-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-064-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-065 — Incident response/on-call runbook

> **4.3.0 disposition: PARTIAL** · DONE 11 · OPEN_HUMAN 4 · PARTIAL 5  
> Evidence: `docs/runbooks/RB-INCIDENT.md` (exists)

**Severity:** High  
**Related controls:** C097  
**Gap statement:** No severity model, paging rules, containment steps, evidence preservation, recovery criteria, or communications workflow exists.  

### Implementation checklist

- [x] `MC-065-T01` Define incident severity levels using customer/security/data-integrity/availability impact and examples specific to unauthorized forwarding, audit integrity failure, identity compromise, and multi-tenant exposure.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-065-T02` Define paging targets, primary/secondary rotations, acknowledgement/escalation timers, and dependency-team engagement paths.  
  **→ OPEN_HUMAN** — rotations UNASSIGNED
- [x] `MC-065-T03` Create containment playbooks for global admission freeze, tenant/lattice quarantine, signer/registry revocation, credential rotation, and deployment forwarding disable.  
  **→ DONE** — implemented; see evidence
- [x] `MC-065-T04` Define evidence-preservation steps for audit anchors, logs, traces, config/policy snapshots, identity records, and affected artifact digests.  
  **→ DONE** — implemented; see evidence
- [x] `MC-065-T05` Define recovery criteria and required verification before clearing emergency controls or returning to normal traffic.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-065-T06` Define communications roles/channels, customer/compliance notification decision process, and status cadence.  
  **→ OPEN_HUMAN** — communications roles undecided
- [x] `MC-065-T07` Require post-incident review with root cause, control failures, corrective actions, owners, due dates, and threat-model/runbook updates.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-065-T08` Exercise the runbook with tabletop and live game-day scenarios at a scheduled cadence.  
  **→ OPEN_HUMAN** — tabletop/game day needs people
- [x] `MC-065-T09` Assign a stable test/evidence ID to each acceptance condition and link it to the RTM and release evidence bundle.  
  **→ DONE** — test ids in release/ci_report.json, linked in release/rtm.json
- [x] `MC-065-T10` Automate the control in CI/CD or scheduled operations wherever objective verification is possible; treat skipped tests as non-evidence unless explicitly approved.  
  **→ DONE** — tools/ci.py; NOT_RUN is never PASS
- [ ] `MC-065-T11` Exercise positive, negative, boundary, security, failure, recovery, and upgrade/rollback paths appropriate to the component.  
  **→ PARTIAL** — document/tooling only; no executable test for this MC
- [x] `MC-065-T12` Retain machine-readable results, environment/tool versions, raw logs where needed, and cryptographic digests of critical evidence.  
  **→ DONE** — release/ci_report.json + environment + digests
- [ ] `MC-065-T13` Define ownership, review/renewal cadence, waiver handling, and release-blocking conditions for stale or failed evidence.  
  **→ PARTIAL** — cadence + release-blocking defined; owners UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-065-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-065-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-065-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [ ] `MC-065-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ PARTIAL** — verification is document review only, or evidence did not verify
- [ ] `MC-065-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-065-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-065-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-066 — Recurring review automation/evidence

> **4.3.0 disposition: PARTIAL** · DONE 9 · OPEN_HUMAN 3 · PARTIAL 8  
> Evidence: `tools/review.py` (exists), `.github/workflows/review.yml` (exists), `release/review.json` (exists), `lane:review` (PASS)

**Severity:** Medium  
**Related controls:** C098  
**Gap statement:** No scheduled access, policy, dependency, configuration, and architecture review mechanism or retained results are present.  

### Implementation checklist

- [ ] `MC-066-T01` Define review frequencies and owners for access/RBAC, elevated grants, break-glass accounts, registry/signer trust, policy bundles, dependencies, config drift, architecture, threat model, and capacity.  
  **→ OPEN_HUMAN** — review owners UNASSIGNED
- [x] `MC-066-T02` Automate extraction of current state and diffs so reviewers assess effective configuration rather than manually assembled screenshots.  
  **→ DONE** — implemented; see evidence
- [x] `MC-066-T03` Flag stale/unused privileges, expired temporary grants, unknown signers/registries, old protocol versions, unsupported dependencies, and unowned exceptions.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-066-T04` Require reviewer disposition, remediation owner, due date, and evidence for every finding.  
  **→ OPEN_HUMAN** — dispositions are human
- [ ] `MC-066-T05` Retain immutable review results and link material findings to tickets/waivers/incidents.  
  **→ PARTIAL** — review output retained per run; no ticket linkage
- [ ] `MC-066-T06` Escalate overdue critical findings and block production exit when required recurring reviews are stale.  
  **→ PARTIAL** — stale review blocks via exit gate reasons; no overdue escalation
- [ ] `MC-066-T07` Add review status to governance dashboards and acceptance evidence.  
  **→ PARTIAL** — review findings not yet surfaced in evidence/dashboards
- [ ] `MC-066-T08` Periodically validate the automation itself against source systems to detect blind spots or partial data extraction.  
  **→ PARTIAL** — extraction checked by tests of its inputs only
- [x] `MC-066-T09` Assign a stable test/evidence ID to each acceptance condition and link it to the RTM and release evidence bundle.  
  **→ DONE** — test ids in release/ci_report.json, linked in release/rtm.json
- [x] `MC-066-T10` Automate the control in CI/CD or scheduled operations wherever objective verification is possible; treat skipped tests as non-evidence unless explicitly approved.  
  **→ DONE** — tools/ci.py; NOT_RUN is never PASS
- [ ] `MC-066-T11` Exercise positive, negative, boundary, security, failure, recovery, and upgrade/rollback paths appropriate to the component.  
  **→ PARTIAL** — document/tooling only; no executable test for this MC
- [x] `MC-066-T12` Retain machine-readable results, environment/tool versions, raw logs where needed, and cryptographic digests of critical evidence.  
  **→ DONE** — release/ci_report.json + environment + digests
- [ ] `MC-066-T13` Define ownership, review/renewal cadence, waiver handling, and release-blocking conditions for stale or failed evidence.  
  **→ PARTIAL** — cadence + release-blocking defined; owners UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-066-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-066-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-066-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-066-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-066-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-066-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-066-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-067 — Exception/waiver/technical-debt register

> **4.3.0 disposition: PARTIAL** · DONE 13 · OPEN_HUMAN 1 · PARTIAL 6  
> Evidence: `release/waivers.json` (exists), `docs/TECH_DEBT.md` (exists), `tools/check_waivers.py` (exists), `lane:waivers` (PASS)

**Severity:** Medium  
**Related controls:** C099  
**Gap statement:** No machine-readable waiver owner, rationale, expiry, compensating control, or deprecation register exists.  

### Implementation checklist

- [x] `MC-067-T01` Create a machine-readable register with unique waiver/debt ID, affected requirement/control, scope/environment, owner, approver, rationale, risk rating, compensating controls, creation date, expiry, and remediation plan.  
  **→ DONE** — implemented; see evidence
- [x] `MC-067-T02` Prohibit indefinite waivers; require explicit expiry/review date and automated notification before expiration.  
  **→ DONE** — implemented; see evidence
- [x] `MC-067-T03` Differentiate temporary security exceptions, functional limitations, technical debt, and deprecation commitments.  
  **→ DONE** — implemented; see evidence
- [x] `MC-067-T04` Link each register entry to RTM rows and production gate results so waived controls cannot appear as ordinary PASS.  
  **→ DONE** — implemented; see evidence
- [x] `MC-067-T05` Block release on expired waivers or waivers missing required approval for their risk level.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-067-T06` Provide trend reporting for open/aging debt and repeated extensions.  
  **→ PARTIAL** — no trend reporting yet (single release)
- [ ] `MC-067-T07` Require closure evidence demonstrating the underlying control is implemented and verified before marking a waiver resolved.  
  **→ PARTIAL** — no waiver-resolution workflow yet
- [x] `MC-067-T08` Archive historical entries without deleting their relationship to prior releases.  
  **→ DONE** — implemented; see evidence
- [x] `MC-067-T09` Assign a stable test/evidence ID to each acceptance condition and link it to the RTM and release evidence bundle.  
  **→ DONE** — test ids in release/ci_report.json, linked in release/rtm.json
- [x] `MC-067-T10` Automate the control in CI/CD or scheduled operations wherever objective verification is possible; treat skipped tests as non-evidence unless explicitly approved.  
  **→ DONE** — tools/ci.py; NOT_RUN is never PASS
- [ ] `MC-067-T11` Exercise positive, negative, boundary, security, failure, recovery, and upgrade/rollback paths appropriate to the component.  
  **→ PARTIAL** — document/tooling only; no executable test for this MC
- [x] `MC-067-T12` Retain machine-readable results, environment/tool versions, raw logs where needed, and cryptographic digests of critical evidence.  
  **→ DONE** — release/ci_report.json + environment + digests
- [ ] `MC-067-T13` Define ownership, review/renewal cadence, waiver handling, and release-blocking conditions for stale or failed evidence.  
  **→ PARTIAL** — cadence + release-blocking defined; owners UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-067-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-067-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-067-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-067-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-067-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-067-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-067-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-068 — Formal production exit gate artifact

> **4.3.0 disposition: PARTIAL_EXTERNAL_DEPENDENCY** · DONE 11 · OPEN_EXTERNAL 1 · OPEN_HUMAN 2 · PARTIAL 6  
> Evidence: `tools/release.py` (exists), `release/exit_gate.json` (exists)

**Severity:** Critical  
**Related controls:** C100  
**Gap statement:** No signed/generated gate proves architecture, security, resilience, performance, observability, testing, rollback, and ownership readiness; current generic checklist satisfaction is insufficient production evidence.  
**Key cross-component dependencies:** MC-007, MC-060, MC-061, MC-064, MC-065  

### Implementation checklist

- [x] `MC-068-T01` Define a signed/generated production exit-gate schema with independent sections for architecture, requirements, interfaces, implementation/config, security, resilience, performance, observability, testing, rollback, operations, ownership, and governance.  
  **→ DONE** — implemented; see evidence
- [x] `MC-068-T02` Require every C001-C100 item to resolve to PASS, approved NOT_APPLICABLE, or unexpired approved waiver; BLOCKED/FAIL prevents production GO.  
  **→ DONE** — implemented; see evidence
- [x] `MC-068-T03` Consume evidence from the RTM/acceptance bundle by immutable digest rather than manually copying status into a report.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-068-T04` Require named approvals from service owner, architecture, security, SRE/operations, and release authority according to organisational policy.  
  **→ OPEN_HUMAN** — named approvals missing
- [ ] `MC-068-T05` Include exact artifact digest, source revision, deployment manifest version, config/policy revision, compatibility matrix, and evidence-bundle digest.  
  **→ PARTIAL** — artifact digest, source tree, compatibility, evidence digests; no deployment-manifest version or production config revision
- [x] `MC-068-T06` Automate objective checks and isolate subjective approvals so humans cannot override missing machine evidence without a formal waiver.  
  **→ DONE** — implemented; see evidence
- [x] `MC-068-T07` Generate a reproducible gate report with timestamp and decision inputs and retain it with the release.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-068-T08` Prevent deployment/promotion automation from bypassing a non-GO gate except through an explicitly audited break-glass process.  
  **→ OPEN_EXTERNAL** — no deployment automation to enforce the gate
- [x] `MC-068-T09` Assign a stable test/evidence ID to each acceptance condition and link it to the RTM and release evidence bundle.  
  **→ DONE** — test ids in release/ci_report.json, linked in release/rtm.json
- [x] `MC-068-T10` Automate the control in CI/CD or scheduled operations wherever objective verification is possible; treat skipped tests as non-evidence unless explicitly approved.  
  **→ DONE** — tools/ci.py; NOT_RUN is never PASS
- [ ] `MC-068-T11` Exercise positive, negative, boundary, security, failure, recovery, and upgrade/rollback paths appropriate to the component.  
  **→ PARTIAL** — document/tooling only; no executable test for this MC
- [x] `MC-068-T12` Retain machine-readable results, environment/tool versions, raw logs where needed, and cryptographic digests of critical evidence.  
  **→ DONE** — release/ci_report.json + environment + digests
- [ ] `MC-068-T13` Define ownership, review/renewal cadence, waiver handling, and release-blocking conditions for stale or failed evidence.  
  **→ PARTIAL** — cadence + release-blocking defined; owners UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-068-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-068-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-068-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [ ] `MC-068-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ PARTIAL** — verification is document review only, or evidence did not verify
- [ ] `MC-068-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-068-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-068-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-069 — Cross-lattice inventory service

> **4.3.0 disposition: PARTIAL_EXTERNAL_DEPENDENCY** · DONE 11 · OPEN_EXTERNAL 1 · OPEN_HUMAN 1 · PARTIAL 7  
> Evidence: `production/service.py` (exists), `schemas/PK_ECP_INVENTORY_1.schema.json` (exists), `tests.unit.test_additions.AdditionsTest.test_inventory_pagination` (1/1 pass)

**Severity:** High  
**Related controls:** C002, C011, C021  
**Gap statement:** `contract.py` says INV-66 owns cross-lattice inventory, but the implementation only retains admitted manifest copies and offers no queryable, durable inventory model.  
**Key cross-component dependencies:** MC-019, MC-038  

### Implementation checklist

- [ ] `MC-069-T01` Define a durable cross-lattice inventory model with organisation/tenant, site, lattice, workload/application, component/provider, artifact digest, desired revision, observed revision/state, and timestamps.  
  **→ PARTIAL** — tenant/lattice/app/component/image/manifest digest/state/decision/seq; no site, desired vs observed revision or timestamps
- [ ] `MC-069-T02` Distinguish desired/admitted state from downstream observed runtime state; record source and freshness for each field.  
  **→ PARTIAL** — desired/admitted + delivery revision; observed runtime state not ingested (debt D-02)
- [ ] `MC-069-T03` Ingest updates from INV-63/wasmCloud reconciliation using authenticated event/query mechanisms with idempotent versioned updates.  
  **→ OPEN_EXTERNAL** — no authenticated INV-63/wasmCloud status feed
- [x] `MC-069-T04` Provide scoped query APIs for tenant/lattice/workload/artifact/revision and pagination suitable for large fleets.  
  **→ DONE** — implemented; see evidence
- [x] `MC-069-T05` Enforce tenant isolation and administrative scope on inventory reads; avoid global inventory disclosure by default.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-069-T06` Define stale/offline semantics and last-observed timestamps for disconnected sites.  
  **→ PARTIAL** — no last-observed timestamps for disconnected sites
- [x] `MC-069-T07` Correlate inventory entries with admission event, GitOps commit, policy/config revision, and deployment acknowledgement.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-069-T08` Add reconciliation tests for out-of-order/duplicate updates, site reconnect, deletion/tombstone, drift, and projection rebuild from source events.  
  **→ PARTIAL** — rebuild-from-events tested; out-of-order/tombstone/drift not tested
- [x] `MC-069-T09` Assign a stable test/evidence ID to each acceptance condition and link it to the RTM and release evidence bundle.  
  **→ DONE** — test ids in release/ci_report.json, linked in release/rtm.json
- [x] `MC-069-T10` Automate the control in CI/CD or scheduled operations wherever objective verification is possible; treat skipped tests as non-evidence unless explicitly approved.  
  **→ DONE** — tools/ci.py; NOT_RUN is never PASS
- [x] `MC-069-T11` Exercise positive, negative, boundary, security, failure, recovery, and upgrade/rollback paths appropriate to the component.  
  **→ DONE** — upgrade/rollback paths only where noted
- [x] `MC-069-T12` Retain machine-readable results, environment/tool versions, raw logs where needed, and cryptographic digests of critical evidence.  
  **→ DONE** — release/ci_report.json + environment + digests
- [ ] `MC-069-T13` Define ownership, review/renewal cadence, waiver handling, and release-blocking conditions for stale or failed evidence.  
  **→ PARTIAL** — cadence + release-blocking defined; owners UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-069-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-069-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-069-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-069-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-069-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-069-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-069-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-070 — Audit query/retention/export service

> **4.3.0 disposition: PARTIAL** · DONE 10 · OPEN_HUMAN 1 · PARTIAL 9  
> Evidence: `production/journal.py` (exists), `production/siem.py` (exists), `tests.unit.test_additions.AdditionsTest.test_siem_export_checkpoint_retry_dedupe` (1/1 pass), `tests.unit.test_additions.AdditionsTest.test_audit_cursor_pagination_and_filters` (1/1 pass)

**Severity:** High  
**Related controls:** C049, C079, C095  
**Gap statement:** The local audit snapshot has no retention class, index/query API, immutable export, SIEM integration, legal hold, or archival lifecycle.  
**Key cross-component dependencies:** MC-014, MC-035, MC-038  

### Implementation checklist

- [ ] `MC-070-T01` Implement durable indexed audit storage and a read/query service independent from the in-process `audit` snapshot.  
  **→ PARTIAL** — journal scan with filters; no secondary index
- [ ] `MC-070-T02` Define retention classes by event type/tenant/regulatory need, hot vs archive tiers, legal hold, and immutable deletion governance.  
  **→ PARTIAL** — one retention class + legal holds; no per-event-type/tenant classes or archive tier
- [ ] `MC-070-T03` Index common dimensions such as time, tenant, lattice, principal, workload, action/outcome, request/trace ID, artifact digest, and event ID while controlling cost/cardinality.  
  **→ PARTIAL** — filters on time/tenant/lattice/principal/request/decision; not indexed
- [ ] `MC-070-T04` Provide cursor-based pagination and bounded query ranges; protect service with authorization, rate limits, and query-cost controls.  
  **→ PARTIAL** — cursor pagination + limits; no rate limit or query-cost control on audit reads
- [x] `MC-070-T05` Provide cryptographically verifiable export bundles including event segment roots/anchors, schema version, and manifest digest.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-070-T06` Implement SIEM/security-lake export with durable checkpointing, idempotent delivery, retries, backpressure, and dead-letter visibility.  
  **→ PARTIAL** — exporter implemented; no real SIEM
- [x] `MC-070-T07` Ensure retention/archival operations preserve tamper-evident chain/anchor verification and legal-hold records.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-070-T08` Add tests for large-range queries, tenant access control, archived retrieval, export verification, SIEM outage/recovery, and restore from backup.  
  **→ PARTIAL** — tenant access control, export verification, SIEM outage/recovery, restore tested; large-range and archived retrieval not
- [x] `MC-070-T09` Assign a stable test/evidence ID to each acceptance condition and link it to the RTM and release evidence bundle.  
  **→ DONE** — test ids in release/ci_report.json, linked in release/rtm.json
- [x] `MC-070-T10` Automate the control in CI/CD or scheduled operations wherever objective verification is possible; treat skipped tests as non-evidence unless explicitly approved.  
  **→ DONE** — tools/ci.py; NOT_RUN is never PASS
- [x] `MC-070-T11` Exercise positive, negative, boundary, security, failure, recovery, and upgrade/rollback paths appropriate to the component.  
  **→ DONE** — upgrade/rollback paths only where noted
- [x] `MC-070-T12` Retain machine-readable results, environment/tool versions, raw logs where needed, and cryptographic digests of critical evidence.  
  **→ DONE** — release/ci_report.json + environment + digests
- [ ] `MC-070-T13` Define ownership, review/renewal cadence, waiver handling, and release-blocking conditions for stale or failed evidence.  
  **→ PARTIAL** — cadence + release-blocking defined; owners UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-070-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-070-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-070-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [x] `MC-070-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ DONE** — verified in a clean container run of tools/ci.py; hosted CI not run
- [ ] `MC-070-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-070-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-070-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

## MC-071 — Repository license/notice metadata

> **4.3.0 disposition: PARTIAL** · DONE 9 · OPEN_HUMAN 5 · PARTIAL 6  
> Evidence: `LICENSE-STATUS.md` (exists), `THIRD-PARTY-NOTICES.md` (exists)

**Severity:** Medium  
**Related controls:** repository governance  
**Gap statement:** No `LICENSE` or `NOTICE` is bundled, so redistribution and third-party compliance terms are not established by this archive. *(repository governance artifact; not represented by a dedicated checklist item)*  

### Implementation checklist

- [ ] `MC-071-T01` Select and document the repository/project license with legal/owner approval and add the complete `LICENSE` text at repository root.  
  **→ OPEN_HUMAN** — license selection is an owner/legal decision
- [ ] `MC-071-T02` Add `NOTICE` when required by the chosen license or bundled third-party works and include copyright/attribution information.  
  **→ OPEN_HUMAN** — depends on T01
- [x] `MC-071-T03` Inventory third-party dependencies/assets and verify their licenses are compatible with distribution and intended deployment.  
  **→ DONE** — implemented; see evidence
- [x] `MC-071-T04` Generate/maintain third-party notices from the dependency lock/SBOM to prevent manual drift.  
  **→ DONE** — implemented; see evidence
- [ ] `MC-071-T05` Add source-file headers only where organisational policy/license requires them; avoid inconsistent or misleading headers.  
  **→ OPEN_HUMAN** — depends on policy
- [ ] `MC-071-T06` Add packaging rules ensuring LICENSE/NOTICE/third-party notices ship in source and binary/container distributions where required.  
  **→ OPEN_HUMAN** — depends on T01
- [ ] `MC-071-T07` Add CI compliance checks for missing/forbidden licenses and new dependency-license changes.  
  **→ PARTIAL** — license lane present but NOT_RUN
- [x] `MC-071-T08` Record license metadata in `pyproject.toml`, SBOM, release manifest, and README.  
  **→ DONE** — implemented; see evidence
- [x] `MC-071-T09` Assign a stable test/evidence ID to each acceptance condition and link it to the RTM and release evidence bundle.  
  **→ DONE** — test ids in release/ci_report.json, linked in release/rtm.json
- [x] `MC-071-T10` Automate the control in CI/CD or scheduled operations wherever objective verification is possible; treat skipped tests as non-evidence unless explicitly approved.  
  **→ DONE** — tools/ci.py; NOT_RUN is never PASS
- [ ] `MC-071-T11` Exercise positive, negative, boundary, security, failure, recovery, and upgrade/rollback paths appropriate to the component.  
  **→ PARTIAL** — document/tooling only; no executable test for this MC
- [x] `MC-071-T12` Retain machine-readable results, environment/tool versions, raw logs where needed, and cryptographic digests of critical evidence.  
  **→ DONE** — release/ci_report.json + environment + digests
- [ ] `MC-071-T13` Define ownership, review/renewal cadence, waiver handling, and release-blocking conditions for stale or failed evidence.  
  **→ PARTIAL** — cadence + release-blocking defined; owners UNASSIGNED

### Definition of done / acceptance evidence

- [x] `MC-071-D01` Implementation/artifact is stored in version control at a documented path, included in the release BOM where applicable, and protected by normal review controls.  
  **→ DONE** — artifacts in version control at documented paths and in RELEASE_MANIFEST.sha256
- [ ] `MC-071-D02` All referenced schemas/configs/tests are version-pinned; no acceptance claim depends on an unversioned “latest” external artifact.  
  **→ PARTIAL** — exact version pins, schemas/fixtures versioned; hash-locked constraints pending (W-09)
- [ ] `MC-071-D03` RTM entries identify the implementation artifact, verification test/evidence IDs, owner, release version, and any approved waiver.  
  **→ PARTIAL** — RTM rows carry artifacts, tests, release, waivers; owner field empty (UNASSIGNED)
- [ ] `MC-071-D04` Automated verification passes in a clean CI or production-like environment; required tests are not silently skipped because an external tool/dependency is absent.  
  **→ PARTIAL** — verification is document review only, or evidence did not verify
- [ ] `MC-071-D05` Security/architecture/operations review is complete at the level required by severity, with unresolved risks captured in the waiver/debt register.  
  **→ OPEN_HUMAN** — no independent security/architecture/operations review recorded (release/reviews.jsonl)
- [x] `MC-071-D06` Release acceptance evidence contains immutable hashes/IDs for the artifacts and test runs that close this component.  
  **→ DONE** — release/evidence.json + RELEASE_MANIFEST.sha256 digests (signature ephemeral)
- [x] `MC-071-D07` Documentation/runbooks are updated so an operator can inspect status, diagnose failure, and perform rollback/recovery without relying on implementation-author memory.  
  **→ DONE** — docs/runbooks + component docs

---

# Program-level closure gate

The 71-component remediation program is complete only when all applicable component checklists satisfy their Definition of Done and the following aggregate gates pass:

- [ ] `INV66-PROG-01` All 71 MC IDs have an RTM disposition and no Critical/High item is silently omitted.
- [ ] `INV66-PROG-02` All C001-C100 controls have machine-readable evidence or an approved, unexpired N/A/waiver disposition.
- [ ] `INV66-PROG-03` Admission, RBAC, audit, inventory, configuration, and administrative interfaces are versioned typed contracts with compatibility tests.
- [ ] `INV66-PROG-04` Authentication, capability-based authorization, policy evaluation, artifact signature/provenance verification, and tenant isolation are enforced end to end.
- [ ] `INV66-PROG-05` Authoritative state and audit data are durable, recoverable, tamper-evident, replicated as required, and validated by crash/partition/restore tests.
- [ ] `INV66-PROG-06` No unadmitted manifest can be forwarded under success, failure, retry, restart, failover, stale-leader, or partition scenarios.
- [ ] `INV66-PROG-07` Performance and capacity tests demonstrate the approved latency/throughput/resource thresholds and release regression gates pass.
- [ ] `INV66-PROG-08` Health, metrics, logs, traces, explain views, dashboards, alerts, retention/privacy controls, and runbook links are operationally validated.
- [ ] `INV66-PROG-09` E2E, compatibility, fuzz/security, concurrency, fault-injection, disaster/reconnect, benchmark/soak, backup/restore, and rollout/rollback suites pass in production-like environments.
- [ ] `INV66-PROG-10` SBOM, vulnerability status, license/notice, signed build/provenance, deployment artifacts, config/policy revisions, and full acceptance evidence are attached to the release.
- [ ] `INV66-PROG-11` Day-0/1/2 and incident runbooks have been exercised by operators; ownership/escalation rotations are live.
- [ ] `INV66-PROG-12` MC-068 production exit gate yields GO with the exact release artifact and evidence-bundle digests intended for production promotion.

## Checklist metrics

- Missing components covered: **71 / 71**
- Critical components: **26**
- High components: **39**
- Medium components: **5**
- Low/conditional components: **1**
- Component-level checkboxes generated: **1,420**
- Program-level gates: **12**
