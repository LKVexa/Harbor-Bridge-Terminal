# License status (MC-071)

**No license has been selected for INV-66.** Choosing one is a legal/owner decision, so this
repository does not assume one: there is no `LICENSE` file, no `license` field in `pyproject.toml`,
and no license classifier. Until the owner chooses, all rights are reserved by the copyright holder
and redistribution terms are undefined.

To close MC-071: choose a license → add `LICENSE` (and `NOTICE` if required) → set `license` in
`pyproject.toml` and the SBOM → turn on the `license` lane in `tools/ci.py` (currently **NOT RUN**).
