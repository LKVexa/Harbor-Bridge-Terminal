# Runtime pins (MC-028-T02). Hash-pinning (--require-hashes) is PENDING: PyPI was unreachable from the
# build environment, so no distribution hashes could be recorded (waiver W-09). Update workflow:
# bump here -> CI (all lanes) -> SBOM regen -> release notes.
cryptography==46.0.7
# test-only
jsonschema==4.26.0
