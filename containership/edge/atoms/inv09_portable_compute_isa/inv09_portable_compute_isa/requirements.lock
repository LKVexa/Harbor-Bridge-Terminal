# M45 pinned, hash-locked install set used for the v4.3.0 evidence run.
# Hashes must be regenerated with `pip-compile --generate-hashes` in the release CI;
# this environment had no PyPI access, so versions are recorded from the build host.
cryptography==46.0.7
jsonschema==4.26.0
