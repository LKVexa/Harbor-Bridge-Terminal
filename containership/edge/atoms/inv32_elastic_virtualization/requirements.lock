# INV-32 dependency lock (v4.3.0).  Runtime: NONE (python stdlib only).
# Build backend pins (hashes recorded when resolved on the release runner -- BLOCKED WS20 hosted CI):
setuptools==79.0.1
wheel==0.42.0
# Optional extra [inventory]: pk_core>=1.0,<2.0 -- unpinned, no artifact supplied (ADR-0002, BLOCKED).
