{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:pk:schema:PK_MOVE_COST:1",
  "title": "PK_MOVE_COST/1",
  "oneOf": [
    {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "from", "to", "size_gb", "egress_per_gb", "locality_multiplier", "total"],
      "properties": {
        "kind": {"const": "move-data"},
        "from": {"type": "string", "minLength": 1},
        "to": {"type": "string", "minLength": 1},
        "size_gb": {"type": "number", "minimum": 0},
        "egress_per_gb": {"type": "number", "minimum": 0},
        "locality_multiplier": {"type": "number", "minimum": 0},
        "total": {"type": "number", "minimum": 0}
      }
    },
    {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "from", "to", "base_compute_move_cost", "locality_multiplier", "total"],
      "properties": {
        "kind": {"const": "move-compute"},
        "from": {"type": "string", "minLength": 1},
        "to": {"type": "string", "minLength": 1},
        "base_compute_move_cost": {"type": "number", "minimum": 0},
        "locality_multiplier": {"type": "number", "minimum": 0},
        "total": {"type": "number", "minimum": 0}
      }
    },
    {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "from", "to", "total"],
      "properties": {
        "kind": {"const": "none"},
        "from": {"type": "string", "minLength": 1},
        "to": {"type": "string", "minLength": 1},
        "total": {"const": 0}
      }
    }
  ]
}
