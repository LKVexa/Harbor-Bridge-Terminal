# INV-38-C043 ambient authority inventory + elimination.
filesystem: {policy: allowlist, writable_paths: [/var/lib/inv38/state]}
network:    {policy: allowlist, destinations: [otlp-collector.internal]}
device:     {policy: explicit-assignment, wildcard_prohibited: true}
kernel:     {policy: minimal-ioctls, generic_passthrough: prohibited}
ipc:        {inherited_fds: sanitized}
env:        {secrets_in_env: prohibited}
