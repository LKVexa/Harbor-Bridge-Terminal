# Compatibility tests (INV-38-C084)
Only the reference row (`test_reference_row.py`) runs here; hardware rows in
`compatibility/test-matrix.yaml` are `QUALIFICATION-REQUIRED` and BLOCKED without
real CPU/hypervisor/provider/NIC platforms.
