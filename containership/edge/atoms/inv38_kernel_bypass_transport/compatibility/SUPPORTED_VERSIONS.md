# Supported versions (INV-38-C093)

Machine-readable: `compatibility/supported-versions.yaml` (mirrored by
`version_support.py`). INV-38 4.2.x is compatible with `pk_core` 1.4–2.x, INV-35/
36/37 4.x, GAP-12 1.x, schema 1–2. Policy: N/N-1, 180-day deprecation notice.
Startup preflight rejects unsupported combinations. Rows for real backend stacks
are `IN_PROGRESS` pending qualification.
