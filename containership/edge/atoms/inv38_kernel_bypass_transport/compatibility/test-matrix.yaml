# INV-38-C084 compatibility matrix. Only the reference row is testable here.
axes: [cpu_arch, os_kernel, python, hypervisor, provider, nic, driver_firmware, rdma_provider, schema_version, adjacent_version]
rows:
  - {id: REF-0, cpu_arch: any, python: ">=3.11", provider: reference-model, schema_version: 1, status: TESTED}
  - {id: HW-1, cpu_arch: x86_64, provider: rdma-core, nic: mellanox-cx6, status: QUALIFICATION-REQUIRED}
unsupported_until_qualified: [HW-1]
