# Performance harness (INV-38-C061/C063/C070/C088)
Model-level microbenchmark harness only. Real latency/throughput/power numbers are
BLOCKED on RDMA hardware; run `benchmarks/*-plan.yaml` on qualified hardware to
populate `benchmarks/*-results.json` and an approved baseline.
