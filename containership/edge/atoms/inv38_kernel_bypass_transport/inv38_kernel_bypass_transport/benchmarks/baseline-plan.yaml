# INV-38-C061 baseline plan (BLOCKED on hardware for actual numbers).
environment_fields: [cpu_model, numa, mem_speed, nic_model, pcie_topology, firmware, driver, kernel, hypervisor, backend_lib, mtu, power_mode]
pinned: {source_revision: required, config: required, message_sizes: [64,512,4096,65536], queue_depths: [1,8,32], warmup_s: 5, run_s: 60, repetitions: 20, stat: "p50/p95/p99/p99.9/max + CI"}
metrics: [post_to_completion_latency, throughput, cpu_util, ctx_switches, pinned_memory, queue_occupancy, startup_s, fallback_switch_latency, recovery_time]
baseline_comparison: kernel-path
