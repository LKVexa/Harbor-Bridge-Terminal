profiles:
  datacenter:   {rdma: mandatory, iommu: required, sriov: preferred, fallback: kernel, hugepages: required}
  cloud_vm:     {rdma: preferred, iommu: assumed_virtual, sriov: if_available, fallback: kernel}
  near_edge:    {rdma: preferred, mem: constrained, fallback: kernel, max_regions: 256}
  far_edge:     {rdma: unsupported, fallback: kernel_only, max_regions: 64, disconnected: queue_or_reject}
external_contract_invariant: same PK_BYPASS_* contract holds across profiles even when fast-path differs
