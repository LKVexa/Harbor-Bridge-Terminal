# Review records (INV-38-C098)
Signed/reviewed records for each recurring review in `governance/review-schedule.yaml`.
`governance_checks.overdue_reviews` blocks release when a mandatory review is overdue.
