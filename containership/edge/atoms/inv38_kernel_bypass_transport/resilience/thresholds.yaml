# INV-38-C052 health/stall thresholds. Mirrors health_stall.py::Thresholds.
oldest_inflight_warn_s: 0.005
oldest_inflight_fail_s: 0.050
min_completion_progress_per_window: 1
healthy_samples_to_recover: 3
remediation:
  DEGRADED: [alert, shed_load]
  FAILED: [switch_to_kernel_fallback, drain, require_operator]
safety:
  # zero-budget SLOs page immediately
  memory_safety_violation: page_immediate
  sealing_violation: page_immediate
