# INV-38-C064 per-tenant overhead plan.
workload_classes: [small_msg_high_rate, large_msg_low_rate, high_region_churn, many_connections]
measure: [cpu, memory, pinned_memory, nic_queues, dma_mappings, bandwidth, completion_processing, telemetry_overhead]
fairness: [p50, p95, p99, throughput_share, queue_wait, rejection_rate, fallback_rate]
budgets: reject-if-exceeds-pinned-memory-or-device-queue-limit
