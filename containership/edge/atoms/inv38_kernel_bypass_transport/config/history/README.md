# Configuration history (INV-38-C036)

Append-only, redacted provenance entries produced by `config_provenance.py`.
Each entry binds config identity, author, source revision and content digest
without copying secrets. Entries are immutable; corrections add a new entry.
See `config/provenance.schema.json`.
