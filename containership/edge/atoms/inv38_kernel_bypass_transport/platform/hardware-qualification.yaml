# INV-38-C031 hardware qualification record (BLOCKED: no hardware in build env).
status: NOT_QUALIFIED
blocker: No physical RDMA NIC / IOMMU available in the build environment.
axes: [nic_model, pcie_iommu_topology, driver, firmware, kernel, hypervisor, cpu_arch]
required_checks: [iommu_isolation, dma_mapping_permissions, mr_pinning_limits, safe_device_reset]
