#!/bin/sh
# sub_mssl_to_lctlc/DF_Fabric/VERIFY -- delegates to the ship (uc.py verify sub_mssl_to_lctlc). Offline; NETWORK=deny.
set -eu
HERE="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
PY="${PYTHON:-python3}"
exec "$PY" -B "$HERE/../../../uc.py" verify sub_mssl_to_lctlc "$@"
