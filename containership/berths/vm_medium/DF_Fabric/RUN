#!/bin/sh
# vm_medium/DF_Fabric/RUN -- delegates to the ship (uc.py run vm_medium). Offline; NETWORK=deny.
set -eu
HERE="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
PY="${PYTHON:-python3}"
exec "$PY" -B "$HERE/../../../uc.py" run vm_medium "$@"
