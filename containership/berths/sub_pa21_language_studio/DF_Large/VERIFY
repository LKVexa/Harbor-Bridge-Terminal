#!/bin/sh
# sub_pa21_language_studio/DF_Large/VERIFY -- delegates to the ship (uc.py verify sub_pa21_language_studio). Offline; NETWORK=deny.
set -eu
HERE="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
PY="${PYTHON:-python3}"
exec "$PY" -B "$HERE/../../../uc.py" verify sub_pa21_language_studio "$@"
