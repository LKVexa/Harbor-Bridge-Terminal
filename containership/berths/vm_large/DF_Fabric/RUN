#!/bin/sh
# vm_large/DF_Fabric/RUN -- delegates to the ship (uc.py run vm_large). Offline; NETWORK=deny.
set -eu
HERE="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
PY="${PYTHON:-python3}"
exec "$PY" -B "$HERE/../../../uc.py" run vm_large "$@"
