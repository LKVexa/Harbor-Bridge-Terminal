#!/bin/sh
# vm_large/DF_Medium/VERIFY -- delegates to the ship (uc.py verify vm_large). Offline; NETWORK=deny.
set -eu
HERE="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
PY="${PYTHON:-python3}"
exec "$PY" -B "$HERE/../../../uc.py" verify vm_large "$@"
