# Containership integration

Use the parent `TERMINAL.cmd` / `START.cmd`, not the standalone Electron launcher. Read `../README.md` and `../docs/vws/`.

The original README, e/, ledger/ and release/review records supplied with RAMWS may contain earlier test claims. Treat those as historical. Current evidence lives at `../evidence/uc240/` and `../docs/vws/VALIDATION.md`.
