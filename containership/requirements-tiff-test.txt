# Optional independent TIFF fixture encoder only; not a production runtime dependency.
# These tested versions together require Python 3.12+; not an automatic install step.
Pillow==12.3.0
numpy==2.3.5
tifffile==2026.5.15
